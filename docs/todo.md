# Backlog

Top of list = pick next. Each item maps to a row in `docs/execution-map.md`. When a slice is done, remove it and add the next downstream slice.

Development-sequencing gate (D-043): no Stage-3 item — Phase 5 quality boosters, the Phase 8 access/visibility/audit/retention slices, or Phase 9 host seams — may be picked until the Stage-2 exit criteria are met. Stage-2 operationalization items (provider hardening, schema-migration tooling, raw-data durability/backup, evaluation/observability) come first. See `docs/product/BuildPlan.md` "Development Sequencing". The Stage-2 items are decomposed and ordered as packet groups `OP-1`..`OP-5` in `docs/OPERATIONALIZATION-ROADMAP.md` (D-044) — pick the next Stage-2 packet from there.

## Subject scoping (Milestone H, D-097) — in progress
- Milestone scope: add an opaque, community-scoped, **nullable** `subject_id` dimension on the closed community-scoped data plane (D-097). Decomposed in `docs/SUBJECT-SCOPING-ROADMAP.md`; sequenced separately from the Phase/Stage axis. Map: execution-map "Subject scoping (`subject_id` dimension, D-097)". H-0 (D-097) + H-1 (data model) landed.
- **H-2 — adapter-axis subject assignment: done (H-2; enforces D-097, no new decision-log entry).** New adapter-owned `adapters/telegram/subject.py` `resolve_subject_id(community_id) -> str | None` (default single-subject mapping returns `None` = community-wide); resolved `subject_id` crosses on the new defaulted `InboundMessage.subject_id`, set at the webhook edge; `domain_service.ingest` threads it onto `Note` / `EventChunk`. Behavior- and data-preserving under the default (persisted `subject_id` stays `null`); H-1's nullable column already round-trips it, so no schema / migration change. Tests: `tests/test_telegram_subject_resolver.py` (new) + `tests/test_domain_service.py` (default-`None` + non-`None` threading guards). Docs: `SUBJECT-SCOPING-ROADMAP.md`, `execution-map.md`, this file.
- **H-3 — optional subject retrieval filter: next.** `Query.subject_scope` + keyword-only optional subject filter on both `storage/search_repository.py` legs (postgres/mock parity), mirroring the D-040 `date_range` seam; `None` = no constraint; composes with `date_range`.
- Deferred: H-4 (regression suite + operator/product docs + closure); core subject registry/entity, subject-selection UX, A-15 visibility (separate).

## Single-chunk /note capture (D-106 → …) — closed
- Milestone scope: close a hidden Telegram diary bug — an explicit `/note` whose payload contains newlines is split into one `EventChunk` per line. Owner-confirmed contract: **one explicit `/note` = exactly one logical chunk**; newlines are content structure, not event separators; per-line splitting is retired for explicit `/note` (supersedes the per-line half of D-005 / the original I-5). Decomposed docs-first (Packet 1) then code (Packet 2). Branch `feat-note-capture-single-chunk`. **Both packets are done; the milestone is closed.** **Out of scope:** any batch / multi-event capture command or separator contract; broader eval corpus redesign / retrieval-quality re-baselining (only the mechanical `#1`/`#2` → `#0` gold-handle collapse was pulled into Packet 2 to keep the gate green); retroactive re-chunking; schema / migrations / core models.
- **Packet 1 — revise I-5 to single-chunk `/note`: done (D-106), pre-checkpoint.** Map: execution-map "Single-chunk `/note` capture (D-106)" (Packet 1 row). Docs-only ratification: new D-106 entry (supersedes the per-line half of D-005 — D-005 not rewritten); `docs/INVARIANTS.md` I-5 retitled "One explicit `/note` is one chunk" and rewritten (I-12 reviewed — no change, replayability is granularity-independent); `CLAUDE.md` preserved-rule #5; `docs/product/TechSpec.md` §6 Chunking Contract; `docs/product/PRD.md` §5 note-capture shape + §6 MVP functional scope; `execution-map.md`; this file. `docs/RUNTIME-INVARIANTS.md` / `docs/RUNBOOK.md` untouched (no per-line contract assertion). No `src/` / `tests/` / schema / migration / config change; no new I-/R- number. See `docs/decision-log.md` D-106.
- **Packet 2 — retire per-line splitting (code): done (enforces D-106), pre-checkpoint.** Map: execution-map "Single-chunk `/note` capture (D-106)" (Packet 2 row). Brings the runtime into compliance with D-106 / revised I-5 — **no new decision-log entry** (enforcement of an existing decision; no invariant change). `core/domain/parser.py` — `ParsedNote.events: list[str]` → `body: str`; `parse_note` stops per-line splitting (first-line date strictness + `normalize_iso_date_token` byte-unchanged). `services/domain_service.py` — one `EventChunk` (`event_index=0`, `chunk_text=parsed.body`) for a non-empty body, none for a date-only note; `note_text=parsed.body` (note_text == chunk_text); `IngestResult.events_count` now the chunk count (0/1), field name retained (rename = out-of-scope model-shape change). **Owner-approved expansions:** `services/dispatcher.py` `_format_ingest_reply` reworded to note vocabulary — pure wording change of the two existing success branches (`"Saved your note for {date}."` / date-only `"… with no content."`); `INVALID_INPUT` byte-identical. `eval/retrieval/gold.json` + `eval/retrieval/observability/gold.json` — `#1`/`#2` expected-handles collapsed to `#0` (no within-query dedup needed) + `_handle_contract` updated, required because `make check` resolves every committed gold handle end-to-end; `corpus.jsonl` unchanged. Tests: `test_note_parser` / `test_domain_service` (single-chunk guard) / `test_query_service` (multi-chunk fixtures → separate notes) / `test_dispatcher_note_normalization` / `test_end_to_end_smoke` / `test_retrieval_harness_shape`. Docs: `docs/RUNBOOK.md`, `QUICKSTART.md`, `execution-map.md`, this file. No schema / migration / config change; canonical I-5 / TechSpec §6 / PRD §5–6 / CLAUDE.md already correct from Packet 1; no new I-/R- number. Full gate green (ruff + mypy + 700 passed / 68 PG-skipped). **Closes the milestone.**

## Grouped diary & multi-diary on one instance (D-093 → D-096) — closed
- Milestone scope: ratify and harden **grouped-diary + multi-diary-on-one-instance** on the existing community-scoped data plane. The mechanics already work (a Telegram group chat is one `community_id` with distinct per-sender `author_user_id` — I-6; distinct chats are isolated communities on one instance — I-7 / R-3 / R-8 / Slice 8.1); what was missing was the ratified contract. Decomposed docs-first in `docs/GROUPED-MULTI-DIARY-ROADMAP.md` (D-093). Stays bounded: **no** core participant/membership/ACL table or registry, **no** `/setup` command (deferred-optional), **no** visibility model (A-15 / Slice 8.2 — deferred and sequenced after the first grouped pilot), **no** subject/child bootstrap (carved out to A-45 / the D-040 lineage), **no** DEPLOY-2, **no** schema/DDL/migration.
- **Packet 1 / G-0 — contract + assumption split + roadmap: done (D-093).** Map: execution-map "Grouped & multi-diary (community bootstrap & membership, D-093)". Docs-only: new D-093 entry, new `docs/GROUPED-MULTI-DIARY-ROADMAP.md` (G-0..G-4 ladder); closes **A-14 → D-093** (community-bootstrap half), opens **A-45** (subject/child bootstrap → D-040 lineage), sequences **A-15** (after the first grouped pilot) in `assumptions.md` + `assumption-audit.md`; execution-map row + note block; this section; cross-reference-only touches to `INVARIANTS.md` (I-6/I-7), `RUNTIME-INVARIANTS.md` (R-3/R-8/R-14), `RUNBOOK.md`, `product/TechSpec.md` §5, `ARCHITECTURE.md` (Axis 5). Ratifies implicit-on-first-message bootstrap, the 1:1 `external_chat_id → community_id` adapter-axis mapping (no longer a "surrogate"), membership inherited from host-chat membership, and multi-diary-on-one-instance as a core capability. No `src/` / `tests/` / schema / migration change. See `docs/decision-log.md` D-093.
- **Packet 2 / G-1 — consolidate the chat→community resolver (code): done (D-094).** New adapter-owned `adapters/telegram/community.py` `resolve_community_id(external_chat_id) -> str` (default 1:1) is the single chat→community seam; the resolved opaque `community_id` is carried on the new required `InboundMessage.community_id` field, set by the webhook edge. The core reads `message.community_id` everywhere it previously derived scope from `external_chat_id` — the full surface was broader than the G-0 audit's "three sites": **6** core/services derivations (`domain_service` `_community_id_for` deleted; `query_service`; `dispatcher` ×4) + **2** `webhook.py` adapter copies (`/ask` footer, `/sources`); the owner ratified full-surface consolidation. `external_chat_id` stays as the transport / R-2 idempotency key only. Behavior-preserving (default 1:1 → resolved value equals prior `external_chat_id`); strengthens I-1 (core scopes on an opaque id, not a "chat id"). Tests: `community_id` added to 8 `InboundMessage` factories; `test_missing_community_id_raises` drives `community_id=""`; new `tests/test_telegram_community_resolver.py`. Docs: `decision-log.md` (D-094), `GROUPED-MULTI-DIARY-ROADMAP.md` (§2 audit + §4 G-1 → landed), this file, `execution-map.md`. No schema/DDL/migration/config change; I-1 (strengthened) / I-6 / I-7 / R-3 / R-8 / R-14 preserved. Full gate green (ruff + mypy + 672 passed / 65 PG-skipped). See `docs/decision-log.md` D-094.
- **Packet 3 / G-2 — grouped + multi-diary regression suite: done (D-095).** Map: execution-map "Grouped & multi-diary (community bootstrap & membership, D-093)" (G-2 row). New consolidated `tests/test_grouped_multi_diary.py` (6 tests, **mock-mode**) pins already-true behavior **through the G-1 `resolve_community_id` seam** (every `InboundMessage` built with `community_id=resolve_community_id(chat)`): one group chat → one `community_id` + distinct per-sender `author_user_id` (I-6; extends the single-author `test_domain_service` coverage); a grouped `/ask` preserves ≥2 distinct contributors through retrieval into `AnswerResult.context.ordered_chunks` and onto the ASK `DispatchResult.grounding_chunks` seam (D-091), driven by full ingest (not pre-built chunks); N distinct chats → N isolated communities (composing with `test_query_service::test_cross_chat_isolation` at grouped granularity); cross-community `/sources`-cache isolation at grouped granularity (composing with `tests/test_read_access_isolation.py` and `tests/test_dispatcher_sources.py::test_two_family_caches_are_independent`). Harness boundary stated in the module docstring: mock-mode only (`MockEmbeddingClient` / `MockChatClient`; sqlite retrieval `NotImplementedError`) — the file adds no PG/sqlite parity for the end-to-end grouped ingest→ask flow; storage-read / per-leg PG parity stays with the existing PG-gated suites, which remain part of full-gate validation. **No `src/` / schema / migration change** (passes against current production code). Docs: `decision-log.md` (D-095), `GROUPED-MULTI-DIARY-ROADMAP.md` (§Purpose status, §2 verdicts, §4 G-2 → landed), `execution-map.md`, this file. I-1 / I-6 / I-7 / R-3 / R-8 preserved. Full gate green (ruff + mypy + 678 passed / 65 PG-skipped). **G-3 (operator/product docs) has since landed (D-096); the milestone is closed.** See `docs/decision-log.md` D-095.
- **Packet 4 / G-3 — operator + product docs: done (D-096). Milestone closed.** Map: execution-map "Grouped & multi-diary (community bootstrap & membership, D-093)" (G-3 row). Docs-only: new `docs/RUNBOOK.md` "Grouped & multi-diary on one instance" how-to (implicit-on-first-message bootstrap; default 1:1 `external_chat_id → community_id` via the `resolve_community_id` edge seam — D-094; host-chat-inherited membership with preserved per-sender `author_user_id` — I-6; running N isolated communities on one instance — I-7 / R-3 / R-8 / Slice 8.1; per-note visibility forward-pointed to A-15 / Slice 8.2 as deferred); reconciled `docs/ARCHITECTURE.md` Axis-5 (named the `resolve_community_id` seam) and `docs/product/TechSpec.md` §5 (marked grouped + multi-diary-on-one-instance **supported**, G-1/G-2); flipped `GROUPED-MULTI-DIARY-ROADMAP.md` (`Status:` line, §Purpose, §4 G-3 row, §6, See also), `execution-map.md` (G-3 row + note block), and this file to milestone-closed — **conditional on this packet landing**. `assumptions.md` / `assumption-audit.md` unchanged (A-14 closed → D-093, A-15 deferred, A-45 open — already correct); no new I-/R- number. No `src/` / `tests/` / schema / migration / config change; the `resolve_community_id` seam and `InboundMessage.community_id` shape are referenced, not modified. I-1 / I-6 / I-7 / R-3 / R-8 / R-14 preserved. See `docs/decision-log.md` D-096.
- **Deferred (not built this milestone):** G-4 visibility model (A-15 / Slice 8.2); A-45 subject/child bootstrap (D-040 lineage); `/setup`; DEPLOY-2.

## Answer-reply contributor attribution (D-091 → D-092) — closed
- Milestone scope: surface **which contributors grounded an `/ask` answer** in the grounded reply, framed as contributor attribution (the set of contributors whose notes grounded the answer), **not** single-author attribution for the whole answer. Reuses the existing adapter-only resolver (D-086) + durable snapshot (D-084) + community scoping (D-089); the core authorship model and the `author_display` seam shape are unchanged. Sequenced separately from the Phase/Stage axis (like the D-081 author-display contract). Stays bounded: **no** A-15 visibility / Slice 8.2 widening, **no** A-14 multi-diary / community-bootstrap reassignment, **no** per-claim `cited_chunk_ids` "citation" attribution (the word "citation" stays reserved per D-036), **no** change to the `author_display` resolver/seam shape or to `/sources` rendering.
- **Packet 1 — contributor-attribution contract + adapter seam decision: done (D-091).** Map: execution-map "Author display-name contract (D-081)" (contract + code rows). Docs-only: pins the contributor set (distinct authors by opaque `author_user_id` of the answer's grounding chunks `AnswerResult.context.ordered_chunks` = `AnswerTrace.context_chunk_ids`), dedup-by-`author_user_id` + first-appearance order, the labeled `Contributors: …` footer render shape, the grounded-only render condition, and the seam (ASK `DispatchResult` carries opaque grounding chunks like `source_chunks`; the Telegram adapter resolves + renders via `resolve_chunk_author_display`, requester-`community_id`-scoped per D-089; I-1 / I-6 / I-7 preserved). `decision-log.md` (D-091), `execution-map.md`, this file, `assumptions.md` + `assumption-audit.md` (A-44 forward cross-ref — **stays resolved/closed, not reopened**; no new assumption number), `RUNBOOK.md` (`/sources` forward note), `INVARIANTS.md` (I-6 cross-ref — no new invariant). No `src/` / `tests/` / schema / migration change; the contract is recorded but **not surfaced** until Packet 2. See `docs/decision-log.md` D-091.
- **Packet 2 — contributor-attribution rendering (code): done (D-092).** Map: execution-map "Author display-name contract (D-081)" (code row). Threads the opaque grounding chunks onto the ASK `DispatchResult` (new `grounding_chunks` field parallel to `source_chunks`, set iff the grounding set is non-empty); the Telegram adapter resolves the distinct contributors (deduped by opaque `author_user_id`, first-appearance order) and appends the `Contributors: …` footer beneath `answer_text` in `adapters/telegram/webhook.py` via the new adapter-only `render_contributors_footer` (reusing `resolve_chunk_author_display`, requester-`community_id`-scoped per D-089). Dispatcher stays channel-neutral (owns only `answer_text` + the opaque chunks). Grounded replies — including `WEAK_EVIDENCE` / `AMBIGUOUS` — get the footer; `NO_EVIDENCE` / empty-query / `PROVIDER_UNAVAILABLE` replies are byte-unchanged. `core/routing/models.py`, `services/dispatcher.py`, `adapters/telegram/{author_display.py,webhook.py}`; `tests/test_author_display_resolution.py`, `tests/test_telegram_ask_contributors.py` (new), `tests/test_dispatcher_retrieval_fallback.py`; `decision-log.md` (D-092), `RUNBOOK.md` (flipped "by contract" → "surfaced"), `execution-map.md`, this file. I-1 / I-6 / I-7 / R-3 preserved; `/sources` rendering unchanged. **Milestone closed.** See `docs/decision-log.md` D-092.

## Slice 8.1 — Community-scoped read-access enforcement (cross-community leakage prevention) — closed (D-087 → D-088 → D-089 → D-090)
- Milestone scope: harden community scoping on the read paths before broader shared/community use, under I-7 / R-3 / R-8 and the Phase-8 DoD lines "cross-community leakage is prevented" / "access behavior is explicit". Stage 3, gate open (OP-1..OP-5 complete). Decomposed docs-first in `docs/READ-ACCESS-ENFORCEMENT-ROADMAP.md` (D-087), which carries the as-built read-path audit. The hot `/ask` path was already community-scoped and tested (R-3 / R-8); the remaining packets closed the latent by-id/trace read seams and the consolidated closure sweep. **All four packets are done; the milestone is closed.** **Out of scope:** visibility model (Slice 8.2, A-15); export/delete/audit/retention (Slice 8.3); community-bootstrap reassignment (A-14); any schema/DDL/migration or rename work.
- **Packet 8.1.0 — audit + decomposition: done (D-087).** Map: execution-map "Read-access enforcement (Slice 8.1)". Docs-only: new D-087 entry, new `docs/READ-ACCESS-ENFORCEMENT-ROADMAP.md`, execution-map row + note block, this section. Records the I-7 / R-3 / R-8 framing, the as-built audit (enforced hot path vs. latent unscoped `get_query` / `get_retrieval_hits_for_query` / `get_answer_trace_for_query` / `get_event_chunk` / `get_source_message` reads), the defensive-scoping decision, the "no `answer_traces` column — recover via `queries` join" decision, and the ordered 8.1.1/8.1.2/8.1.3 ladder. No `src/` / `tests/` / schema change. See `docs/decision-log.md` D-087.
- **Packet 8.1.1 — defensive scoping of unused by-id/trace reads: done (D-088).** Map: same. Added a mandatory **keyword-only** `community_id` + null-guard + owning-community filter to `get_query`, `get_retrieval_hits_for_query`, `get_answer_trace_for_query`, `get_event_chunk` across the `DomainRepository` Protocol + mock / sqlite / postgres (the two trace reads scope via the `query_id → queries.community_id` join; `get_query` / `get_event_chunk` filter their own column; parent-missing → `[]` / `None` fail-closed, pinned by one shared parametrized assertion per trace method across mock / sqlite / postgres). All 58 test-only call sites pass `community_id=` explicitly; new guard + cross-community isolation tests in `tests/test_storage_query_traces.py` + `tests/test_storage_answer_traces.py`. Keyword-only prevents a silent `str`/`str` positional swap. No live `/ask` behavior change, no schema change; `get_source_message` / `/sources` byte-unchanged (8.1.2). `make check` green. See `docs/decision-log.md` D-088.
- **Packet 8.1.2 — `get_source_message` scoping + `/sources` isolation: done (D-089).** Map: same. Scoped `get_source_message` (mandatory **keyword-only** `community_id` + R-3 null-guard + own-column filter on `source_messages.community_id`) across the `DomainRepository` Protocol + mock / sqlite / postgres. The live `/sources` author-resolution path threads the **requester-scoped** `community_id` (resolved at the webhook edge from the inbound chat via the A-14 identity mapping) through `render_source_block` → `resolve_chunk_author_display` → `get_source_message`, keeping storage/helper seams on `community_id` vocabulary (no Telegram identifier leaks past the edge). A cross-community source reads as `None` and falls through to the existing opaque author floor — no raise, rendered-block format byte-unchanged. `_latest_sources` / dispatcher unchanged (relied upon as already-community-keyed, D-036; covered by `test_two_family_caches_are_independent`). New `tests/test_storage_source_messages.py` (parametrized guard + isolation across mock / sqlite / PG-gated postgres) + a seam-focused `test_bridge_floor_when_community_mismatch`; storage call sites pass `community_id=`. No schema change. `make check` green (635 passed). See `docs/decision-log.md` D-089.
- **Packet 8.1.3 — milestone closure / verification: done (D-090). Slice 8.1 complete.** Map: same. New `tests/test_read_access_isolation.py` is the single consolidated cross-community isolation sweep — a parametrized `scoped_store` fixture (mock / sqlite / PG-gated postgres) pinning both the null-`community_id` guard (R-3) and cross-community fail-closed sentinels (`None` / `[]`) for all five scoped reads (`get_query`, `get_retrieval_hits_for_query`, `get_answer_trace_for_query`, `get_event_chunk`, `get_source_message`); its docstring indexes the in-place hot-path / prompt-assembly / `/sources`-seam / `_latest_sources` evidence. This also closes the one truthfulness gap surfaced in planning: `get_event_chunk` (scoped by D-088) had no isolation/guard test of its own — now pinned, green, no `src/` change needed. New `docs/RUNBOOK.md` "Read-access scoping (Slice 8.1 / D-087, D-088, D-089, D-090)" operator note records the enforcement contract (fail-closed, own-column-vs-join, keyword-only `community_id`, no-`answer_traces`-column, `MEMORY_RAG_PG_TEST_DSN` caveat). Roadmap §0/§4/§6 + execution-map Slice 8.1 / 8.1.3 rows flipped to complete. Phase-8 DoD lines "cross-community leakage is prevented" / "access behavior is explicit" satisfied for the read surface. No schema change; I-7 / R-3 / R-8 unaffected; A-14 / A-15 stay open. See `docs/decision-log.md` D-090.

## Stage-1 capture/routing baseline correction — closed (D-078 → D-079 → D-080 → D-085)
- Milestone scope: closed a contract inconsistency in the Stage-1 capture/routing baseline — D-028 enforced the draft floor but kept the high-confidence plain-text NOTE/ASK auto-routes on top, which could silently upgrade a casual dated line or a question-shaped message into a fully-indexed NOTE / executed ASK the user never explicitly requested. The correction was docs-first, then code, then the `/note`-default companion. Stayed bounded to the routing contract: no author-name display, group-use, or multi-diary widening. D-072 stays parked on the sibling `rescue/d072-doc-closure-and-routing-contract` branch and is not reused. **All four packets are done; the milestone is closed.**
- **Packet 1 — docs/routing-contract correction: done (D-078).** Map: execution-map "Stage-1 capture/routing baseline correction". Docs-only entry recording that command-less plain text routes only to the draft floor and that heuristic plain-text NOTE/ASK auto-routing is retired (supersedes only the heuristic-routing portions of D-020 and D-028; A-28 stays open; CLARIFY unchanged). Aligns `assumptions.md` / `assumption-audit.md` (A-16/A-17 closed + annotated, fixing a standing open-row inconsistency; A-28 annotated), `INVARIANTS.md` (I-14), `RUNTIME-INVARIANTS.md` (R-11/R-13), `ARCHITECTURE.md`, `product/PRD.md` + `product/TechSpec.md` (narrow), `QUICKSTART.md` (forward note only — examples unchanged), `RUNBOOK.md`, and `execution-map.md`. No `src/` / `tests/` change; the live classifier still auto-routes until Packet 2. See `docs/decision-log.md` D-078.
- **Packet 2 — classifier code change: done (D-079).** Collapsed the NOTE/ASK branches in `src/memory_rag/core/routing/classifier.py` into the draft floor; updated `tests/test_routing_classifier.py` + dispatcher / end-to-end routing tests (removed the now-impossible heuristic-marker / heuristic-normalize tests); removed the dead dispatcher `is_heuristic` markers + normalize-gate; refreshed the QUICKSTART examples + RUNBOOK notes to past tense and the I-14 / R-11 / R-13 trailing "later packet" clauses to D-079 enforcement. Map: same. Brings the runtime into compliance with D-078. See `docs/decision-log.md` D-079.
- **Packet 2.1 — residual doc-clause reconciliation: done (D-080).** Docs-only follow-up to D-078 (contract) and D-079 (code). Flipped the six residual "classifier code change deferred / lands in a later packet" clauses (`assumptions.md` A-16/A-17 pointer, `assumption-audit.md` A-16 row, `product/PRD.md` §5 + §6, `ARCHITECTURE.md` routing-contract line, `product/TechSpec.md` §"Convenience routing") to the past-tense D-079 enforcement wording already used in I-14 / R-11 / R-13. Classifier enforcement is now both recorded **and** enforced consistently across all canonical docs. No `src/` / `tests/` / schema / migration change; A-28 stays open. Map: same. See `docs/decision-log.md` D-080.
- **Packet 3 — `/note`-without-explicit-date → "today": done (D-085).** The explicit `/note` dispatch path defaults a missing/unrecognized first-line date to "today" (`InboundMessage.received_at.date()`, UTC) by prepending a canonical `YYYY-MM-DD` line in `_normalize_note_first_line` (`src/memory_rag/services/dispatcher.py`); a dateless `/note` now saves under today instead of returning `INVALID_INPUT`, which survives only for an empty/whitespace-only `/note`. Reuses the existing `received_at` field — no clock seam / constructor change. `core/domain/parser.parse_note` strictness unchanged (default lives in the dispatcher seam; A-28 stays open as the A-12 precursor). Tests: `tests/test_dispatcher_note_normalization.py` + `tests/test_end_to_end_smoke.py` + `tests/test_note_parser.py` (incl. sibling-wording guards on the INVALID_INPUT / Saved / draft literals). Docs: `RUNBOOK.md`, `QUICKSTART.md`, `assumptions.md` + `assumption-audit.md` (A-28), `execution-map.md`, this file. Map: same. **Closes the milestone.** See `docs/decision-log.md` D-085.

## Slice 3.7 — Language-aware sparse FTS (dual-config tsvector union, D-039)
- Owner: agent
- Map: execution-map 3.7
- Mechanism (decided — D-039 selected the first quality lever): retire the sparse-leg `simple` dictionary for a **dual-config tsvector union**. `event_chunks.chunk_text_tsv` generated as `to_tsvector('russian', chunk_text) || to_tsvector('english', chunk_text)`; `sparse_candidates` queries with `websearch_to_tsquery('russian', $q) || websearch_to_tsquery('english', $q)`, ranked by `ts_rank_cd`. No language detection on either side — every chunk and every query is processed under both built-in configs.
- Sequencing (baseline-vs-quality discipline): **(1)** the operator runs the D-038 harness in Postgres mode and pastes the baseline snapshot into the D-038 entry; **(2)** *only then* this implementation packet lands the dual-config union and is re-measured against that snapshot. The implementation must not land before step (1).
- Concrete: change the generated-column expression in `storage/postgres/schema.sql`; change the `tsquery` construction in `storage/postgres/store.py` `sparse_candidates`; preserve mock + sqlite parity (sqlite still `NotImplementedError`); A-34 destructive local upgrade (`docker compose down -v`); update TechSpec §9 + RUNBOOK; re-run the D-038 harness Postgres mode.
- Deferred follow-up levers (one per future packet, **do not bundle more than one**): **BM25-grade sparse** (`pg_search`, `bm25_catalog`, or app-side BM25 over tokenized chunks); **reranker / cross-encoder**; **Qdrant or another dedicated vector / search system**; **3072-dim ANN strategy** (A-36b: halfvec + HNSW vs alternatives). Each measures against the D-038 baseline when picked.

## Slice 3.4 (cont.) — Metadata filtering: child + visibility dimensions (after D-040)
- Owner: agent
- Map: execution-map 3.4
- Context: D-040 shipped the date dimension and established the keyword-only optional-filter seam on both `SearchRepository` legs (`date_range: DateRange | None = None`). The remaining two metadata-filter dimensions are additive packets on that same seam.
- Concrete: add `child_id` filtering and `visibility_scope` filtering onto the seam, **one per packet — do not bundle**. Child filtering needs a `Query.child_scope` expression story first. Visibility filtering is blocked on **A-15** (visibility-scope enumerated values are undefined) and cannot land until A-15 is resolved.
- Follow-up: inbound `/ask` date syntax / natural-language date parsing (a Telegram-side packet that parses a message into a `DateRange` and passes it to `QueryService.answer(..., date_range=)`).

## DEPLOY-1 — Self-hosted VPS reference deployment (D-060) — in progress
- Work-group **DEPLOY-1** (D-060; see `docs/SELF-HOSTED-DEPLOYMENT-ROADMAP.md`). Sequenced separately from the Phase/Stage axis: DEPLOY-1 is the first implemented reference deployment shape (self-hosted VPS + Telegram, single-community pilot); DEPLOY-2 (managed-cloud reference deployment) is the deferred second peer. D-026 / D-027 peer parity preserved.
- Every DEPLOY-1.x packet must cite "operates within DEPLOY-1 invariants — A-22 updated by D-060" in its own implementation plan, and either honors the invariants in `docs/SELF-HOSTED-DEPLOYMENT-ROADMAP.md` §2 unchanged or revises a default in §3 with a small follow-up decision-log note.
- **DEPLOY-1.1 — decision + roadmap: done (D-060).** Locks the DEPLOY-1 invariants (Debian / Ubuntu LTS; single-community / single-tenant default; public DNS + HTTPS required; off-box backup destination required, S3-compatible or equivalent; operator-facing idempotent install/upgrade script) and names the current defaults (reverse proxy / TLS terminator; backup tool; installer implementation). Closes A-22; A-41 stays open and deferred until DEPLOY-2; opens A-42 (closed by D-060) and A-43 (open). Map: execution-map "Deployment-shape rollout". Docs-only.
- **DEPLOY-1.2 — VPS runtime shape: done (D-061).** `Dockerfile` + `.dockerignore` + docker-compose `vps` profile (`app_init` one-shot for OP-1 migrations + `app` running uvicorn behind FastAPI, both gated by `profiles: ["vps"]`) bringing the app and OP-1 / OP-4-shaped Postgres up on a clean Debian / Ubuntu LTS VPS via the single canonical path `docker compose --profile vps up -d --build`. App port is loopback-only (`127.0.0.1:8000`) until DEPLOY-1.3. OP-1 migrations + OP-4 archive volume shape reused unchanged. Map: execution-map "Deployment-shape rollout". See `docs/RUNBOOK.md` "VPS runtime shape (DEPLOY-1.2 / D-061)".
- **DEPLOY-1.3 — reverse-proxy + TLS contour: done (D-062).** `configs/caddy/Caddyfile` + new `caddy` service in `docker-compose.yml` gated by `profiles: ["vps"]` + two new `.env.example` knobs (`PUBLIC_HOSTNAME`, `ACME_EMAIL`) bringing the public DNS + HTTPS contour up in front of the DEPLOY-1.2 `app` service via the unchanged single canonical path `docker compose --profile vps up -d --build`. Pins **Caddy** as the §3 reverse-proxy / TLS terminator default from the candidate set Caddy / nginx / other ACME-capable proxy. The DEPLOY-1.2 loopback `127.0.0.1:8000:8000` publish on `app` is retained as operator-only bypass-the-proxy inspection, **not** a closure signal — decisive evidence is `https://$PUBLIC_HOSTNAME/health` + HTTP → HTTPS redirect, documented in `docs/RUNBOOK.md` "Reverse-proxy + TLS contour (DEPLOY-1.3 / D-062)". Map: execution-map "Deployment-shape rollout".
- **DEPLOY-1.4 — installer / upgrade script: done (D-063).** `scripts/installer/deploy.sh` — operator-facing, idempotent, non-interactive bash installer that wraps the canonical `docker compose --profile vps up -d --build` bring-up with preflight, a state-machine driven by `.installer-state.json` + `INSTALLER_CONFIG_VERSION` (the D-060 configuration-versioning seam, realized here), and an honest status outcome that distinguishes the mandatory loopback `/health` probe from the best-effort public-TLS probe. Pins **bash, non-interactive** as the §3 installer-implementation default from the candidate set bash / Python CLI; interactive / non-interactive. Also adds `.installer-state.json` and `.installer-state.last_failure.json` `.gitignore` entries. Map: execution-map "Deployment-shape rollout". See `docs/RUNBOOK.md` "Installer / upgrade script (DEPLOY-1.4 / D-063)".
- **DEPLOY-1.5 — Telegram webhook registration automation: done (D-064).** `scripts/installer/deploy.sh` extended with `register_telegram_webhook` (best-effort, mirrors `public_tls_probe` semantics: `registered (<url>)` / `skipped (public_tls_probe=…)` / `failed (…)`), the new `--unregister-webhook` teardown subcommand, a `webhook_registration` block in `.installer-state.json`, the `INSTALLER_CONFIG_VERSION=1 → 2` bump with the first non-trivial migration helper (`migrate_v1_to_v2`, no-op stamp), and the two new preflight env keys (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET`). Map: execution-map "Deployment-shape rollout". See `docs/RUNBOOK.md` "Telegram webhook registration (DEPLOY-1.5 / D-064)".
- **DEPLOY-1.6 — off-box backup sink wiring: done (D-065).** A new `pg_offbox_uploader` sidecar service in `docker-compose.yml` (image `rclone/rclone:1.66`, gated by `profiles: ["backup"]`) running `scripts/pg_offbox_uploader/uploader.sh` mirrors `/archive/base` + `/archive/wal` to the operator-supplied S3-compatible target via `rclone sync` whenever a new OP-4.2 cycle has succeeded; `scripts/installer/deploy.sh` extended with `probe_offbox_backup` (active probe via `docker run rclone/rclone:1.66 lsd`) and the new `offbox_backup_probe` field in `.installer-state.json`; `INSTALLER_CONFIG_VERSION` bumped `2 → 3` with `migrate_v2_to_v3` (second non-trivial use of the D-063 seam); `selected_defaults.backup_tool` flipped from `null` to `"rclone"` in both `write_state_success` and `write_state_unregistered`; five new optional `BACKUP_S3_*` knobs in `.env.example`. Pins **rclone** as the §3 backup-tool default from the candidate set restic / rclone / `pg_dump` / `pg_basebackup`. A-43 (logs-first observability scope) deferred to a later DEPLOY-1.x packet — the off-box sink reuses the existing `pg_backup.*` log-prefix family. Map: execution-map "Deployment-shape rollout". See `docs/RUNBOOK.md` "Off-box backup sink (DEPLOY-1.6 / D-065)".
- **DEPLOY-1.7-preflight — local-only upgrade-drill harness: done (D-066).** New `scripts/installer/drill_upgrade_local.sh` + `docs/deploy1-drill/deploy1-upgrade-drill-<YYYYMMDD>-evidence.json` exercise the D-063 configuration-versioning seam against real prior commits (`7cb96fa` (DEPLOY-1.4, `installer_config_version=1`) / `e435e1a` (DEPLOY-1.5, v2) / `0aef179` (DEPLOY-1.6, v3)) via a sandboxed git worktree under `mktemp -d`; pinned `COMPOSE_PROJECT_NAME=deploy1-preflight-drill` keeps named volumes alive across legs while isolated from operator compose state. Locally confirmed: v0 → v1 → v2 → v3 chain advance, state-file shape transitions (the `webhook_registration` block appearing at leg 2 and the `offbox_backup_probe` field + `selected_defaults.backup_tool="rclone"` flip appearing at leg 3, both captured verbatim in `state_file_after`), migration helper ordering. Locally captured but classified `operator_dependent` (DEPLOY-1.7's responsibility): `public_tls_probe` / `webhook_registration` / `offbox_backup_probe` recorded verbatim under `observed_probes` per leg — not pre-asserted. Map: execution-map "Deployment-shape rollout". See `docs/RUNBOOK.md` "Local-only upgrade-drill preflight (DEPLOY-1.7-preflight / D-066)".
- **DEPLOY-1.7a — clean-VPS pilot smoke + off-box backup §2-invariant verification: done (D-067).** Committed evidence artifact `docs/deploy1-drill/deploy1-pilot-smoke-20260527-evidence.json` captures verbatim: installer `.installer-state.json` fields (`installer_config_version`, `selected_defaults`, `last_outcome`, `loopback_health`, `public_tls_probe`, `offbox_backup_probe`, `webhook_registration`); the observed `https://$PUBLIC_HOSTNAME/health` body; the `https://$PUBLIC_HOSTNAME/telegram/webhook` round-trip with the canonical `telegram.webhook update_id=<id> route=start ...` in-app log line + matching `POST /telegram/webhook 200` access log + observed `/start` latency (~2–3 s); an explicit `getUpdates=409` note framed as expected-with-webhook-active (not a defect); and the off-box happy-path + additivity-smoke leg against a real S3-compatible target (`pg_backup.offbox.begin → pg_backup.offbox.ok`, `/archive/last_offbox.json status=ok`, `<prefix>/base/...` + `<prefix>/wal/...` listing summary, then a forced upload failure → `status=error` with `/archive/last_success.json` unchanged and `pg_backup.cycle.ok` unaffected). Credential text redacted from the evidence artifact (no `PUBLIC_HOSTNAME` / `BACKUP_S3_*` / bot-token values appear literally). Three Phase-4 UX polish observations surfaced during smoke (`/note` first-line ISO-date strictness; `/ask` user-facing wording leaking chunk UUID + the phrase "dense+sparse RRF"; no-memories-matched UX) are flagged in D-067 as observations, deferred to a separate post-DEPLOY-1 packet. Map: execution-map "Deployment-shape rollout". See `docs/RUNBOOK.md` "Clean-VPS pilot smoke (DEPLOY-1.7a / D-067)".
- **DEPLOY-1.7b — v2 → v3 cross-version upgrade drill on a real previously-installed v2 VPS: re-scoped to DEPLOY-2 prep (D-076).** Operator-procedure prep landed in DEPLOY-1 scope by D-068 (committed template at `docs/deploy1-drill/deploy1-cross-version-drill-TEMPLATE.json` and the "v2 → v3 cross-version upgrade drill (DEPLOY-1.7b / D-068)" subsection in `docs/RUNBOOK.md`); both are **retained verbatim** and re-scoped as DEPLOY-2 prep assets — v2 → v3 cross-version migration is a natural follow-up alongside the managed-cloud peer shape. The drill itself remains outstanding against a previously-installed v2 VPS that does not yet exist; it is **no longer a DEPLOY-1 closure dependency**. Map: execution-map "Deployment-shape rollout".
- **DEPLOY-1 closure prep (post-REAL-1): docs-only prep landed (D-076).** New committed evidence-file template at `docs/deploy1-drill/deploy1-closure-post-real1-TEMPLATE.json` carrying `"_template": true` and six top-level branches (`metadata` / `installer_state` / `live_probes` / `post_real1_round_trip` / `summary` / `out_of_scope_for_this_packet`) with the dual `<REDACTED>` / `<TO_FILL_BY_OPERATOR>` placeholder convention. New "DEPLOY-1 closure procedure (post-REAL-1) (D-076)" subsection in `docs/RUNBOOK.md` naming the operator pre-conditions (already-deployed v3 contour from DEPLOY-1.7a; canonical REAL-1 env knobs), the numbered procedure against the deployed v3 contour (snapshot `.installer-state.json` → capture the A-43-pinned `pg_backup.*` / Caddy access / `telegram.webhook` / `retrieval.hybrid` / `answer.*` lines verbatim → one `/note` → `/ask` round-trip mirroring REAL-1.1's envelope → assemble the dated artifact → run the redaction grep checklist), the evidence-file shape, the redaction rule, the closure signal, and the `make check` non-impact note. DEPLOY-1 closes when the operator runs the procedure and commits the populated dated evidence artifact at `docs/deploy1-drill/deploy1-closure-post-real1-<YYYYMMDD>-evidence.json` with all four summary booleans green. Map: execution-map "Deployment-shape rollout". See `docs/RUNBOOK.md` "DEPLOY-1 closure procedure (post-REAL-1) (D-076)".
- **A-43 — logs-first observability scope pin: done (D-077, closes A-43).** Pinned to the existing `pg_backup.*` log-prefix family already in `scripts/pg_backup/scheduler.sh` + `scripts/installer/deploy.sh` + `scripts/pg_offbox_uploader/uploader.sh`; Caddy access logs at the reverse-proxy contour established by DEPLOY-1.3 / D-062; the app-side `telegram.webhook` / `retrieval.hybrid` / `answer.*` log families already emitted by `src/memory_rag/adapters/telegram/webhook.py` / `services/retrieval.py` / `services/query_service.py` / `services/dispatcher.py`. Captured verbatim, not invented. **No new logging contract in `src/`.** Forward seam to remote sinks deliberately unpinned and deferred (to a later observability packet or DEPLOY-2 prep). Closes A-43. Map: execution-map "Deployment-shape rollout".
- **DEPLOY-2 — managed-cloud reference deployment: deferred.** No near-term operator. Resolves A-41 when it lands; gets its own roadmap doc when pulled. Inherits DEPLOY-1.7b's committed cross-version-drill template + RUNBOOK subsection (retained verbatim by D-076) as DEPLOY-2 prep.

## Post-DEPLOY-1 Phase-4 UX polish (D-067 §Observations)
- Milestone scope: close the three Phase-4 user-facing surface gaps surfaced by the DEPLOY-1.7a clean-VPS pilot smoke (D-067 §Observations). Each packet stays within "Phase-4 surface polish without retrieval / answer-path / schema / migration change."
- **Packet 1 — `/ask` user-facing reply de-leaks chunk UUID + `dense+sparse RRF`: done (D-069).** Map: execution-map "Post-DEPLOY-1 Phase-4 UX polish". `src/memory_rag/services/dispatcher.py` drops the `(hybrid retrieval — dense+sparse RRF)` success-case trailer (success replies are now `result.answer_text` alone; weak-evidence / ambiguous / no-evidence / provider-unavailable / parse-failure replies unchanged) and renders `/sources` blocks as `[YYYY-MM-DD] (i/N)\n\n<chunk_text>` instead of `[YYYY-MM-DD] <chunk_id>\n\n<chunk_text>`. The `(i/N)` index is per-last-`/ask` ephemeral ordering, not a stable cross-`/ask` identifier; the underlying `chunk_id` stays available via `AnswerTrace.context_chunk_ids` for operator forensics. `tests/test_dispatcher_sources.py` (six `source_blocks` literal updates) and `tests/test_end_to_end_smoke.py` (two RRF / hybrid-jargon negative assertions plus an explicit `"\n\n" not in text` guardrail proving no remnant blank trailer survives) updated. `docs/RUNBOOK.md` §"Selected-chunks recall (`/sources`, D-036)" + §"Hybrid retrieval (D-025)" updated. R-6 / I-9 unaffected. See `docs/decision-log.md` D-069.
- **Packet 2 — `/note` first-line date — six-form near-ISO whitelist + de-leaked error wording + proactive `/start` blurb: done (D-070).** Map: execution-map "Post-DEPLOY-1 Phase-4 UX polish". The explicit `/note` dispatcher path now normalizes a six-form near-ISO whitelist (`YYYY-MM-DD`, `YYYY/MM/DD`, `YYYY.MM.DD`, `DD-MM-YYYY`, `DD/MM/YYYY`, `DD.MM.YYYY` — zero-padded only) to canonical `YYYY-MM-DD` before the strict parser runs. DD-first inputs are always interpreted as DD/MM/YYYY by intentional product convention (concrete pin: `05/09/2026` → `2026-09-05`). The user-facing error wording drops the dev-leaking `"Mock"` prefix (now: `"First line must be a date like 2026-05-09. Got: '<got>'."`) and `_REPLY_START` is extended to proactively name the accepted formats and the DD/MM/YYYY convention. New pure helper `normalize_iso_date_token` in `src/memory_rag/core/domain/parser.py` (the in-parser strictness — `parse_note`, `_parse_iso_date`, `_split_non_empty_lines` — is byte-for-byte preserved); new dispatcher-local `_normalize_note_first_line` helper wired into the `RouteKind.NOTE` branch under `if not is_heuristic:` so the legacy heuristic auto-route stays byte-identical. New test file `tests/test_dispatcher_note_normalization.py` (eleven tests for helper behavior + dispatcher seam + `/start` blurb pins); `tests/test_note_parser.py` extended with eight parametrized blocks including the DD/MM/YYYY convention pin; `tests/test_end_to_end_smoke.py` extended with four new positive smoke cases (including the convention pin at the smoke layer) and the legacy-classifier regression guardrail `test_heuristic_classifier_not_broadened_by_packet_2`. `docs/RUNBOOK.md` gained a new "`/note` first-line date format (D-070)" subsection inside §"Command surface (D-028, D-030, D-031, D-036)"; `QUICKSTART.md` curl-example error literal updated. The legacy plain-text NOTE auto-route in `core/routing/classifier.py` is **not** modified and its current strictness is **not** endorsed as a long-term contract — that heuristic is misaligned with the drafts-vs-notes product contract and is slated for separate cleanup. A-28 deliberately not edited (in-parser strictness preserved, so A-28's named claim stays accurate; holistic rewrite deferred to the legacy-classifier cleanup milestone). I-5 / I-15 / R-13 / D-026 unaffected. See `docs/decision-log.md` D-070.
- **Packet 3 — `/ask` empty-evidence NO_EVIDENCE reply gets plain-language wording + two short question-side nudges: done (D-071).** Map: execution-map "Post-DEPLOY-1 Phase-4 UX polish". `src/memory_rag/services/dispatcher.py` adds a new module-level template constant `_REPLY_NO_MATCHES_TEMPLATE = "Nothing in your saved notes matched '{query}'. Try rephrasing the question, or use words that appear in your notes."` and the empty-evidence branch of `_format_answer_reply` (`FallbackMode.NO_EVIDENCE` with non-empty `query_text` and empty `evidence`) now returns the formatted template — replacing the prior bare `"No memories matched '{query_text}'."`. Wording is plain-language, uses the canonical "notes" noun (per `docs/GLOSSARY.md`), stays scoped to "nothing matched this question among your saved notes" (does **not** imply the user has no data at all), is neutral about cause, and carries two short question-side nudges from the allowed palette (rephrase; use words from saved notes) — no `/help`, no `/note` capture-nudge, no broader coaching. `tests/test_dispatcher_retrieval_fallback.py` updates the empty-evidence asserts in `test_not_implemented_error_translates_to_no_evidence` and `test_llm_marker_no_evidence_distinct_from_empty_retrieval` (the byte-distinctness assertion in the latter continues to hold), and adds a new explicit anti-bleed guard `test_sibling_fallback_wording_unchanged` pinning the `PROVIDER_UNAVAILABLE` and `PARSE_FAILURE` reply literals so any accidental cross-contour wording change is caught in this packet's own test diff. `tests/test_end_to_end_smoke.py` updates the two smoke-level empty-evidence asserts. `docs/RUNBOOK.md` §"Hybrid retrieval (D-025)" empty-merged-set sentence and `QUICKSTART.md` two `# → text: …` example-output comments updated to the new wording. The vocabulary migration is contour-scoped — product-prose mentions of "memory/journal core" / "Diary Memory Service" stay as-is; sibling fallback wording (empty-query `"No query text provided."`, LLM-marker NO_EVIDENCE, `WEAK_EVIDENCE`, `AMBIGUOUS`, `PROVIDER_UNAVAILABLE`, `PARSE_FAILURE`, `NONE`) is byte-identical; `/start` (D-070's surface) is not reopened. R-6 / I-9 / I-1 unaffected. **Milestone closed:** the three D-067 §Observations are now done (D-069 / D-070 / D-071). See `docs/decision-log.md` D-071.

## OP-1 — Schema evolution before non-local deployment (Stage 2 — operationalization) — complete
- Stage-2 packet group **OP-1** (D-044; see `docs/OPERATIONALIZATION-ROADMAP.md`). OP-1 leads Stage 2, because OP-2/OP-3/OP-4 all depend on non-destructive migrations.
- **OP-1.1 — migration tooling foundation: done (D-045).** `yoyo-migrations` is wired; `src/memory_rag/storage/postgres/migrations/0001.baseline-schema.sql` captures the schema; `schema.sql` is retired; `PostgresDomainStore` applies migrations to head on bootstrap; a pre-existing local volume is adopted with a one-time `stamp` (no destructive reset). See `docs/RUNBOOK.md` "Schema migrations (OP-1 / D-045, D-046)".
- **OP-1.2 — non-destructive schema-changing upgrade: done (D-046).** `src/memory_rag/storage/postgres/migrations/0002.index-embedding-status.sql` adds the first additive schema-changing upgrade (an index on `event_chunks(embedding_status)`); gated tests prove the 0001→0002 upgrade is non-destructive over populated data, including a stamp-adopted volume.
- **OP-1 is complete and A-34 is resolved.** Both halves of the exit criterion — fresh-environment bootstrap and a non-destructive upgrade from a prior schema version — succeed without a destructive volume reset.

## OP-2 — Provider hardening (Stage 2 — operationalization)
- Stage-2 packet group **OP-2** (D-044; see `docs/OPERATIONALIZATION-ROADMAP.md`). Runs after OP-1; ordered 6.1 → 6.2 → 6.3.
- **OP-2.1 / Slice 6.1 — timeouts, bounded retries, error classification: done (D-047).** `src/memory_rag/adapters/resilience.py` provides the shared bounded-retry/timeout primitive; both OpenAI adapters build the SDK client with an explicit `timeout` + `max_retries=0` and run every call through it; `config.py` adds `provider_timeout_seconds` / `provider_max_attempts`. R-9 is enforced for provider calls. See `docs/RUNBOOK.md` "Provider resilience (D-047, D-049)".
- **OP-2.2 / Slice 6.2 — dead-letter surface: done (D-048).** `core/domain/models.py` adds the channel-neutral `IndexingDeadLetter` entity; migration `0003.indexing-dead-letter-table.sql` adds the additive `indexing_dead_letters` table (SQLite + mock parity); `DomainRepository` gains the `save` / `list` / `get` dead-letter seams; on an embedding failure `DomainService` attempts one best-effort dead-letter write after the `embedding_status='failed'` marking. Failed indexing jobs survive and are inspectable. See `docs/RUNBOOK.md` "Dead-letter surface (OP-2.2 / D-048)".
- **OP-2.3 / Slice 6.3 — rate-limit backoff: done (D-049).** `adapters/resilience.py` adds `compute_backoff` (exponential delay with full jitter, clamped to the cap) and `extract_retry_after_seconds`; `run_with_retries` waits between retryable attempts through an injected `sleep` seam, honoring a clamped server `Retry-After` when present, and logs `delay_ms` / `delay_source` on the retryable `provider.attempt` line that is followed by a wait; `config.py` adds `provider_backoff_base_seconds` / `provider_backoff_cap_seconds`. Worst-case bounded wall time per provider call is now `timeout × attempts + backoff_cap × (attempts − 1)`. See `docs/RUNBOOK.md` "Provider resilience (D-047, D-049)".
- **OP-2 is complete.** Slices 6.1 / 6.2 / 6.3 have all landed; the Phase 6 Definition of Done is met.

## OP-3 — Reconciliation for failed embeddings (Stage 2 — operationalization, A-35) — complete
- Stage-2 packet group **OP-3** (D-044; see `docs/OPERATIONALIZATION-ROADMAP.md`). Runs after OP-1 (schema-migration tooling) and OP-2 (provider hardening), because the reconciliation job consumes OP-2's bounded-backoff retry and dead-letter primitives.
- **OP-3 is complete and A-35 is resolved.** Failed embeddings are discoverable, retryable, and exhausted retries are routed to the `indexing_dead_letters` surface.
- **OP-3.1 — failed-embedding discovery + read-only entrypoint: done (D-050).** `DomainRepository.list_failed_event_chunks` (community-scoped, oldest-first, parity across mock / sqlite / postgres); `services/reconciliation.py` with `ReconciliationService.discover_failed_chunks` and the read-only `python -m memory_rag.services.reconciliation` CLI, replacing the raw `psql` probe as the failed-chunk inspection surface. Discovery only — no retry. See `docs/RUNBOOK.md` "Failed embeddings".
- **OP-3.2a — failed-embedding retry execution: done (D-051).** `ReconciliationService` gains an optional injected `EmbeddingClient` and `retry_failed_chunks`, which consumes `list_failed_event_chunks`, groups failed chunks by `source_message_id`, retries each group via `EmbeddingClient.embed` (OP-2 bounded backoff stays internal to the client), persists `EmbeddingRecord` rows and transitions succeeded chunks `failed → ready`, and leaves exhausted-retry chunks `failed`; `RetryOutcomeReport` + `render_retry_report` and `reconciliation.retry.*` logs make the outcome observable; the `python -m memory_rag.services.reconciliation` CLI gains a `--retry` mode. See `docs/RUNBOOK.md` "Failed embeddings".
- **OP-3.2b — failed-embedding retry dead-letter routing: done (D-052).** `retry_failed_chunks` routes an exhausted retry group to the OP-2.2 `indexing_dead_letters` surface with a best-effort, append-only `save_indexing_dead_letter` write; `RetryGroupOutcome.dead_letter_id`, the `reconciliation.retry.group.failed` log, and `render_retry_report` surface the dead-letter identity. **Resolves A-35.** See `docs/RUNBOOK.md` "Failed embeddings".

## OP-4 — Raw-data durability & backup (Stage 2 — operationalization, A-40) — complete
- Stage-2 packet group **OP-4** (D-044; see `docs/OPERATIONALIZATION-ROADMAP.md`). Depends only on OP-1 (recovery restores into a schema-versioned database) and may run in parallel with OP-2/OP-3. OP-4.1 decomposed OP-4 into OP-4.1/4.2/4.3.
- **OP-4.1 — backup mechanism & recovery contour decision: done (D-053).** Resolves A-40 (docs-only). For the reference/local Postgres shape the recovery primitive is a nightly physical base backup (`pg_basebackup`) + continuous WAL archiving (`archive_command`) → point-in-time recovery; managed-cloud and self-hosted shapes use the provider- or operator-owned equivalent (no vendor named — A-41 stays open). RPO ≤ 5 min / RTO ≤ 1 h for raw `SourceMessage` data; base backups retained 30 days; restore drill once before the first non-local deployment, then quarterly thereafter. See `docs/RUNBOOK.md` "Raw-data durability and recovery".
- **OP-4.2 — backup automation: done (D-054).** Implements the D-053 contour for the reference Postgres shape: always-on WAL archiving (`configs/postgres/archive.conf` → `docker-compose.yml`), a separate `memory_rag_pg_archive` volume, and an opt-in `pg_backup` sidecar (Compose profile `backup`) that runs `pg_basebackup` nightly in the `03:00–05:00` window plus `pg_archivecleanup`-based retention. See `docs/RUNBOOK.md` "Raw-data durability and recovery" → "Backup automation".
- **OP-4.3 — restore drill: done (D-055).** Adds operator-grade restore tooling — `scripts/pg_restore/restore.sh` and the opt-in `pg_restore` Compose service (profile `restore`) — and executed the restore drill on the reference/local shape: a full restore recovered all raw `SourceMessage` rows (RTO 5 s) and a PITR restore recovered the expected pre-target rows (RTO 3 s); RPO is bounded by `archive_timeout=300` ≤ 5 min. Both D-053 targets met — see `docs/op4-drill/op4.3-20260519-evidence.json` and `docs/RUNBOOK.md` "Restore drill (OP-4.3 / D-055)". **Phase 8 raw-data durability DoD met; OP-4 complete.**

## OP-5 — Evaluation & observability (Stage 2 — operationalization) — complete
- Stage-2 packet group **OP-5** (D-044; see `docs/OPERATIONALIZATION-ROADMAP.md`). Closes Stage 2. The gold-eval-set work (7.1) extends the D-038 retrieval harness. **All four OP-5 packets have landed.**
- **OP-5.1 — gold eval set: done (D-056).** Added the OP-5 *observability* gold eval set — `eval/retrieval/observability/{gold.json,corpus.jsonl}`, ~21 queries over a 19-message corpus, curated for coverage diversity (≥2 negatives, ≥2 multilingual, ≥3 paraphrase, single/multi-hit mix). It sits **beside** the frozen D-038 baseline set (`eval/retrieval/{gold.json,corpus.jsonl}`) rather than superseding it — the D-038 Postgres baseline is still uncaptured. The default mock harness invocation still loads the D-038 set; the observability set is selected via explicit `--gold`/`--corpus` flags. `tests/test_retrieval_harness_shape.py` parametrized over both fixture pairs (mock-mode, shape-only). See `docs/RUNBOOK.md` "Gold-set fixtures — D-038 baseline vs OP-5 observability (D-056)".
- **OP-5.2a — retrieval hit-rate / empty-rate metrics: done (D-057).** Map: execution-map 7.2. `AggregateMetrics` gains `hit_rate` (fraction of non-empty-gold queries that surfaced an expected chunk in the fused list — **non-empty-gold denominator**, owner-confirmed, which keeps it distinct from `per_leg_recall_at_20.fused`) and `empty_rate` (fraction of all queries whose fused list came back empty); two pure helper functions compute them from the existing `PerQueryResult` rows; the CLI human report renders both with explicit denominator annotations. Inspection only — no thresholds, CLI exit stays `0`. See `docs/RUNBOOK.md` "Retrieval-quality inspection harness (D-038)".
- **OP-5.2b — answer-path groundedness-proxy metric: done (D-058).** Map: execution-map 7.2. `run_answer_harness` drives `QueryService.answer` over every gold query (`RouteKind.ASK`) and grades a per-answer `PerAnswerResult`; `GroundednessMetrics` aggregates `groundedness_rate` (**non-empty-gold denominator**, mirroring `hit_rate`) and `fallback_mode_counts` (over all queries). Groundedness is an explicit **fallback-derived proxy** for the I-9 citation-subset semantics — `{NONE, WEAK_EVIDENCE, AMBIGUOUS}` → grounded; `PARSE_FAILURE` (the I-9 violation contour) and `PROVIDER_UNAVAILABLE` / `NO_EVIDENCE` not grounded — **not** a citation-coverage or factuality score. The CLI section title is "Groundedness proxy (answer-path, fallback-derived, inspection only)"; the RUNBOOK subsection title matches verbatim. Inspection only — no thresholds, CLI exit stays `0`. Closes execution-map row 7.2 together with OP-5.2a. See `docs/RUNBOOK.md` "Groundedness proxy (answer-path, fallback-derived, inspection only)".
- **OP-5.3 — cost & latency aggregation: done (D-059).** Map: execution-map 7.3. `HarnessReport` gains an optional `cost_latency: CostLatencyMetrics | None` wrapping `CostMetrics` (token totals + per-call mean with an explicit `answer_calls_with_tokens` denominator) and `LatencyMetrics` (wall-clock mean / p50 / max for retrieval and for `QueryService.answer`, both denominator = all queries). Tokens flow from `ChatResponse.token_counts` via a new `RecordingChatClient` single-call / single-consumer shim whose `consume_last()` clear-on-read semantics structurally prevent prior-response misattribution onto no-chat-call rows. Wall-clock around `dense+sparse+RRF` excludes the query-embedding lookup (mode-asymmetric). Aggregate latency is wall-clock only — provider-attributed `ChatResponse.latency_ms` remains trace-level provenance on `AnswerTrace`. `p95` intentionally omitted at ~20-21 query gold-set size — too noisy. CLI section title `Cost & latency (wall-clock + provider-reported tokens, inspection only)` matches the RUNBOOK subsection verbatim. Inspection only — no thresholds, CLI exit stays `0`. **Closes execution-map row 7.3 and completes OP-5; the Stage-2 → Stage-3 operationalization gate is open.** See `docs/RUNBOOK.md` "Cost & latency (wall-clock + provider-reported tokens, inspection only)".
- **Still open before Stage 3 unblocks Slice 3.7:** the operator-run D-038 Postgres baseline capture (precondition for Slice 3.7 / D-039) — a Phase-3 quality-lever precondition, not a Stage-2 OP-5 item. The Stage-2 → Stage-3 gate itself is open.

---

Closed in the OP-5.3 cost-latency packet (D-059):
- `src/memory_rag/eval/retrieval/harness.py` — new `CostMetrics` / `LatencyMetrics` / `CostLatencyMetrics` dataclasses; new `RecordingChatClient` shim implementing the `ChatClient` Protocol with a single-call / single-consumer slot and read-and-consume `consume_last` (clear-on-read prevents misattribution onto no-chat-call rows); new pure helpers `_latency_stats(values) -> (mean, p50, max)`, `cost_metrics(rows) -> CostMetrics`, `latency_metrics(retrieval_rows, answer_rows) -> LatencyMetrics` (all return zero-valued metrics on empty input — empty-report → 0 contract); new per-row carrier fields `PerQueryResult.retrieval_latency_ms: float = 0.0` and `PerAnswerResult.{answer_latency_ms: float = 0.0, prompt_tokens: int = 0, completion_tokens: int = 0}` (defaults preserve kwarg-construction in existing test helpers). `run_harness` wraps `dense + sparse + RRF` with `time.perf_counter` and populates `retrieval_latency_ms`; the query-embedding lookup stays outside this boundary because mock and Postgres modes obtain query embeddings asymmetrically (live `MockEmbeddingClient.embed` vs pinned `embeddings_cache.json`). `run_answer_harness` gains keyword-only `chat_recorder: RecordingChatClient | None = None`, wraps `query_service.answer(...)` in `time.perf_counter` for `answer_latency_ms`, and reads provider-reported token counts via `consume_last()` (`token_counts.get("prompt", 0)` + `.get("completion", 0)`). `HarnessReport` gains an optional `cost_latency: CostLatencyMetrics | None = None` field.
- `src/memory_rag/eval/retrieval/__main__.py` — `_run_mock` and `_run_postgres` wrap the operator-selected chat client in `RecordingChatClient` before passing it to `QueryService`, forward the recorder to `run_answer_harness(chat_recorder=...)`, compute `CostLatencyMetrics` via `cost_metrics` / `latency_metrics`, and attach it via `dataclasses.replace`. `_format_human` renders the new `Cost & latency (wall-clock + provider-reported tokens, inspection only)` section after the groundedness block, with explicit denominator annotations: `(denominator: answer-path calls with non-empty token_counts, n=<int>)` on the per-call mean, `(denominator: all queries)` on both latency blocks. JSON output picks up the new fields via `asdict`. `_TRUNCATE_TABLES` is **unchanged** — OP-5.2b already covers the answer-path tables and OP-5.3 adds no persistence.
- `tests/test_retrieval_harness_cost_latency.py` (new, 12 tests) — pure-function semantics: `cost_metrics([])` → zero-valued `CostMetrics`; sum / mean correctness; zero-token-row exclusion from the `answer_calls_with_tokens` denominator (rows with `(10, 20, 0)` totals → `answer_calls_with_tokens=2`, mean = 15.0); div-by-zero guard returns `0.0`; `_latency_stats` mean / p50 / max on a fixed list; even-length median averages the two middle values; empty `_latency_stats` → `(0.0, 0.0, 0.0)`; `latency_metrics` per-half independence; the `RecordingChatClient` read-and-consume contract; **`test_recorder_no_misattribution_across_calls`** — the user-required guardrail: first call records tokens and is consumed; second call short-circuits (chat NOT invoked); second consume returns `None` and the row stays at zero tokens (no token leakage across mixed contours).
- `tests/test_retrieval_harness_shape.py` (extended) — the mock end-to-end helper wraps the `MockChatClient` in `RecordingChatClient`, drives the answer harness with it, and computes `cost_latency`; new `test_mock_mode_includes_cost_and_latency_shape` parametrized over both fixture pairs (D-038 baseline + OP-5 observability) with shape-only `isinstance` + `>= 0` assertions over every new field, the arithmetic check `total_tokens == prompt + completion`, and the distributional sanity `max >= mean` and `max >= p50` on each latency block; per-row `retrieval_latency_ms` / `answer_latency_ms` / `prompt_tokens` / `completion_tokens` type+non-negativity assertions in the existing per-query and groundedness shape tests. No quality-value assertions.
- Docs: D-059 in `decision-log.md` (with mock-mode observed numbers — deterministic token totals, machine-dependent latencies — and `[[feedback_harness_is_inspection_not_gate]]` / `[[feedback_metric_denominator_explicit]]` / `[[feedback_honest_mock_provenance]]` / `[[feedback_contract_decisions_in_decision_log]]` tags); row 7.3 filled (→ OP-5.3) in `execution-map.md`; new `Cost & latency (wall-clock + provider-reported tokens, inspection only)` subsection in `RUNBOOK.md` with denominator narration, the p50/p95 rationale (p50 included as small-sample robustness check; p95 intentionally omitted at ~20-21 sample size), and the wall-clock-vs-`ChatResponse.latency_ms` distinction; OP-5 flipped to *(complete)* in `OPERATIONALIZATION-ROADMAP.md` (Status + §2 / §3 OP-5 rows, plus the OP-2 ↔ OP-5.3 cross-cut rewritten in present tense) and this file.
- `core` change confined to the inspection-only `src/memory_rag/eval/` subsystem — no `AnswerResult` / `QueryService` / `parse_structured_answer` / `core/answers/` change, no production retrieval or answer-path behavior change, no schema / DDL / migration, no boot-gate or Telegram surface, no live OpenAI in `make check`. `INVARIANTS.md` / `RUNTIME-INVARIANTS.md` / `assumptions.md` untouched — the aggregates enforce nothing; the read-and-consume `RecordingChatClient` contract, wall-clock-only aggregate scope, query-embedding-lookup exclusion, `answer_calls_with_tokens` denominator, and p95-omission rationale are packet-level contract decisions in D-059.
- **Explicitly not in this packet:** quality thresholds / gating / regression budgets / CI gates over cost or latency; live OpenAI inside `make check`; any change to `AnswerResult` / `QueryService` / `parse_structured_answer` / retrieval-path code outside the eval-harness surface; aggregating provider-attributed `ChatResponse.latency_ms` (stays trace-level provenance on `AnswerTrace` per D-034 / D-035); aggregating `chat_model_name` per-row; distributional latency stats beyond mean / p50 / max (p95 deferred until a larger sample size); the operator-run D-038 Postgres baseline capture; D-026 renames; schema / DDL changes; exposing `cited_chunk_ids` on `AnswerResult` (still D-058's recorded follow-up).

Closed in the OP-5.2b groundedness-proxy packet (D-058):
- `src/memory_rag/eval/retrieval/harness.py` — new `PerAnswerResult` / `GroundednessMetrics` / `GroundednessReport` dataclasses; `_GROUNDED_FALLBACKS` constant + pure helpers `is_grounded` / `groundedness_rate` / `fallback_mode_counts`; `run_answer_harness(*, query_service, gold)` loop that for each gold query builds an `InboundMessage` (`RouteKind.ASK`), calls `QueryService.answer`, and grades a `PerAnswerResult` from `AnswerResult.fallback` + `AnswerResult.context`; optional `groundedness: GroundednessReport | None = None` field on `HarnessReport` (`run_harness` leaves it `None` — retrieval only; the CLI fills it).
- `src/memory_rag/eval/retrieval/__main__.py` — `_run_mock` / `_run_postgres` build a `QueryService` over the already-ingested store (mock: `MockChatClient`; postgres: `build_chat_client(settings)`, operator-selected `CHAT_BACKEND` — defaults to mock, no live API forced) and attach the groundedness report via `dataclasses.replace`. `_format_human` renders the "Groundedness proxy (answer-path, fallback-derived, inspection only)" section with `(proxy: fallback-derived; denominator: non-empty-gold queries only)` annotation and a sorted `fallback_mode_counts` breakdown. `_TRUNCATE_TABLES` extended with `answer_traces` / `retrieval_hits` / `queries` so an operator Postgres answer-harness run starts clean (eval DB only).
- `tests/test_retrieval_harness_groundedness.py` (new) — pure-function tests pinning the proxy mapping (`is_grounded` `True` for `NONE` / `WEAK_EVIDENCE` / `AMBIGUOUS`; `False` for `NO_EVIDENCE` / `PROVIDER_UNAVAILABLE` / `PARSE_FAILURE`; coverage check over every `FallbackMode` member); the non-empty-gold denominator for `groundedness_rate` (partial / all / none / all-negative / empty cases, negatives excluded even when their `grounded` flag differs); `fallback_mode_counts` shape (counts every row, sums to row count).
- `tests/test_retrieval_harness_shape.py` (extended) — the mock end-to-end helper now also builds `MockChatClient` + `QueryService` and runs `run_answer_harness`; new shape-only `isinstance` + `[0.0, 1.0]` bound assertions for `GroundednessReport` / `GroundednessMetrics` / `PerAnswerResult` over both fixture pairs, plus a per-row `grounded == is_grounded(FallbackMode(fallback_mode))` consistency check. No quality-value assertions.
- Docs: D-058 in `decision-log.md`; row 7.2 closed (→ OP-5.2a + OP-5.2b) in `execution-map.md`; new "Groundedness proxy (answer-path, fallback-derived, inspection only)" subsection in `RUNBOOK.md` (title matches the CLI section verbatim); OP-5.2b landed within OP-5 in `OPERATIONALIZATION-ROADMAP.md` (Status + §2 / §3 rows) and this file.
- `core` change confined to the inspection-only `src/memory_rag/eval/` subsystem — no `AnswerResult` / `QueryService` / `parse_structured_answer` change, no production retrieval or answer-path behavior change, no schema/migration, no boot-gate or Telegram surface, no live OpenAI in `make check`. `INVARIANTS.md` / `RUNTIME-INVARIANTS.md` / `assumptions.md` untouched — the proxy framing and the non-empty-gold denominator are packet-level contract decisions in D-058 (`[[feedback_contract_decisions_in_decision_log]]`, `[[feedback_metric_denominator_explicit]]`).
- **Explicitly not in this packet:** exposing `cited_chunk_ids` on `AnswerResult` for true citation-coverage metrics (recorded deferred follow-up in D-058); cost & latency aggregation (OP-5.3); the operator-run D-038 Postgres baseline capture; quality thresholds / gating / nDCG / factuality classification / CI gates; live OpenAI inside `make check`; any answer-path or retrieval-behavior change in production `src/`; schema/DDL changes; D-026 renames.

Closed in the OP-5.2a retrieval-metrics packet (D-057):
- `src/memory_rag/eval/retrieval/harness.py` — `AggregateMetrics` gains `hit_rate` + `empty_rate` (two `float` fields); new pure helper functions `hit_rate` / `empty_rate` compute them from the per-query `PerQueryResult` rows; `run_harness` populates the two fields. `hit_rate` uses a non-empty-gold denominator (owner-confirmed) — negative queries are excluded, keeping it distinct from `per_leg_recall_at_20.fused`; `empty_rate` divides by all queries.
- `src/memory_rag/eval/retrieval/__main__.py` — `_format_human` renders both metrics with explicit denominator annotations (`(denominator: non-empty-gold queries only)` / `(denominator: all queries)`); the `--json` path picks them up via `asdict`.
- `tests/test_retrieval_harness_metrics.py` (new) — pure-function tests pinning `hit_rate` / `empty_rate` semantics on constructed inputs: non-empty-gold denominator, negatives excluded from `hit_rate`'s denominator and counted in `empty_rate`'s, empty-report → `0.0`.
- `tests/test_retrieval_harness_shape.py` (extended) — shape-only `isinstance` + `[0.0, 1.0]` bounds assertions for the two new aggregate fields over both fixture pairs. No quality-value assertions.
- Docs: D-057 in `decision-log.md`; row 7.2 filled (→ OP-5.2a) in `execution-map.md`; hit-rate / empty-rate documented in the harness subsection of `RUNBOOK.md`; OP-5.2a landed within OP-5 in `OPERATIONALIZATION-ROADMAP.md` (Status + §2 / §3 rows) and this file.
- `core` change confined to the inspection-only `src/memory_rag/eval/` subsystem — no production retrieval-behavior change (`services/retrieval.py` / `storage/search_repository.py` untouched), no schema/migration, no boot-gate or Telegram surface, no live OpenAI in `make check`. `INVARIANTS.md` / `RUNTIME-INVARIANTS.md` / `assumptions.md` untouched — the `hit_rate` denominator (owner-confirmed) and `empty_rate` definition (proposed default) are packet-level contract decisions in D-057.
- **Explicitly not in this packet:** the groundedness check / any `QueryService.answer` / `AnswerContext` / I-9 citation-subset routing (OP-5.2b); cost & latency aggregation (OP-5.3); the operator-run D-038 Postgres baseline capture; quality thresholds / gating / nDCG / graded relevance / CI gates; live OpenAI inside `make check`; any retrieval-behavior change in production `src/`; schema/DDL changes; D-026 renames.

Closed in the OP-5.1 gold-eval-set packet (D-056):
- `eval/retrieval/observability/gold.json` + `eval/retrieval/observability/corpus.jsonl` (new) — the OP-5 observability gold set: ~21 queries over a 19-message corpus (`obs-msg-1`…`obs-msg-19`, a distinct id prefix; `community_id_default` = `eval-community`). Curated for coverage diversity per the OP-5.1 contract recorded in D-056 and the file's `_coverage_axes` key. Sits **beside** the frozen D-038 baseline set, not superseding it (D-038 Postgres baseline still uncaptured).
- `tests/test_retrieval_harness_shape.py` (extended) — `OBS_GOLD_PATH` / `OBS_CORPUS_PATH` constants; the two end-to-end shape tests and the two corpus-ingest/provenance tests parametrized over both fixture pairs. Mock-mode, shape-only — no quality-value assertions. The end-to-end mock run resolves every observability handle, so a bad handle fails `make check`.
- `.gitignore` — narrow entries for the operator-produced `eval/retrieval/embeddings_cache.json` and `eval/retrieval/*/embeddings_cache.json` (D-038 had left this implicit).
- Docs: D-056 in `decision-log.md`; row 7.1 filled (→ OP-5.1) in `execution-map.md`; "Gold-set fixtures" subsection + invocation contract in `RUNBOOK.md`; OP-5 in progress / OP-5.1 landed in `OPERATIONALIZATION-ROADMAP.md` (Status + §2 / §3 rows) and this file.
- No harness code change (the CLI and `regenerate_embeddings` already accept `--gold`/`--corpus`/`--cache` flags), no `src/` change, no retrieval-behavior change, no schema/migration. `INVARIANTS.md` / `RUNTIME-INVARIANTS.md` / `assumptions.md` untouched — set relation, naming, and coverage axes are packet-level contract decisions in D-056.
- **Explicitly not in this packet:** the operator-run D-038 Postgres baseline capture; a Postgres-mode capture over the observability set and its embeddings-cache regeneration; retrieval/groundedness metrics and cost/latency aggregation (OP-5.2 / OP-5.3); hard thresholds / quality gating / nDCG / graded relevance; live OpenAI inside `make check`; any retrieval-behavior or harness code change; D-026 renames; schema/DDL changes.

---

Closed in the OP-4.2 backup-automation packet (D-054):
- `docker-compose.yml` — the `postgres` service `command:` passes `-c` flags for `wal_level=replica`, `archive_mode=on`, `archive_timeout=300` (~5 min), and an idempotent/atomic `archive_command` copying each completed WAL segment into `/archive/wal`; mounts the new `memory_rag_pg_archive` volume at `/archive`; a profile-less one-shot `pg_archive_init` service creates `/archive/{wal,base}` and `chown`s it to UID 999; a new `pg_backup` sidecar under the `backup` Compose profile runs the scheduler. New named volume `memory_rag_pg_archive`.
- `configs/postgres/initdb-replication-hba.sh` (new) — initdb-time hook (mounted into `/docker-entrypoint-initdb.d/`) that appends a `host replication` rule to `pg_hba.conf` so the `pg_backup` sidecar's `pg_basebackup` can connect over the Compose network.
- `scripts/pg_backup/{scheduler,backup,prune}.sh` (new) — one long-running `scheduler.sh` per container runs `backup.sh` (`pg_basebackup --format=tar --wal-method=none --gzip`, recording `START_WAL`) then `prune.sh` (30-day base-backup retention + `pg_archivecleanup` keyed on the oldest retained backup) once per day in the `[BACKUP_WINDOW_START,END)` local window; `backup.sh`/`prune.sh` share a `flock` on `/archive/.backup.lock`; cycles write `/archive/last_success.json` / `last_failure.json`.
- `.env.example` (`BACKUP_WINDOW_START` / `BACKUP_WINDOW_END` / `BASE_RETENTION_DAYS` / `TZ`); `Makefile` (`backup-up` / `backup-run` / `backup-prune`).
- Docs: D-054 in `decision-log.md`; OP-4.2 row filled + OP-4 narrative in `execution-map.md`; "Backup automation" subsection in `RUNBOOK.md`; OP-4.2 landed in `OPERATIONALIZATION-ROADMAP.md` (Status + §2 / §3 rows) and this file.
- Infra + config + docs only — no schema / migration / `src/` change, no boot-gate or Telegram surface. `make check` is unaffected. `assumptions.md` / `assumption-audit.md` untouched — A-40 stays closed, A-41 stays open.
- **Explicitly not in this packet:** the executed restore drill and any RPO/RTO measurement (OP-4.3); managed-cloud / self-hosted shape automation and the provider choice (A-41); off-box / remote archive storage; any schema/migration or `src/` change; OP-5 work; the D-026 `diary_rag` / `family_id` / `Note` renames.

Closed in the OP-3.2b failed-embedding retry dead-letter-routing packet (D-052):
- `src/memory_rag/services/reconciliation.py` — `retry_failed_chunks`'s failure path builds one `IndexingDeadLetter` (fresh `uuid4` id; the group's `source_message_id` / `community_id` / `chunk_ids`; live client `model_name`; exception class name; retry-run timestamp) and persists it via the existing `DomainRepository.save_indexing_dead_letter` seam. Best-effort and append-only — the group is already `failed`, the write runs after that outcome, and a write failure is logged (`dead_letter.write_failed`) and swallowed so it never regresses the outcome or raises. `RetryGroupOutcome` gains `dead_letter_id: str | None` (set iff the group failed and its write succeeded); the `reconciliation.retry.group.failed` log and `render_retry_report` surface it. No new repository method, no schema, no migration.
- `tests/test_reconciliation.py` (extended) — exhausted-group dead-letter write (row fields + outcome identity), dead-letter write failure swallowed (no raise, no state regression, `dead_letter.write_failed` logged), success path writes no dead letter, `render_retry_report` shows / omits `dead_letter_id`, PG-DSN-gated end-to-end exhausted-retry dead-letter row.
- Docs: D-052 in `decision-log.md`; OP-3.2b row landed + OP-3 narrative in `execution-map.md`; "Failed embeddings" dead-letter routing in `RUNBOOK.md`; OP-3.2b landed / OP-3 complete in `OPERATIONALIZATION-ROADMAP.md`; **A-35 closed** in `assumptions.md` and `assumption-audit.md`.
- No new repository method / schema / migration / boot-gate / scheduler / Telegram surface. No live OpenAI call in `make check`. `INVARIANTS.md` / `RUNTIME-INVARIANTS.md` untouched — the dead-letter write reuses the OP-2.2 append-only, best-effort discipline.
- **Explicitly not in this packet:** any new dead-letter schema / column (`resolved`, `status`, retry counters) / migration; dead-letter dedup; scheduler / background / periodic retry; any bounded-attempt policy beyond OP-2's client-internal retry; OP-4 / OP-5 work; the D-026 `diary_rag` / `family_id` / `Note` renames.

Closed in the OP-3.2a failed-embedding retry-execution packet (D-051):
- `src/memory_rag/services/reconciliation.py` — `ReconciliationService.__init__` gains an optional `embedding_client: EmbeddingClient | None`; new `retry_failed_chunks(community_id, *, limit=None) -> RetryOutcomeReport` consumes the OP-3.1 `list_failed_event_chunks` seam, groups failed chunks by `source_message_id`, retries each group with one `EmbeddingClient.embed` call (OP-2 bounded backoff internal to the client), persists `EmbeddingRecord` rows then transitions succeeded chunks `failed → ready`, leaves exhausted-retry chunks `failed` (no state regression), and raises `RuntimeError` without a client. New channel-neutral frozen/slotted `RetryGroupOutcome` / `RetryOutcomeReport` and pure `render_retry_report`; `reconciliation.retry.group.ok|failed` + `reconciliation.retry.summary` logs; `_main` gains a `--retry` mode (`build_embedding_client(Settings())` + Postgres store). Reuses `save_embedding_records` / `set_chunk_embedding_status` — no new repository method.
- `src/memory_rag/services/__init__.py` — also re-exports `RetryOutcomeReport`.
- `tests/test_reconciliation.py` (extended) — retry success (`failed → ready`, records, honest provenance), exhausted failure, per-`source_message_id` grouping, mixed outcomes, empty-set no-op, `limit` parity, records-before-status ordering, `UNIQUE`-collision group failure, missing-client `RuntimeError`, `render_retry_report`, `_main --retry` offline wiring, PG-DSN-gated end-to-end retry.
- Docs: D-051 in `decision-log.md`; OP-3.2 row split into OP-3.2a / OP-3.2b in `execution-map.md`; retry subsection in `RUNBOOK.md` "Failed embeddings"; OP-3.2a recorded as landed in `OPERATIONALIZATION-ROADMAP.md`.
- No new repository method / schema / migration / boot-gate / scheduler / Telegram surface. No live OpenAI call in `make check`. `INVARIANTS.md` / `RUNTIME-INVARIANTS.md` / `assumptions.md` untouched — the retry path reuses existing guarantees and A-35 stays open.
- **Explicitly not in this packet:** dead-letter routing for exhausted retries; any new dead-letter schema / column / migration; A-35 closure; scheduler / background worker / boot gate / Telegram surface; the D-026 `diary_rag` / `family_id` / `Note` renames.

Closed in the OP-3.1 failed-embedding discovery packet (D-050):
- `src/memory_rag/storage/repository.py` — `DomainRepository` Protocol gains one method, `list_failed_event_chunks(community_id, *, limit=None) -> list[EventChunk]`: chunks at `embedding_status='failed'`, community-scoped (I-7 / R-3), ordered `(created_at ASC, chunk_id ASC)` oldest-first, optional `limit`.
- `src/memory_rag/storage/{mock,sqlite,postgres}/store.py` — the method implemented with full parity; validation (empty `community_id`, negative `limit`) mirrors `list_indexing_dead_letters`. Postgres reuses the OP-1.2 `idx_event_chunks_embedding_status` index — no new schema or migration.
- `src/memory_rag/services/reconciliation.py` (new) — `ReconciliationService.discover_failed_chunks` (read-only discovery; emits `reconciliation.discovered`); channel-neutral frozen/slotted `FailedEmbeddingReport`; pure `render_report`; the `python -m memory_rag.services.reconciliation --community <id> [--limit N]` operator CLI (Postgres-targeted, mirroring `migrations_runner`; `DEFAULT_DISCOVERY_LIMIT = 100`).
- `src/memory_rag/services/__init__.py` — re-exports `ReconciliationService` and `FailedEmbeddingReport`.
- `tests/test_storage_failed_chunks.py` (new) — mock + sqlite offline parity, PG-DSN-gated postgres parity (ordering, limit, empty, community scoping, only-`failed`, validation). `tests/test_reconciliation.py` (new) — service discovery, read-only assertion, `render_report`, `_main` wiring via an injected store, PG-DSN-gated end-to-end.
- Docs: D-050 in `decision-log.md`; new OP-3 section + OP-3.1 row in `execution-map.md`; "Failed embeddings" subsection in `RUNBOOK.md` (CLI as the inspection surface); OP-3.1 recorded as landed in `OPERATIONALIZATION-ROADMAP.md`.
- No schema / migration / boot-gate / scheduler / Telegram surface. No new runtime dependency. A-34 does not apply. `INVARIANTS.md` / `RUNTIME-INVARIANTS.md` / `assumptions.md` untouched — read-only discovery enforces no invariant and A-35 stays open.
- **Explicitly not in this packet:** any retry loop re-invoking `EmbeddingClient`; the `failed → ready` transition; bounded backoff during reconciliation; dead-letter write routing; scheduler / background worker / boot gate / Telegram surface; retry-outcome metrics beyond the discovery report; a total-failed-count query; A-35 resolution; the D-026 `diary_rag` / `family_id` / `Note` renames.

Closed in the Slice 3.4 date-range retrieval-filter packet (D-040):
- `src/diary_rag/core/diary/models.py` adds the channel-neutral frozen/slotted `DateRange(start, end)` value object — inclusive `entry_date` lower/upper bound, both sides `date | None`, both-`None` is a valid no-constraint range, `start > end` rejected in `__post_init__`. `src/diary_rag/core/diary/__init__.py` re-exports it.
- `src/diary_rag/storage/search_repository.py` — both `SearchRepository` Protocol legs gain keyword-only `date_range: DateRange | None = None`.
- `src/diary_rag/storage/postgres/store.py` — module-level `_date_range_sql` helper composes a conditional `entry_date >= / <=` fragment with positional params; both legs splice it after the existing `WHERE` predicates.
- `src/diary_rag/storage/mock/store.py` — module-level `_chunk_in_date_range` helper; both legs apply the identical inclusive comparison (mock/Postgres parity).
- `src/diary_rag/storage/sqlite/store.py` — both legs accept the new keyword-only param for `HybridDiaryStore` Protocol parity; bodies unchanged (still `NotImplementedError` per D-025).
- `src/diary_rag/services/query_service.py` — `answer` gains a per-call keyword-only `date_range` threaded to both legs; RRF, trace persistence, and the `Query` row are untouched.
- `tests/test_diary_models.py` (new) — `DateRange` validation. `tests/test_search_repository_{mock,postgres}.py` and `tests/test_query_service.py` extended with date-filter cases (partial / full range, inclusive bounds, `None`-unchanged, mock/Postgres parity matrix).
- Docs: D-040 in `decision-log.md`; TechSpec §9 (leg signatures + new "Metadata filtering" subsection); `execution-map.md` row 3.4.
- No schema / DDL / migration change; A-34 does not apply. `services/retrieval.py` and the eval harness unchanged. New runtime dependencies: none.
- **Explicitly not in this packet:** `child_id` and `visibility_scope` filtering (dimensions 2–3; visibility waits on A-15); inbound Telegram date syntax / NL date parsing; SQLite real retrieval; schema / DDL / migration; retrieval-quality tuning / date-diversity reranking; `Query` row schema changes; the D-026 renames.

Closed in the Slice 3.6 retrieval-quality inspection harness packet (D-038):
- `src/diary_rag/eval/__init__.py` and `src/diary_rag/eval/retrieval/__init__.py` (new namespace packages).
- `src/diary_rag/eval/retrieval/harness.py` (new) — pure metric functions (`recall_at_k`, `mrr_at_k`, `first_relevant_rank`), frozen-dataclass report shape (`GoldQuery`, `GoldSet`, `CorpusMessage`, `PerLegRecall`, `AggregateMetrics`, `PerQueryResult`, `HarnessReport`), gold/corpus/embedding-cache loaders (cache loader aborts on `model_name` / `dimension` mismatch), `ingest_fixture_corpus` which drives `DiaryService.ingest` and builds the `f"{external_message_id}#{event_index}"` → live `chunk_id` handle map (event_index = 0-based EventChunk ordinal within the parsed source message; restated in module/function docstrings and in the gold-set schema), and `run_harness(*, mode, store, gold, handles_to_chunk_ids, embedding_model_name, query_embedding_lookup, top_k=5, candidate_k=20, corpus_size)` which calls `SearchRepository.dense_candidates` + `sparse_candidates` + `reciprocal_rank_fusion` exactly the way `QueryService` does and computes the report.
- `src/diary_rag/eval/retrieval/__main__.py` (new) — CLI with `--mode {mock,postgres}`, optional `--gold`, `--corpus`, `--embeddings-cache`, `--top-k`, `--candidate-k`, `--json`. Mock mode wires `MockDiaryStore` + `MockEmbeddingClient` in-process. Postgres mode requires `DIARY_RAG_PG_TEST_DSN`, truncates `embedding_records, event_chunks, diary_entries, source_messages`, builds the `EmbeddingClient` via `build_embedding_client(Settings())` so the corpus-side model_name matches the cached query embeddings, ingests via `DiaryService.ingest`, then runs gold queries with cache-backed query embeddings. Exit code is always `0` — inspection, not gate.
- `src/diary_rag/eval/retrieval/regenerate_embeddings.py` (new) — operator-only ritual that pins query embeddings to `text-embedding-3-large` @ 3072-dim. Refuses to overwrite an existing cache without `--force` (regenerating invalidates prior baseline snapshots); refuses to run without `OPENAI_API_KEY`.
- `eval/retrieval/gold.json` (new) — 12 hand-curated queries (clear-keyword, paraphrase, mixed-signal, two Russian queries, one no-expected-match query). Schema includes a `_handle_contract` field documenting the handle scheme.
- `eval/retrieval/corpus.jsonl` (new) — 12 fixture raw `SourceMessage`s producing 36 chunks total. Mix of English and one Russian line per the A-37 contour.
- `eval/retrieval/embeddings_cache.json` — **not** committed by this packet; produced by the operator-only regenerate script.
- `tests/test_retrieval_harness_shape.py` (new) — 10 mock-mode cases under `make check`: report shape; per-query shape including the three `first_relevant_rank_in_{dense,sparse,fused}` fields, the explicit `reciprocal_rank_in_fused: float`, and `recall_at_{5,10,20}`; `reciprocal_rank_in_fused == 1.0/rank` consistency; pure-function correctness for `recall_at_k` (incl. `k=10`), `mrr_at_k`, `first_relevant_rank`; handle-resolution (0-based EventChunk ordinal); honest mock provenance (`model_name == "mock"`); `KeyError` on stale gold handle; CLI imports cleanly without DSN and raises a clear `RuntimeError` when `--mode postgres` is invoked without one; `DiaryService.ingest` succeeds on every fixture corpus message. No quality-value assertions.
- Docs: D-038 in `decision-log.md` (with a "Baseline snapshot (observed)" subsection filled in by the operator-run Postgres-mode measurement); new "Retrieval-quality inspection harness (D-038)" subsection in `RUNBOOK.md` covering both modes, the regenerate ritual, and the handle contract; row 3.6 in `execution-map.md`; "search-quality fork" item in `todo.md` narrowed to "pick one variant and measure against the D-038 baseline; do not bundle more than one".
- `docs/INVARIANTS.md` and `docs/RUNTIME-INVARIANTS.md` not touched — the harness does not change what code enforces.
- `docs/assumptions.md` not touched — packet-level contract decisions (handle scheme, metric shape, cache-regenerate policy) are recorded in D-038, not opened as assumptions.
- New runtime dependencies: none. No schema change, no migration, no boot-gate change. A-34 destructive-upgrade discipline does not apply.
- **Explicitly not in this packet:** any retrieval-behavior change in `services/retrieval.py` or `storage/search_repository.py`; live OpenAI calls inside `make check`; hard-threshold assertions or any quality gate; the first quality-decision packet (BM25 via `pg_search` / `bm25_catalog` / app-side BM25, **or** reranker / cross-encoder, **or** Qdrant / external vector DB, **or** multilingual sparse tuning beyond `simple`, **or** A-36b halfvec(3072) + HNSW); Slice 3.4 metadata filtering; schema / DDL / migration changes (A-34); nDCG / graded-relevance metrics; multi-snapshot / time-series tracking; wiring the harness through `QueryService.answer` end-to-end; the `RouteKind.ENTRY → NOTE` / `DiaryEntry` / `family_id` / `diary_rag` package renames (D-026).

Closed in the Slice 4.5 OpenAI chat adapter packet (D-037):
- `src/diary_rag/adapters/answers/openai_client.py` (new) — `OpenAIChatClient(api_key, *, model_name)`. Constructor refuses empty `api_key` / empty `model_name`; lazy-imports `openai`. `complete(prompt)` calls `client.chat.completions.create(model=…, messages=[{system,user}], response_format={"type":"json_object"}, temperature=0)`, single attempt; measures wallclock latency with `time.perf_counter()`; maps `response.usage.{prompt_tokens, completion_tokens}` → `{"prompt", "completion"}` (empty if `usage is None`). `openai.OpenAIError` / `TimeoutError` are caught at the SDK boundary and re-raised as `ChatProviderUnavailableError` so D-035 grading writes `FallbackMode.PROVIDER_UNAVAILABLE`.
- `src/diary_rag/adapters/answers/factory.py` — new `openai` branch: `OpenAIChatClient(api_key=settings.openai_api_key, model_name=settings.chat_model)`. Mock branch unchanged. Module docstring updated.
- `src/diary_rag/adapters/answers/__init__.py` — re-exports `OpenAIChatClient`.
- `src/diary_rag/config.py` — `chat_backend: Literal["mock","openai"]` (default `"mock"`); `chat_model` docstring records it is load-bearing for the openai backend.
- `src/diary_rag/app.py` — `_CANONICAL_OPENAI_CHAT_MODEL = "gpt-4.1"`; `_verify_chat_contour` aborts when `chat_backend == "openai"` and `chat_model != "gpt-4.1"`; the `OpenAIChatClient` constructor's missing-key `ValueError` continues to surface through `BootHealthError`; the existing non-empty `model_name` check is unchanged. The `app.created` log line gains `chat_model=…`.
- `.env.example` — Chat contour stanza updated; canonical `CHAT_MODEL=gpt-4.1` is the default value; default `CHAT_BACKEND` stays `mock`.
- New tests: `tests/test_boot_chat_gate.py` (default-clean / mock-ignores-chat-model / canonical-success / wrong-model-aborts / empty-model-aborts / missing-key-aborts — all offline, no real OpenAI calls); `tests/test_chat_client_openai.py` (optional live smoke gated by `DIARY_RAG_OPENAI_TEST_KEY`, **not** part of `make check`).
- Docs: D-037 in `decision-log.md`; A-9 closed (`→ D-037`) in `assumptions.md`; R-10 in `RUNTIME-INVARIANTS.md` extended in place with a Slice 4.5 stanza; new "Chat backend (D-037)" subsection in `RUNBOOK.md`; row 4.5 added in `execution-map.md`.
- New runtime dependencies: none. `openai` is already pulled in by D-024.
- A-34 destructive-upgrade discipline does not apply (no DDL change).
- **Explicitly not in this packet:** Telegram answer rendering / `/sources` / dispatcher (D-036, already landed); retries / backoff / circuit breakers (Phase 6); dead-letter / repair loops (Phase 6); rate-limit awareness, request hashing, cost tracking (Phase 6 / Phase 7); streaming, multi-turn, tool-use; Anthropic or any second provider; provider observability expansion beyond the existing `chat_backend` / `chat_model` log fields; `confidence_band` schema work (still deferred per D-035); live OpenAI calls inside `make check`; `RouteKind.ENTRY → NOTE` / `DiaryEntry` / `family_id` / `diary_rag` package renames (D-026); migration tooling / DDL changes (A-34); metadata filtering / Slice 3.4; search-quality fork (BM25 / reranker / Qdrant / halfvec / HNSW).

Closed in the Slice 4.4 answer-text reply + on-demand `/sources` packet (D-036):
- `src/diary_rag/core/routing/models.py` — `RouteKind.SOURCES` added (`lifecycle_for(SOURCES) → "other"` via fallthrough); `DispatchResult.source_blocks: list[str] | None = None` added next to `drafts` / `document` for the SOURCES outbound branch.
- `src/diary_rag/adapters/telegram/commands.py` — `"/sources": RouteKind.SOURCES` added to `COMMAND_TOKENS`; module docstring updated.
- `src/diary_rag/services/dispatcher.py` — `_format_answer_reply` rewritten: body for `NONE` / `WEAK_EVIDENCE` / `AMBIGUOUS` is now `result.answer_text + "\n\n" + <trailer>`; the four fixed-reply contours (`PROVIDER_UNAVAILABLE`, `PARSE_FAILURE`, both `NO_EVIDENCE` sub-paths) are unchanged. The LLM-marker `NO_EVIDENCE` branch deliberately does not surface the model's `"no_evidence"` prose. `_format_evidence_lines` removed. New module-level constants: `_REPLY_SOURCES_NONE`, `_render_source_block(chunk)`. `Dispatcher.__init__` adds private `self._latest_sources: dict[str, tuple[EventChunk, ...]] = {}`; the ASK branch calls a new `_update_latest_sources(family_id, answer)` helper that overwrites on non-empty `answer.context.ordered_chunks` and clears otherwise (every `/ask` writes — no skip path). New `_dispatch_sources(message)` reads the cache and returns a `DispatchResult` carrying `source_blocks=[…]` plus a header `f"Selected chunks for your last /ask ({N} chunk(s)):"` (or the fail-closed reply when the cache is empty). `_REPLY_START` / `_REPLY_HELP` / `_REPLY_UNKNOWN` strings updated to list `/sources`.
- `src/diary_rag/adapters/telegram/webhook.py` — SOURCES outbound branch added after the `result.drafts` block: calls `pack_drafts_into_messages(result.reply_text, result.source_blocks)` and delivers each message via `telegram_client.send_message`, mirroring the drafts error-handling shape (`sources.delivery_failed` log line; short usage-fallback on outbound failure; `sources.delivered` info log on success).
- New tests: `tests/test_dispatcher_sources.py` (full cache lifecycle — fail-closed contour, ASK→SOURCES success, two-ASK invalidation, empty-retrieval clears, empty-query clears, `PROVIDER_UNAVAILABLE` / `PARSE_FAILURE` still overwrite, two-family isolation, `/sources` is read-only); `tests/test_telegram_sources.py` (webhook combined single-message delivery, fail-closed inline reply, forced multi-message split on oversized chunks).
- Extended tests (in place): `tests/test_dispatcher_retrieval_fallback.py` weak-evidence + ambiguous cases flipped from evidence-bullets assertions to `answer_text` body assertions; `tests/test_end_to_end_smoke.py` `Found N memory` shapes flipped to assert the mock chat's deterministic `answer_text` body + unchanged trailers; new `/ask → /sources` smoke chains added; weak-evidence smoke body assertion flipped; `tests/test_telegram_reply.py` help-reply asserts `/sources` is listed; `tests/test_telegram_commands.py` parses `/sources` to `RouteKind.SOURCES`.
- Docs: D-036 in `decision-log.md` with the `/sources` semantic contract (selected chunks as-is, NOT citations, NOT fine-grained attribution, NOT the pre-RRF candidate pool), the cache lifecycle, the Dispatcher singleton proof, and the multi-worker caveat. `R-6` in `RUNTIME-INVARIANTS.md` tightened in place to record that the reply body is now `answer_text`. `RUNBOOK.md` "Command surface" subsection updated to list `/sources`; new "Selected-chunks recall (`/sources`, D-036)" subsection added. `execution-map.md` row 4.4 updated. `assumptions.md` not touched (internal contract decisions go in the decision-log).
- No schema change. No migration. No new env var. No new runtime dep. A-34 destructive-upgrade discipline does not apply.
- **Explicitly not in this packet:** rendering `StructuredAnswer.cited_chunk_ids` (the LLM-emitted subset); fine-grained / per-quote / per-sentence attribution; rendering the pre-RRF candidate pool; cross-restart or cross-worker durability of the latest-sources cache; `/sources N` argument parsing or retention/expiry; user-facing `/trace` command; real OpenAI / Anthropic chat adapter (Phase 6); retry / repair / recovery loops; metadata filtering (Slice 3.4); search-quality fork (BM25 / reranker / Qdrant / halfvec / HNSW); schema migration tooling (A-34); the `RouteKind.ENTRY → NOTE` / `DiaryEntry` / `family_id` / `diary_rag` package renames (D-026).

Closed in the Slice 4.3b answer-side fallback grading packet (D-035):
- `src/diary_rag/core/diary/models.py` — `FallbackMode` extended with `WEAK_EVIDENCE`, `AMBIGUOUS`, `PROVIDER_UNAVAILABLE`, `PARSE_FAILURE`. `AnswerTrace` docstring rewritten in place around the eight-row contract table (one row per effective path; the same value of `Query.fallback` is written from the same decision per call so they always agree).
- `src/diary_rag/core/diary/answer_schema.py` — `UncertaintyMarker = Literal["confident", "uncertain", "no_evidence", "ambiguous"]`. The parser's existing "empty `cited_chunk_ids` only with `no_evidence`" rule now applies to the extended marker set; `"uncertain"` and `"ambiguous"` therefore require non-empty citations.
- `src/diary_rag/core/answers/client.py` — `ChatProviderUnavailableError` added as a typed exception; re-exported from `src/diary_rag/core/answers/__init__.py`. Real provider adapters will raise it in Phase 6; the mock never raises naturally.
- `src/diary_rag/services/query_service.py` — `answer()` reorganised: a single `_finalize(...)` helper persists `Query`, retrieval hits, and `AnswerTrace` from one decision so `Query.fallback` and `AnswerTrace.fallback_mode` are always equal. `chat_client.complete` is wrapped in a narrow `except ChatProviderUnavailableError:` (graded as `PROVIDER_UNAVAILABLE`); `parse_structured_answer` is wrapped in a narrow `except StructuredAnswerError:` (graded as `PARSE_FAILURE`, with `response.raw_text` preserved as `answer_text` for forensics). `_persist_no_evidence_answer_trace` generalised to `_persist_answer_trace`. New module-level `_MARKER_TO_FALLBACK` map.
- `src/diary_rag/services/dispatcher.py` — `_format_answer_reply` switches on `AnswerResult.fallback` and sub-branches `NO_EVIDENCE` on `bool(AnswerResult.evidence)` so the LLM-marker path (retrieval returned chunks; model judged them not-evidence) produces a distinct reply from the empty-retrieval path (R-6). Four new module-level reply constants: `_TRAILER_WEAK_EVIDENCE`, `_TRAILER_AMBIGUOUS`, `_REPLY_PROVIDER_UNAVAILABLE`, `_REPLY_PARSE_FAILURE`. Heuristic-marker append unchanged.
- `src/diary_rag/storage/postgres/schema.sql` — CHECK constraints on `queries.fallback` and `answer_traces.fallback_mode` widened to admit the four new `FallbackMode` values.
- `src/diary_rag/storage/sqlite/store.py` — same CHECK widening in the in-file DDL.
- `src/diary_rag/storage/mock/store.py` — no change (no validator).
- New tests: per-contour cases in `tests/test_query_service.py` (weak-evidence, ambiguous, LLM-marker `no_evidence` distinct from empty-retrieval, provider-unavailable, parse-failure — each with stub chat clients defined locally); the dedicated LLM-marker distinctness case and five other contour cases in `tests/test_dispatcher_retrieval_fallback.py` (now covering both command and heuristic route-source for at least one contour); parametrised round-trip across mock + sqlite + postgres for each new `FallbackMode` in `tests/test_storage_answer_traces.py` plus a Postgres negative case asserting the CHECK rejects an unknown value; `"ambiguous"` accepted with citations / `"ambiguous"`/`"uncertain"` rejected with empty citations / parametrised round-trip across all four markers in `tests/test_answer_schema.py`; weak-evidence webhook smoke in `tests/test_end_to_end_smoke.py` asserting log line + reply trailer + persisted trace shape end-to-end.
- Docs: D-035 in `decision-log.md`. I-9 in `INVARIANTS.md` and R-5 + R-6 in `RUNTIME-INVARIANTS.md` tightened in place; the "deferred to Slice 4.3" phrasing on the answer-trace half is removed. RUNBOOK Answer-traces subsection rewritten around the per-`fallback_mode` shape table. `execution-map.md` row 4.3b updated to record the landed grading work. `assumptions.md` not touched (packet-level contract decisions go in the decision-log).
- New runtime dependencies: none.
- A-34 destructive-upgrade discipline applies to existing local Postgres volumes that pre-date the CHECK widening on `queries.fallback` and `answer_traces.fallback_mode`.
- **Explicitly not in this packet:** real OpenAI / Anthropic chat adapter (Phase 6); Telegram answer-text rendering / citation rendering / reply rewrite (Slice 4.4); retry / repair / recovery loops for `PARSE_FAILURE` or `PROVIDER_UNAVAILABLE`; `confidence_band` column (deferred with rationale recorded in D-035); metadata filtering (Slice 3.4); search-quality fork (BM25 / reranker / Qdrant / halfvec / HNSW); schema migration tooling (A-34); the `RouteKind.ENTRY → NOTE` / `DiaryEntry` / `family_id` / `diary_rag` package renames (D-026); `MockChatClient` production-shaped knobs.

Closed in the Slice 4.3a chat-client + AnswerTrace packet (D-034):
- `src/diary_rag/core/answers/{__init__,client}.py` (new) — channel-neutral `ChatClient` Protocol (`model_name: str`, `complete(prompt: AnswerPrompt) -> ChatResponse`) plus `ChatResponse(raw_text, model_name, token_counts: dict[str, int], latency_ms)` as a frozen / slotted dataclass. `ChatResponse.latency_ms` is the single source of truth for chat-call latency; no other layer measures it independently.
- `src/diary_rag/adapters/answers/{__init__,mock,factory}.py` (new) — `MockChatClient` returns a deterministic JSON `raw_text` that round-trips through `parse_structured_answer`; honest provenance (`model_name="mock"`, `latency_ms=0`). `build_chat_client(settings)` factory mirrors `build_embedding_client(settings)`.
- `src/diary_rag/core/diary/models.py` — `AnswerTrace(answer_trace_id, query_id, prompt_version, context_chunk_ids: tuple[str, ...], answer_text, fallback_mode, model_name, token_counts: dict[str, int], latency_ms, created_at)` added as a frozen / slotted dataclass. `AnswerResult` gains an optional `answer_text: str | None = None`; the Telegram reply layer keeps reading `evidence` in this packet (Slice 4.4 will switch to `answer_text` + citations). **No `confidence_band` field** — Slice 4.3 owns that contract decision.
- `src/diary_rag/core/diary/__init__.py` — re-exports `AnswerTrace`.
- `src/diary_rag/storage/repository.py` — `DiaryRepository` Protocol gains exactly two methods (`save_answer_trace`, `get_answer_trace_for_query`). All three backends implement the seam: mock keeps a process-local dict keyed by `query_id`; SQLite adds an `answer_traces` table to its in-file DDL with JSON-`TEXT` serialisation for `context_chunk_ids` and `token_counts`; Postgres uses `TEXT[]` and `JSONB`. `UNIQUE (query_id)` enforced on sqlite/postgres at the DB level. `MockDiaryStore` adds `len_answer_traces()` (mock-only test helper; not on the Protocol).
- `src/diary_rag/storage/postgres/schema.sql` — appends the `answer_traces` table with FK to `queries(query_id)`, `UNIQUE (query_id)`, `fallback_mode` CHECK matching `FallbackMode`, `latency_ms` CHECK `>= 0`, and `idx_answer_traces_query_id`.
- `src/diary_rag/services/query_service.py` — `__init__` takes `(repo, search_repo, embedding_client, chat_client, *, top_k, candidate_k)` (one new positional arg). `answer()` is updated:
  - Empty-query branch: persist `Query` + `AnswerTrace` with empty `context_chunk_ids`, empty `answer_text`, `latency_ms=0`, empty `token_counts`, and `fallback_mode=NO_EVIDENCE` (preserving the existing `Query.fallback` semantics from D-032 — **not a remap**). No LLM call.
  - No-evidence branch (merged empty after retrieval): same shape as empty-query.
  - Success branch: `build_answer_prompt(context)` → `chat_client.complete(prompt)` → `parse_structured_answer(response.raw_text, context=context)` → persist `AnswerTrace` carrying `prompt_version`, `context_chunk_ids`, the LLM-produced `answer_text`, `model_name=response.model_name`, `token_counts=dict(response.token_counts)`, and `latency_ms=response.latency_ms` (read directly from `ChatResponse`; no independent measurement). Returns `AnswerResult` with both the existing `evidence` list and the new `answer_text`.
  - If `parse_structured_answer` raises a typed `StructuredAnswerError` on the success branch, the exception propagates — no retry loop, no repair prompt, no malformed-JSON recovery workflow (deferred to Slice 4.3b).
- `src/diary_rag/adapters/telegram/webhook.py` — `get_dispatcher` builds the chat client via `build_chat_client(settings)` and passes it to `QueryService`. The boot log line includes `chat_backend=…` and `chat_model=…`.
- `src/diary_rag/config.py` — adds `chat_backend: Literal["mock"]` (the pre-existing `chat_model` placeholder is retained; the mock ignores it).
- `src/diary_rag/app.py` — `_verify_chat_contour(settings)` instantiates the chat client and asserts a non-empty `model_name`; failure aborts boot (R-10).
- `.env.example` — documents the new `CHAT_BACKEND` knob.
- New tests: `tests/test_chat_client_mock.py` (six mock-behavior cases including round-trip through `parse_structured_answer` and the no-evidence path); `tests/test_storage_answer_traces.py` (mock + sqlite + postgres parity for round-trip, missing-returns-none, duplicate-`query_id`, and the empty-context shape).
- Extended tests: `tests/test_query_service.py` gains three `AnswerTrace` persistence cases (success, no-evidence, empty-query) plus passes `MockChatClient` to `_wire`. `tests/test_end_to_end_smoke.py` adds the analogous `AnswerTrace` assertions on the existing success and no-evidence webhook flows. Existing `QueryService` constructor call sites in `tests/test_telegram_reply.py`, `tests/test_telegram_export.py`, `tests/test_telegram_drafts.py`, and `tests/test_dispatcher_drafts.py` pass `MockChatClient()` as the new positional argument (five call sites updated; `test_dispatcher_retrieval_fallback.py` uses a `_RaisingQueryService` stub and is unchanged).
- Docs: D-034 in `decision-log.md`. I-9 in `INVARIANTS.md` and R-5 in `RUNTIME-INVARIANTS.md` tightened in place to record that answer-side trace persistence is enforced on the success and no-evidence/empty-query contours. `RUNBOOK.md` gains an "Answer traces" subsection with one operator SQL one-liner and the standard A-34 destructive-upgrade note. `execution-map.md` row 4.3a added (4.3 split into 4.3a + 4.3b). No new I- or R- numbers, no new assumptions.
- New runtime dependencies: none.
- A-34 destructive-upgrade discipline applies to existing local Postgres volumes that pre-date the new `answer_traces` table.
- **Explicitly not in this packet:** real OpenAI / Anthropic chat adapter; weak-evidence / ambiguous / provider-unavailable / parse-failure grading (Slice 4.3b); `confidence_band` semantics; Telegram citation rendering or dispatcher rewiring (Slice 4.4); search-quality fork; `RouteKind.ENTRY → NOTE` / `DiaryEntry` / `family_id` / `diary_rag` package renames (D-026); schema migration tooling (A-34).

Closed in the Slice 4.2 answer prompt contract packet (D-033):
- `src/diary_rag/core/diary/answer_prompt.py` (new) — `PROMPT_VERSION: Final[str] = "v1"`; frozen / slotted `AnswerPrompt(prompt_version, system_text, user_text, cited_chunk_ids: tuple[str, ...])`; pure `build_answer_prompt(context: AnswerContext) -> AnswerPrompt`. Output is fully deterministic for a given input. R-8 enforced in code: `CrossFamilyContextError` raised when `ordered_chunks` span more than one `family_id`. Empty `ordered_chunks` renders an explicit "no diary chunks were retrieved" placeholder rather than raising — the caller (chat-client seam, future packet) decides whether to invoke the LLM.
- `src/diary_rag/core/diary/answer_schema.py` (new) — frozen / slotted `StructuredAnswer(answer_text: str, cited_chunk_ids: tuple[str, ...], uncertainty: UncertaintyMarker)`; `UncertaintyMarker = Literal["confident", "uncertain", "no_evidence"]`; strict `parse_structured_answer(raw, *, context: AnswerContext) -> StructuredAnswer`. Typed errors: `StructuredAnswerError` (base), `MalformedAnswerJSONError`, `AnswerSchemaMismatchError`, `FabricatedCitationError`. Parser enforces I-9 at the contract boundary: `cited_chunk_ids ⊆ AnswerContext.ordered_chunks`; empty `cited_chunk_ids` only with `uncertainty == "no_evidence"`. Stdlib `json` only; no Pydantic in core.
- `src/diary_rag/core/diary/__init__.py` — re-exports `PROMPT_VERSION`, `AnswerPrompt`, `build_answer_prompt`, `CrossFamilyContextError`, `StructuredAnswer`, `parse_structured_answer`, `UncertaintyMarker`, and the four error classes.
- New tests: `tests/test_answer_prompt.py` (determinism, version pinning, chunk_id propagation in user_text, R-8 cross-family raise, empty-context placeholder, system-text shape); `tests/test_answer_schema.py` (round-trip, fabricated-citation rejection, malformed JSON, non-object top-level, missing/extra/wrong-typed fields, unknown uncertainty marker, empty-citations rule on both sides).
- Docs: D-033 in `decision-log.md`. I-9 tightened in place in `INVARIANTS.md` to record the parser-enforced citation-subset rule. `execution-map.md` row 4.2 updated. No new I- numbers, no R- changes; `assumptions.md` not touched (the `UncertaintyMarker` literal set is a packet-level contract decision, recorded in D-033, not an open assumption).
- New runtime dependencies: none. Stdlib `json` covers the parser.
- **Explicitly not in this packet:** `ChatClient` Protocol; `MockChatClient` or OpenAI chat client; any LLM call; `AnswerTrace` schema or persistence; weak-evidence / ambiguous / provider-unavailable fallback grading; Telegram citation rendering or dispatcher rewiring; any change to `QueryService.answer`, retrieval, repository, or `storage/postgres/schema.sql`; search-quality fork; the `RouteKind.ENTRY → NOTE` / `DiaryEntry` / `family_id` / `diary_rag` package renames (D-026); schema migration tooling (A-34).

Closed in the Slice 4.1 context-assembler packet:
- `src/diary_rag/core/diary/models.py` — channel-neutral `AnswerContext` (`query_id`, `query_text`, `ordered_chunks: tuple[EventChunk, ...]`, `model_name`, `created_at`); minimal canonical payload, no presentation shapes baked into the domain model. `AnswerResult` gains an optional `context: AnswerContext | None = None` field so the upcoming chat-client / answer-trace packets consume the assembled view without another refactor.
- `src/diary_rag/core/diary/__init__.py` — exports `AnswerContext`.
- `src/diary_rag/services/context_assembler.py` (new) — pure `assemble_answer_context(query, merged)` mapping. Order is preserved from RRF (chunk-id dedup and tie-break already applied upstream); family scoping stays upstream (R-3); the assembler does no re-scoping and no re-dedup.
- `src/diary_rag/services/query_service.py` — `QueryService.answer` builds the `Query` once per call and invokes `assemble_answer_context` after RRF; attaches the resulting `AnswerContext` to `AnswerResult.context` in the success, no-evidence, and empty-query branches. The `NotImplementedError` fallback in `Dispatcher` continues to construct an `AnswerResult` with `context=None` (no retrieval call ran). `retrieval.hybrid` log line and persisted `Query` / `RetrievalHit` rows unchanged.
- New tests: `tests/test_context_assembler.py` covers query-identity propagation, empty-input shape, RRF order preservation, immutable-tuple type, and the fallback-agnostic mapping.
- Extended tests: `tests/test_query_service.py` adds three context-attachment assertions (success / no-evidence / empty-query). Other test files unchanged.
- Docs: `execution-map.md` row 4.1 updated to reflect the landed seam; `todo.md` opens 4.2 (chat client + AnswerTrace) as the next packet. No new invariants — R-5 / I-9 wording is unchanged (answer-side `AnswerTrace` still deferred).
- **Explicitly not in this packet:** `ChatClient` Protocol; mock / OpenAI chat clients; `AnswerTrace` dataclass / repo seam / DB tables; versioned answer prompt; weak-evidence / ambiguous / provider-unavailable fallback grading; Telegram citation rendering or reply-layout changes; schema migrations; new boot gates; renames deferred under D-026; retrieval-quality changes.

Closed in the Slice 3.5 retrieval-trace persistence packet (D-032):
- `src/diary_rag/core/diary/models.py` — `RetrievalLeg` (`StrEnum`), `Query`, `RetrievalHit` channel-neutral dataclasses; exported from `core/diary/__init__.py`.
- `src/diary_rag/services/retrieval.py` — `reciprocal_rank_fusion` now returns `list[FusedHit]` so the fused RRF score persists on merged rows; tie-breaking and `DEFAULT_RRF_K = 60` unchanged.
- `src/diary_rag/services/query_service.py` — `QueryService.__init__` takes `(repo, search_repo, embedding_client, *, top_k, candidate_k)`; every `answer()` call writes one `Query` row plus per-leg + merged `RetrievalHit` rows (per-leg score = RRF contribution `1.0 / (RRF_K + rank)`, merged score = fused RRF score). Empty-query and empty-merged paths still persist the `Query` row with zero hits. The `retrieval.hybrid` log line gains `query_id=…` and `fallback=…`.
- `src/diary_rag/storage/repository.py` — `DiaryRepository` Protocol gains `save_query`, `save_retrieval_hits`, `get_query`, `get_retrieval_hits_for_query` (the latter ordered by `(leg ASC, rank ASC)` for stable inspection).
- All three backends implement the new seam fully: `MockDiaryStore` (process-local dicts), `SqliteDiaryStore` (new `queries` + `retrieval_hits` tables in the in-file DDL), `PostgresDiaryStore` (new tables in `schema.sql` with `UNIQUE (query_id, chunk_id, leg)`, `idx_queries_family_id`, `idx_retrieval_hits_query_id`, FK to `event_chunks`). SQLite's retrieval-side `dense_candidates` / `sparse_candidates` continue to raise `NotImplementedError` (D-025 unchanged).
- `src/diary_rag/adapters/telegram/webhook.py` passes the store object twice into `QueryService` (it satisfies both `DiaryRepository` and `SearchRepository` structurally via `HybridDiaryStore`).
- New tests: `tests/test_storage_query_traces.py` covers mock + sqlite + postgres parity for save/get of queries and hits, ordering, no-evidence-with-zero-hits, and the `UNIQUE (query_id, chunk_id, leg)` constraint on sqlite/postgres.
- Extended tests: `tests/test_query_service.py` adds three persistence cases (successful-with-hits, no-evidence-with-zero-hits, empty-query-with-zero-hits); `tests/test_retrieval_rrf.py` updated to the new `FusedHit` shape plus a score-monotonicity assertion; `tests/test_end_to_end_smoke.py` extended with the two persistence assertions on the existing success and no-evidence cases; `tests/test_dispatcher_drafts.py`, `tests/test_telegram_export.py`, `tests/test_telegram_reply.py`, `tests/test_telegram_drafts.py`, `tests/test_end_to_end_smoke.py` updated for the new three-arg `QueryService` constructor.
- Docs: D-032 in `decision-log.md`. I-9 in `INVARIANTS.md` and R-5 in `RUNTIME-INVARIANTS.md` tightened in place to record that retrieval-side trace persistence is enforced; answer-side `AnswerTrace` still pending Phase 4. `RUNBOOK.md` gains a "Retrieval traces" subsection with two operator SQL one-liners (recent traces; no-evidence only) and the standard A-34 destructive-upgrade note. `execution-map.md` row 3.5 updated. `todo.md` opens Phase 4 (grounded answer pipeline) as the next slice.
- A-34 destructive-upgrade discipline applies to existing local Postgres volumes that pre-date the two new tables; reset with `docker compose down -v` or run the new `CREATE TABLE` / `CREATE INDEX` statements manually.
- **Explicitly not in this packet:** `AnswerTrace` persistence (Phase 4); metadata filtering / Slice 3.4; retrieval-quality changes; BM25 / reranker / Qdrant / halfvec / HNSW; user-facing `/trace` command; schema migration tooling; Telegram adapter wording; renames deferred under D-026; broader TechSpec §5 alignment for the deferred `Query.source_message_id` / `Query.author_user_id` / `RetrievalHit.score_dense|sparse|hybrid` fields.

Closed in the Slice 3.3 baseline hybrid retrieval packet (D-025):
- `src/diary_rag/storage/search_repository.py` — `SearchRepository` Protocol (`dense_candidates`, `sparse_candidates`) + `HybridDiaryStore` (combined ingest + retrieval).
- `src/diary_rag/services/retrieval.py` — pure service-layer Reciprocal Rank Fusion (RRF, k=60); no calibration, no reranker.
- Postgres dense leg: exact family-scoped scan over `vector(3072)` with `embedding <=> %s::vector`, `embedding_status='ready'` + `model_name` filter. No HNSW; A-36 replaced by A-36b (halfvec/HNSW deferred to next quality-decision packet).
- Postgres sparse leg: generated stored column `event_chunks.chunk_text_tsv tsvector GENERATED ALWAYS AS (to_tsvector('simple', chunk_text)) STORED` + GIN index; `websearch_to_tsquery('simple', $q)` ranked by `ts_rank_cd`. A-37 (sparse dictionary `simple`) opened.
- Mock backend: deterministic dense (cosine + 0.5 distance threshold so unrelated queries don't fabricate matches) and sparse (whitespace token overlap). `MockEmbeddingClient` provenance unchanged.
- SQLite backend: `dense_candidates` / `sparse_candidates` raise `NotImplementedError`; dispatcher catches and returns `FallbackMode.NO_EVIDENCE` with a `retrieval.unavailable` log line. SQLite remains opt-in for ingest.
- `DiaryRepository.search_chunks` removed; `DiaryRepository.get_event_chunk(chunk_id)` added as the small chunk-by-id read primitive.
- `QueryService` rewritten: constructor takes `SearchRepository` + `EmbeddingClient`; embeds query once; calls both legs; RRF-merges; logs `retrieval.hybrid family_id=… model=… dense_n=… sparse_n=… merged_n=…`. Two new `Settings` knobs: `retrieval_top_k` (5), `retrieval_candidate_k` (20).
- Dispatcher: reply trailer changes to `(hybrid retrieval — dense+sparse RRF)`; the no-evidence reply drops the substring parenthetical.
- Docs: D-025 in `decision-log.md`. A-6, A-29, A-36 closed (A-36 replaced by A-36b). A-37 opened. I-8 updated in place to record that hybrid retrieval is now enforced rather than declared. R-4 updated to record the `ready`-only filter on dense.
- New tests: `tests/test_retrieval_rrf.py`, `tests/test_search_repository_mock.py`, `tests/test_search_repository_postgres.py`, `tests/test_dispatcher_retrieval_fallback.py`. Existing tests adjusted: `test_query_service.py` rewritten around hybrid; substring assertions removed from `test_postgres_store.py` / `test_sqlite_store.py`; `test_end_to_end_smoke.py` trailer updated; `test_indexing_pipeline.py` uses `get_event_chunk` instead of substring.
- **Explicitly not in this packet:** BM25, reranker / cross-encoder, Qdrant or any external vector/search system, halfvec / HNSW migration, metadata filters (3.4), retrieval traces (3.5), multilingual sparse tuning, query-embedding caching, migration tool (A-34 unchanged).

Closed in the Phase 3.1+3.2 embedding-adapter + sync-indexing packet (D-024):
- `core/embeddings/{client,models}.py` — `EmbeddingClient` Protocol, `EmbeddingRecord`, `EmbeddingStatus`.
- `adapters/embeddings/{mock,openai_client,factory}.py` — `MockEmbeddingClient` (honest `model_name="mock"`, dimension=3072), `OpenAIEmbeddingClient` (`text-embedding-3-large`, passes `dimensions=3072` explicitly), and the single `build_embedding_client(settings)` factory used by both the boot gate and the dispatcher.
- `DiaryRepository` Protocol gains `save_embedding_records`, `count_embedding_records_for_source`, `set_chunk_embedding_status`; all three backends (mock / sqlite / postgres) implement them.
- Postgres schema: `CREATE EXTENSION vector`, `embedding_records` with `vector(3072)`, `event_chunks.embedding_status TEXT NOT NULL DEFAULT 'pending'` with CHECK on the StrEnum. No ANN index — pgvector's HNSW/IVFFlat cap at 2000 dim (A-36 deferred to Slice 3.3).
- SQLite: `embedding_records` with little-endian f32 `BLOB`; same `embedding_status` column and CHECK.
- `DiaryService.ingest` calls the embedding client after `save_event_chunks` commits; success → `EmbeddingRecord` rows + `embedding_status='ready'`; failure → `embedding_status='failed'` + zero records + `FallbackMode.NONE` (I-2, I-3); replay short-circuits before the embedding step (R-2 extension).
- `create_app` boot gate (R-10 partial): refuses to start when `EMBEDDING_DIMENSION ≠ 3072`, when `EMBEDDING_BACKEND=openai` with the wrong model or no API key, or when `STORAGE_BACKEND=postgres` and the connected database lacks the `vector` extension.
- `docker-compose.yml`: `postgres:16-alpine` → `pgvector/pgvector:pg16`. Destructive volume reset required (A-34).
- `pyproject.toml`: `openai` and `pgvector` runtime deps; mypy `ignore_missing_imports` for `pgvector.*`.
- `.env.example`: new `EMBEDDING_BACKEND`, default `EMBEDDING_MODEL=text-embedding-3-large`, `EMBEDDING_DIMENSION=3072`.
- New tests: `test_embedding_client_mock.py`, `test_indexing_pipeline.py` (parametrised across mock / sqlite / postgres backends), `test_boot_dimension_gate.py`. Optional/manual: `test_embedding_client_openai.py` (gated by `DIARY_RAG_OPENAI_TEST_KEY`; **not** in the standard packet gate). Extended `test_diary_service.py` for fresh-ingest / replay / failure paths.
- Docs: D-024 in `decision-log.md`. Closed A-5 / A-7 / A-8; opened A-35 (sync indexing, no auto-retry) and A-36 (3072-dim ANN strategy is open). R-10 wording tightened in `RUNTIME-INVARIANTS.md`. RUNBOOK, QUICKSTART, README, execution-map updated.

Closed in the webhook idempotency packet (D-023, slice 2.4):
- `SourceMessage` and `InboundMessage` now carry `external_message_id` and `edit_seq`; the idempotency key is `(external_chat_id, external_message_id, edit_seq)`.
- `DiaryRepository.get_or_create_source_message` returns `(SourceMessage, bool)` where the bool indicates replay/existing-row; mock, sqlite, and postgres backends all enforce uniqueness via DB-native conflict handling (`INSERT ... ON CONFLICT DO NOTHING` / `INSERT OR IGNORE` / dict-keyed dedupe).
- `DiaryRepository` gains `get_diary_entry_by_source_message_id` and `count_event_chunks_for_source` so `DiaryService.ingest` can reconstruct the original `IngestResult` on replay without re-parsing or re-chunking.
- `IngestResult.replayed: bool` flag propagates through `Dispatcher` metadata; the Telegram webhook log line now includes `edit_seq=…` and `effective_path=fresh|replay`.
- `TelegramMessage` accepts an optional `edit_date`; the webhook derives `edit_seq = edit_date if present else 0`.
- TechSpec §5 reconciled: `telegram_chat_id` / `telegram_user_id` → `external_chat_id` / `external_user_id`, plus `external_message_id` and `edit_seq`.
- Closed A-30 (mock non-idempotent state). Updated A-33 (Postgres contour). Opened A-34 (destructive local schema upgrades — no migration tool yet).
- New tests across all backends and the E2E webhook layer assert: replay short-circuits with no duplicate rows, edited state coexists as a distinct row, replay log line carries `effective_path=replay`.

Closed in Slice 0.3: A-1 → D-016, A-2 → D-017, A-3 → D-018, A-4 → D-019.

Closed in Slice 1.1:
- `pyproject.toml` for Python 3.11, `uv`-managed venv, Ruff + Mypy + Pytest wired.
- `Makefile` real targets: `format`, `lint`, `typecheck`, `test`, `check`, `run`.
- `src/diary_rag` package skeleton (`config`, `logging`, `app`, `__main__`) plus placeholder packages for `adapters/telegram`, `core/routing`, `services`, `storage/mock`.
- FastAPI `/health` endpoint smokeable via `make run`.
- `make check` is green; `/health` returns 200.
- `.python-version` pins 3.11.

Closed in Slice 1.2:
- `POST /telegram/webhook` mounted on the FastAPI app (D-019).
- `X-Telegram-Bot-Api-Secret-Token` validation, fail-closed when secret is unset or mismatched (A-26).
- Telegram update Pydantic schema (`adapters/telegram/models.py`).
- Command parser for `/start`, `/help`, `/entry`, `/ask` with `@BotName` suffix stripping.
- Channel-neutral routing types in `core/routing` (`RouteKind`, `InboundMessage`, `DispatchResult`).
- `Dispatcher` (`services/dispatcher.py`) with stub handlers per route.
- `sendMessage`-shaped JSON returned in the webhook response body — no outbound HTTP.
- Tests: secret gating, command parsing, dispatch wiring, reply payload, update schema.
- New open assumption: A-26.

Closed in the heuristic-routing packet:
- `core/routing/classifier.py` with deterministic ENTRY/ASK/CLARIFY rules; reuses `parse_diary_entry` for ISO-date detection (A-28).
- `RouteKind.CLARIFY` added; `InboundMessage.route_source` is now required (`"command"` | `"heuristic"`).
- Webhook calls the classifier when `parse_command` returns UNKNOWN with non-empty text; logs `route` + `route_source` + `confidence`.
- Dispatcher gains a CLARIFY handler with a fixed reply naming `/entry` and `/ask`; heuristic-routed ENTRY/ASK replies carry an explicit marker (R-6).
- `QueryService` strips trailing `?.!,;:` from the query payload before substring search — minimum normalization for plain-text questions to match.
- E2E smoke (`tests/test_end_to_end_smoke.py`) covers heuristic ENTRY, heuristic ASK, and CLARIFY (latter asserts no persistence).
- New decision: D-020 (heuristic routing rules + CLARIFY UX), closing A-16 and A-17. New open assumption: A-31 (mock-only per-route persistence).

Closed in the canonical local Postgres backend packet:
- `src/diary_rag/storage/postgres/{__init__,store}.py` — `PostgresDiaryStore` implementing `DiaryRepository` via psycopg3 sync + `psycopg_pool.ConnectionPool`; deterministic `close()` for tests/local use.
- `src/diary_rag/storage/postgres/schema.sql` — single canonical DDL loaded via `importlib.resources`; CREATE TABLE / CREATE INDEX IF NOT EXISTS; `detected_route` CHECK covers all `RouteKind` values.
- `docker-compose.yml` — single `postgres:16-alpine` service with `${VAR:-default}` env, named volume `diary_pg_data`, `pg_isready` healthcheck.
- `tests/test_postgres_store.py` — gated by `DIARY_RAG_PG_TEST_DSN`; mirrors SQLite cases (round-trip, family scoping, top-k, case-insensitive, empty inputs, R-3, restart survival).
- `config.Settings.postgres_dsn()` helper; `storage_backend` Literal extended to include `"postgres"`; `_build_store` adds a postgres branch with lazy import.
- `pyproject.toml`: `psycopg[binary]` and `psycopg-pool` runtime deps; hatch force-include for `schema.sql`.
- Docs: D-022 in `decision-log.md`; A-32 closed and A-33 opened in `assumptions.md`; row 2.0 added to `execution-map.md`; Postgres section in `QUICKSTART.md`; pointer in `RUNBOOK.md`; comment in `.env.example`.

Closed in the mock diary/query contour packet:
- Channel-neutral domain dataclasses `SourceMessage`, `DiaryEntry`, `EventChunk`, plus `Evidence`, `IngestResult`, `AnswerResult`, `FallbackMode` in `core/diary/models.py`.
- Strict ISO-only date parser in `core/diary/parser.py`.
- `MockDiaryStore` (`storage/mock/store.py`) holds sources, entries, chunks; deterministic case-insensitive substring search scoped to `family_id`.
- `DiaryService` records the raw `SourceMessage` before parsing (I-3, R-1) and falls back to `INVALID_INPUT` on a non-ISO first line; `QueryService` returns `NO_EVIDENCE` rather than fabricating answers (I-9, R-5/R-6) and rejects calls without `family_id` (R-3).
- `Dispatcher` wires `ENTRY` → `DiaryService.ingest`, `ASK` → `QueryService.answer`, with channel-neutral reply formatting.
- Webhook smoke: `/entry 2026-05-09\n…` then `/ask <substring>` returns a grounded-style mock reply listing the matched line with its date.
- New open assumptions: A-28 (mock ISO-only date parsing), A-29 (substring-match retrieval), A-30 (process-local non-idempotent mock state).
- `AnswerTrace` persistence is deliberately deferred to Phase 4.

## Real-answer end-to-end value-loop proof (REAL-1) — complete
- Milestone scope: under the binding owner rule, the product baseline is incomplete until at least one real-backend `/note` → retrieval → grounded-answer round-trip has been recorded as a committed evidence artifact. REAL-1 exercises the already-wired OpenAI paths (D-024 embeddings + D-037 chat client + boot gate) under the existing OP-2 / OP-4 / OP-5 contours and lands a committed evidence artifact analogous to DEPLOY-1.7a (D-067). Sequenced separately from the Phase/Stage axis, mirroring the "Deployment-shape rollout" framing in `docs/execution-map.md`. Reuse-only: no new resilience knob, no new aggregate, no new harness code, no behavioral change. REAL-1 is closed by the populated dated evidence artifact.
- **REAL-1.0 — operator procedure prep: done (D-073).** Map: execution-map "Real-answer e2e proof rollout". Landed the RUNBOOK procedure, evidence-file template, and cross-doc registration for the bounded real-answer smoke.
- **REAL-1.1 — operator live run + populated dated evidence: done (D-074).** `docs/real-answer-drill/real-answer-smoke-20260528-evidence.json` records a real Postgres + real OpenAI `/note` → retrieval → grounded-answer round-trip. The artifact confirms `summary.note_round_trip_green == true`, `summary.ask_round_trip_green == true`, `summary.answer_grounded == true`, and `summary.closes_real_1 == true`. Closes REAL-1.

## Author display-name contract (D-081)
- Milestone scope: pin the opaque-ID → display-name seam (D-081) before any capture/render code. Author display resolved **only at the Telegram adapter seam** (`username → first_name → opaque short-ID`, host-supplied / non-authoritative); the core carries only the opaque `author_user_id` (I-1 / I-6 unchanged; A-44). Stays bounded: no `src/` / `tests/` / schema / capture / storage / rendering work; no group-use / multi-diary widening; A-15 visibility semantics untouched.
- **Packet 1 — display-name contract + initial `/sources` surface: done (D-081).** Map: execution-map "Author display-name contract (D-081)". Docs-only: `decision-log.md` (D-081); `assumptions.md` (A-44) + `assumption-audit.md`; `GLOSSARY.md`; `ARCHITECTURE.md` (Axis 5 + adapter bullets); `product/TechSpec.md` (§5); `RUNBOOK.md` (`/sources` forward note); `INVARIANTS.md` (I-6 cross-ref — no new invariant). `/sources` is the sole milestone display surface; answer-reply attribution deferred. See `docs/decision-log.md` D-081.
- **Author display-input persistence shape + capture contract: done (D-082).** Docs-only decision (Option 3 hybrid): minimal snapshot-oriented persistence shape + capture contract — adapter/storage-owned `username` / `first_name` snapshot, nullable, non-authoritative, for later adapter-side display resolution only; never a core field; adapter-side identity directory/projection deferred to a later group-use milestone. `decision-log.md` (D-082); `assumptions.md` + `assumption-audit.md` (A-44 narrowed, stays open); `GLOSSARY.md`; `ARCHITECTURE.md`; `product/TechSpec.md` (§5); `INVARIANTS.md` (I-6 cross-ref — no new invariant); execution-map; this list. No `src/` / `tests/` / schema / migration; code/migration is a later packet. See `docs/decision-log.md` D-082. Map: execution-map "Author display-name contract (D-081)".
- **Adapter-owned storage port + side-table seam: done (D-083).** Docs-only decision of record (Option A): the display-input snapshot lands in a separate Telegram-adapter-owned side table written through an adapter-owned storage port distinct from the core `DomainRepository`, keyed by the message idempotency tuple `external_chat_id + external_message_id + edit_seq` as opaque scalars; the snapshot never enters `InboundMessage` / `SourceMessage` / any core type or core function signature (preserves the D-026 / D-041 boundary and the open-source shared-core rule — host-identity capture stays adapter-owned). Snapshot stays nullable / non-authoritative / host-supplied / resolution-only. Migration surface named descriptively only (next file in `storage/postgres/migrations/`). `decision-log.md` (D-083); `assumptions.md` + `assumption-audit.md` (A-44 narrowed, stays open); `GLOSSARY.md`; `ARCHITECTURE.md` (Axis 5 + adapter bullets); `product/TechSpec.md` (§5); `INVARIANTS.md` (I-6 cross-ref — no new invariant); execution-map; this list. No `src/` / `tests/` / schema / DDL / migration / config change; code/migration is the next packet. See `docs/decision-log.md` D-083. Map: execution-map "Author display-name contract (D-081)".
- **Author display-input capture + durable landing: done (D-084).** Code packet implementing the D-083 seam: `TelegramUser` carries nullable `username` / `first_name`; the webhook snapshots them on source-message-bearing routes (note/draft) straight from `message.from_` (never a core type), best-effort. Adapter-owned `AuthorDisplayInputStore` port co-located on the per-backend store (distinct from `DomainRepository`); new `author_display_inputs` side table with mock / sqlite / postgres parity + forward migration `0004`, keyed by the message idempotency tuple as opaque scalars; R-2 idempotency (re-delivery no-op, edits new row); both-null recorded. `decision-log.md` (D-084); `assumptions.md` + `assumption-audit.md` (A-44 → capture/landing implemented, stays open); execution-map; this list. See `docs/decision-log.md` D-084. Map: same.
- **Author display-name resolution + `/sources` rendering: done (D-086).** Adapter-side resolution helper (`@username → first_name → opaque `user-<last8>` floor`) over the D-084 snapshot, bridging the opaque boundary via `get_source_message` to recover the keying tuple; `/sources` renders a `— <author>` line per block, resolution staying adapter-only (mirrors the `/drafts` return-domain-objects pattern — `DispatchResult.source_blocks` → `source_chunks`, dispatcher `_render_source_block` removed, new `get_backend_store` dependency). `src/memory_rag/adapters/telegram/{author_display.py,webhook.py}`, `core/routing/models.py`, `services/dispatcher.py`; `tests/test_author_display_resolution.py` (new) + `test_{telegram_sources,dispatcher_sources,author_display_boundary}.py`; `decision-log.md` (D-086), `RUNBOOK.md`, `assumptions.md` + `assumption-audit.md` (**A-44 resolved**), execution-map, this list. No core repo/type signature, schema, or migration change. **Closes A-44**; answer-reply (`/ask` reply) attribution remains a separate deferred placeholder. See `docs/decision-log.md` D-086. Map: same.
- **Answer-reply (`/ask` reply) contributor attribution: now active — see "Answer-reply contributor attribution (D-091)" below.** The D-081-named deferred placeholder is taken up by the `feat/ask-reply-contributor-attribution` milestone; its contract is pinned by D-091. Map: execution-map "Author display-name contract (D-081)".
