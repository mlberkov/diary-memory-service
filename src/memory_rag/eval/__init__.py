"""Evaluation tooling.

Sibling of ``adapters``, ``services``, and ``storage``: inspection-only
code that reads from existing seams (``SearchRepository``,
``DomainService``) and produces measurements. Nothing in this namespace
is on the request path.
"""
