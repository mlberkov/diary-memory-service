# env
.env
.env.*
!.env.example

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# build / temp
dist/
build/
*.egg-info/
tmp/
temp/
coverage/
htmlcov/

# logs
*.log

# local artifacts
.workspaces/
.artifacts/

# Local runtime SQLite artifacts
data/*.db
data/*.db-shm
data/*.db-wal

# Operator-produced retrieval-harness query-embedding caches (D-038, D-056) —
# pinned to a point-in-time OpenAI output, regenerated by an operator ritual.
eval/retrieval/embeddings_cache.json
eval/retrieval/*/embeddings_cache.json

# DEPLOY-1.4 / D-063 — installer-owned per-host state file (the
# configuration-versioning seam). Written by scripts/installer/deploy.sh
# alongside its sibling failure marker; never committed.
.installer-state.json
.installer-state.last_failure.json
