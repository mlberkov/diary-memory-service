# Execution Map — Phases → Files → Slices

Files listed are *targets*. Most do not exist yet. Each row will be split into one or more entries in `docs/todo.md` as the work approaches. Phase headers mirror `docs/product/BuildPlan.md`.

Use-case nouns below (`family`, `child`) describe the first implemented use case; `community` / `subject` are the canonical core vocabulary — see `docs/GLOSSARY.md` and D-041.

Each phase carries its development stage (D-043; see `docs/product/BuildPlan.md`
"Development Sequencing"). Execution order follows the stage map, **not** the
phase numbers: Stage 1 = Phases 0–4, Stage 2 = Phases 6–7 + Phase 8 raw-data
durability + A-34, Stage 3 = Phase 5 + Phase 8 access/visibility/audit/retention
+ Phase 9. No Stage-3 slice starts until the Stage-2 exit criteria are met. The
Stage-2 packet groups (`OP-1`..`OP-5`) and their execution order are decomposed
in `docs/OPERATIONALIZATION-ROADMAP.md` (D-044).

## Phase 0 — Operating Setup *(Stage 1 — Product baseline)*
| Slice | Files / artifacts |
| --- | --- |
| 0.1 canonical docs | `docs/product/PRD.md`, `docs/product/BuildPlan.md`, `docs/product/TechSpec.md`, `docs/decision-log.md`, `AGENTS.md`, `CLAUDE.md` |
| 0.2 supporting docs | `README.md`, `QUICKSTART.md`, `docs/ARCHITECTURE.md`, `docs/INVARIANTS.md`, `docs/RUNTIME-INVARIANTS.md`, `docs/CHECKLIST.md`, `docs/RUNBOOK.md`, `docs/execution-map.md`, `docs/assumptions.md`, `docs/assumption-audit.md`, `docs/todo.md` |
| 0.3 scaffold review | `Makefile`, `.env.example`, `.gitignore` |

## Phase 1 — Telegram Shell and Mock Flow *(Stage 1 — Product baseline)*
| Slice | Files / artifacts |
| --- | --- |
| 1.1 language & toolchain | `pyproject.toml` (Py 3.11, D-016), `.python-version`, `uv` lockfile (D-017), Ruff + Mypy + Pytest configs (D-018); `Makefile` `format`/`lint`/`typecheck`/`test`/`check`/`run`; `src/memory_rag/{config,logging,app,__main__}.py`; placeholders `adapters/telegram/`, `core/routing/`, `services/`, `storage/mock/`; FastAPI `/health` smoke; `tests/test_smoke.py`, `test_config.py`, `test_app_health.py` |
| 1.2 Telegram adapter shell | `POST /telegram/webhook` (D-019) with secret-token gating; `adapters/telegram/{webhook,models,commands,reply}.py`; channel-neutral `core/routing/models.py` (`RouteKind`, `InboundMessage`, `DispatchResult`); `services/dispatcher.py` with stub handlers; tests `test_telegram_{commands,models,reply,webhook_secret,dispatch}.py`; dev-tunnel docs in `QUICKSTART.md` |
| 1.3 mock services | `core/domain/{models,parser}.py` (channel-neutral dataclasses + ISO date parser); `storage/mock/store.py` (`MockDomainStore` with deterministic substring search); `services/{domain_service,query_service}.py`; `Dispatcher` wires `NOTE`/`ASK` to those services. `MockEmbeddingClient` / `MockChatClient` deferred until 3.1/4.1 sketch the real shapes |
| 1.4 routing | `core/routing/classifier.py` (deterministic NOTE/ASK/CLARIFY) reusing `core/domain/parser.parse_note`; `RouteKind.CLARIFY`, `RouteSource`, required `InboundMessage.route_source`; webhook calls classifier on UNKNOWN+text; `services/dispatcher.py` adds CLARIFY handler and heuristic markers; `services/query_service.py` adds terminal-punctuation strip; `tests/test_routing_classifier.py`; new heuristic + command-wins-over-heuristic cases in `test_telegram_dispatch.py`; CLARIFY/marker assertions in `test_telegram_reply.py`. Closes A-16/A-17 via D-020. *(Heuristic plain-text NOTE/ASK auto-routing retired by D-078 and enforced in code by D-079 — command-less plain text routes only to the draft floor; the dispatcher's heuristic markers + NOTE normalize-gate were removed with it; see the "Stage-1 capture/routing baseline correction" section below.)* |
| 1.5 mock end-to-end | webhook smoke: `/note` then `/ask` returns grounded-style reply with date and matched line; heuristic NOTE/ASK/CLARIFY flows added to `tests/test_end_to_end_smoke.py` and the curl recipe in `QUICKSTART.md`. |

## Phase 2 — Durable Backend Core *(Stage 1 — Product baseline)*
| Slice | Files / artifacts |
| --- | --- |
| 2.0 canonical local Postgres backend | `src/memory_rag/storage/postgres/{__init__,store}.py`, `src/memory_rag/storage/postgres/migrations/` (versioned schema; `schema.sql` retired by OP-1.1 / D-045), `docker-compose.yml`, `tests/test_postgres_store.py`; `_build_store` postgres branch in `adapters/telegram/webhook.py`; `Settings.postgres_dsn()` (D-022) |
| 2.1 schema (identities) | initial migration: `users`, `families`, `children`, `telegram_chats`, `source_messages` |
| 2.2 schema (notes) | migration: `notes`, `event_chunks` with lineage |
| 2.3 ingestion pipeline | parser (`parse_version`), event splitter, chunk creation; raw persisted first (I-3, R-1) |
| 2.4 idempotent webhook | `DomainRepository.get_or_create_source_message` keyed on `(external_chat_id, external_message_id, edit_seq)`; `UNIQUE` constraint + `INSERT ... ON CONFLICT DO NOTHING` (Postgres) / `INSERT OR IGNORE` (SQLite) / dict dedupe (mock); `DomainService.ingest` short-circuits on replay; webhook logs `effective_path=fresh|replay`; D-023 |
| 2.5 edit/delete strategy | **Closed (D-118 / ED-n).** Supersession (edits create revisions) + tombstone (deletes are soft, hard delete is explicit/audited) + re-embed on revision; state model `active | superseded | tombstoned`; resolves TechSpec §12, **closes A-10**. Decomposed docs-first in `docs/EDIT-DELETE-ROADMAP.md` (ED-0..ED-n) — see the block below. ED-1 landed the `lifecycle_state` column + `supersedes_*` lineage + the active-state predicate + the R-4 wording; ED-2 wired `/edit` supersession; ED-3 wired the delete half — reply-targeted `/delete` + NOTE→DRAFT edit-removal tombstone the active revision, plus an operator-only audited hard-delete; ED-n closed the milestone with the populated dated evidence artifact `docs/edit-delete-drill/edit-delete-smoke-20260615-evidence.json` and PG acceptance legs (`73 passed`). |
| 2.6 stage status tracking | per-record `parse_status`, `embedding_status`, `index_status` |

Slice 2.5 is decomposed docs-first in `docs/EDIT-DELETE-ROADMAP.md` (D-114),
mirroring the OP-/RC-/G-/H- roadmap precedent: the decision entry carries the
stable contract, the roadmap carries the refinable ED-0..ED-n sequence. ED-0 is
the docs-only contract packet; ED-1 lands the state model + schema + retrieval
predicate (and the R-4 wording generalization); ED-2 the `/edit` supersession +
re-embed; ED-3 the `/delete` control surface; ED-n the drill evidence + close.

| Packet | Files / artifacts |
| --- | --- |
| ED-0 — contract + decomposition | **Done (D-114).** Docs-only ratification: `docs/decision-log.md` (new D-114 — supersession + tombstone + re-embed contract, `active | superseded | tombstoned` state model, A-10 closed, R-4 generalization named but deferred to ED-1); new `docs/EDIT-DELETE-ROADMAP.md` (ED-0..ED-n ladder); `docs/product/TechSpec.md` §12 (rewritten to the contract); `docs/assumptions.md` + `docs/assumption-audit.md` (**A-10 closed → D-114**); `docs/INVARIANTS.md` (I-13 cross-reference reconciliation only — no new I-number, no enforcement rewrite); this block + the 2.5 row; `docs/todo.md`. `docs/RUNTIME-INVARIANTS.md` untouched (the R-4 wording generalization lands when the predicate ships in ED-1). No `src/` / `tests/` / schema / migration / config change; no new I-/R- number. → ED-0 (D-114). |
| ED-1 — state model + schema + retrieval predicate | **Done (D-115), pre-checkpoint.** Single `lifecycle_state` column (`active | superseded | tombstoned`, CHECK + DEFAULT `'active'`) + nullable `supersedes_note_id` / `supersedes_chunk_id` lineage columns on `notes` / `event_chunks` (additive migration `0010.note-chunk-lifecycle-state`, `LifecycleState` enum); both retrieval legs generalized to filter `lifecycle_state='active'` (sparse gains its first active-state filter); R-4 wording generalized in `docs/RUNTIME-INVARIANTS.md`; backend parity (mock + Postgres enforce the predicate; SQLite round-trips the columns, retrieval still `NotImplementedError`). New `tests/test_lifecycle_state_retrieval.py`; `tests/test_postgres_migrations.py` extended (0010 discovery / packaging / upgrade-preserves-data, `MIGRATION_COUNT` 9 → 10). No state-transition writer; no `lifecycle_state` index; no new I-/R- number. → ED-1 (D-115). |
| ED-2 — `/edit` supersession + re-embed | **Done (D-116), pre-checkpoint.** A parsed note edit whose external message already has an `active` note creates a new `active` revision with `supersedes_note_id` / `supersedes_chunk_id` lineage; the prior note + chunk flip to `superseded` (retained, I-6 authorship intact); the new chunk re-embeds via the existing synchronous `ingest` path; the superseded revision is excluded from both retrieval legs immediately (R-4) and never re-embedded. Four `DomainRepository` seams (`get_active_note_for_external_message`, `get_active_chunk_for_note`, `mark_note_superseded`, `mark_chunk_superseded`) across mock / sqlite / postgres; the `ingest()` lookup→lineage→save→flip(chunk-then-note)→re-embed block. Trigger is "prior active note exists" (replay-safe), NOTE→NOTE only; a malformed or draft edit supersedes nothing; supersession holds even if re-embed fails. New `tests/test_edit_supersession_ingest.py`. No new schema; no new I-/R- number. → ED-2 (D-116). |
| ED-3 — `/delete` control surface | **Done (D-117), pre-checkpoint.** Reply-targeted `/delete` (the user replies to the `/note`; the opaque `reply_to_external_message_id` resolves it) and the NOTE→DRAFT edit-removal both tombstone the active note + chunk (chunk-then-note, retained with I-6 authorship; excluded from retrieval immediately, R-4); every miss is a friendly fail-closed no-op; idempotent. Operator-only `hard_delete_source_message` physically removes the raw source + derived notes/chunks/embeddings/referencing retrieval-hits in FK-safe order, community-scoped, audited via a structured `audit.hard_delete` log record (no audit table, no migration). Three `DomainRepository` seams (`mark_note_tombstoned`, `mark_chunk_tombstoned`, `hard_delete_source_message`) across mock / sqlite / postgres; `RouteKind.DELETE` + `InboundMessage.reply_to_external_message_id` + `TelegramReplyTarget`; `Dispatcher._dispatch_delete` + three pinned `_REPLY_DELETE_*` strings; `DeleteOutcome` / `HardDeleteOutcome`. New `tests/test_delete_tombstone.py` + `tests/test_dispatcher_delete.py`; the ED-2 NOTE→DRAFT test updated to the new tombstone behavior. No new schema; no new I-/R- number; R-4 wording flipped. → ED-3 (D-117). |
| ED-n — drill evidence + close | **Done (D-118), pre-checkpoint. Closes the milestone.** Operator-run real-backend drill per the RUNBOOK "Edit/delete real-backend drill (ED-n)" subsection: live Telegram seed NOTE; NOTE→NOTE edit supersession through `edited_message` after the narrow corrective adapter patch; retrieval after edit; reply-targeted `/delete`; retrieval after delete; NOTE→DRAFT tombstone; operator-only audited hard-delete through the documented inline Python path; PG acceptance legs under a dedicated `MEMORY_RAG_PG_TEST_DSN` (`73 passed`). Populated dated artifact: `docs/edit-delete-drill/edit-delete-smoke-20260615-evidence.json`; redaction check clean. |

## Phase 3 — Embeddings and Hybrid Retrieval *(Stage 1 — Product baseline)*
| Slice | Files / artifacts |
| --- | --- |
| 3.1+3.2 embedding adapter + sync indexing | `core/embeddings/{client,models}.py` (`EmbeddingClient` Protocol, `EmbeddingRecord`, `EmbeddingStatus`); `adapters/embeddings/{mock,openai_client,factory}.py`; `event_chunks.embedding_status` column + `embedding_records` table with `vector(3072)`; pgvector image swap in `docker-compose.yml`; boot dimension + pgvector probe (R-10); `DomainService` calls embedding client after `save_event_chunks`; failure → `embedding_status='failed'`; replay short-circuits before embedding. Closes A-5 / A-7 / A-8 via D-024; opens A-35 / A-36 |
| 3.3 baseline hybrid retrieval | `storage/search_repository.py` (`SearchRepository` + `HybridDomainStore`); `services/retrieval.py` (service-layer RRF); generated `event_chunks.chunk_text_tsv` + GIN index; Postgres dense exact-scan + sparse `tsvector('simple')`; mock + sqlite parity (mock deterministic; sqlite `NotImplementedError`); `QueryService` rewritten to hybrid; dispatcher converts `NotImplementedError` → `NO_EVIDENCE`. Closes A-6 / A-29 via D-025; opens A-36b (ANN strategy) and A-37 (sparse dictionary). **Out of scope: BM25, rerankers, Qdrant** (next quality-decision packet) |
| 3.4 metadata filtering (date dimension) | **Decision D-040.** Date is metadata-filter dimension 1 of 3. `core/domain/models.py` adds the channel-neutral frozen `DateRange(start, end)` (inclusive `note_date` bound, either side optional, `start > end` rejected); `core/domain/__init__.py` re-exports it. `storage/search_repository.py` both Protocol legs gain keyword-only `date_range: DateRange \| None = None`. `storage/postgres/store.py` both legs compose a conditional `note_date` range predicate (positional params); `storage/mock/store.py` applies the deterministic equivalent (mock/Postgres parity); `storage/sqlite/store.py` signature-only (still `NotImplementedError`). `services/query_service.py` `answer` gains a per-call `date_range` threaded to both legs (honored end-to-end; no inbound `/ask` date syntax yet). `tests/test_domain_models.py` (new); `tests/test_search_repository_{mock,postgres}.py` and `tests/test_query_service.py` extended. Closes D-040. **Out of scope: child / visibility filters (dimensions 2–3; visibility waits on A-15), inbound date syntax / NL date parsing, SQLite real retrieval, schema/DDL, date-diversity reranking.** |
| 3.5 retrieval traces | `core/domain/models.py` adds `Query`, `RetrievalHit`, `RetrievalLeg`; `storage/repository.py` adds `save_query` / `save_retrieval_hits` / `get_query` / `get_retrieval_hits_for_query`; mock / sqlite / postgres backends implement the seam (SQLite is real for trace storage even though retrieval still raises `NotImplementedError`); `storage/postgres/schema.sql` adds `queries` + `retrieval_hits` tables with `UNIQUE (query_id, chunk_id, leg)`; `services/retrieval.py` returns `list[FusedHit]` so the fused score persists on merged rows; `services/query_service.py` writes one `Query` row + per-leg + merged `RetrievalHit` rows per `/ask`, including the `NO_EVIDENCE` zero-hits case; `tests/test_storage_query_traces.py`; operator SQL inspection via RUNBOOK (no user `/trace` command yet). Closes D-032. |
| 3.6 retrieval-quality inspection harness | `src/memory_rag/eval/retrieval/{harness,__main__,regenerate_embeddings}.py`; `eval/retrieval/{gold.json,corpus.jsonl}` (embeddings cache is operator-produced, not committed); `tests/test_retrieval_harness_shape.py` (mock-mode shape-only check under `make check`); new D-038 entry in `decision-log.md` (baseline snapshot pasted by operator after the Postgres run); RUNBOOK "Retrieval-quality inspection harness (D-038)" subsection; `todo.md` "search-quality fork" item narrowed. Postgres mode is operator-run, env-gated by `MEMORY_RAG_PG_TEST_DSN`, produces the baseline snapshot for the next quality-decision packet to measure against. Closes D-038. **Out of scope:** any retrieval-behavior change; live OpenAI inside `make check`; quality thresholds / gating; the first quality-decision packet (BM25 / reranker / Qdrant / halfvec / multilingual sparse); Slice 3.4 metadata filters; schema/DDL changes; nDCG; D-026 renames. Cross-reference: Phase 7.1's gold-eval-set bracketing is seeded here, narrowed to the D-025 baseline; broader Phase 7 evaluation (groundedness, cost/latency, multi-snapshot tracking) remains its own slice. |
| 3.7 language-aware sparse FTS | **Decision D-039** (docs-only) selects a dual-config tsvector union as the first quality lever. Implementation: `storage/postgres/schema.sql` generated `chunk_text_tsv` unions `to_tsvector('russian', chunk_text)` and `to_tsvector('english', chunk_text)`; `storage/postgres/store.py` `sparse_candidates` unions `websearch_to_tsquery('russian', $q)` and `websearch_to_tsquery('english', $q)`, ranked by `ts_rank_cd`; mock + sqlite parity preserved (mock deterministic; sqlite still `NotImplementedError`); A-34 destructive local upgrade (generated-column change → `docker compose down -v`); re-run the D-038 harness in Postgres mode against the recorded baseline; TechSpec §9 + RUNBOOK updated when behavior changes. Implements D-039 / resolves A-37. **Sequencing: the implementation lands only after the operator records the D-038 baseline snapshot.** **Out of scope:** BM25, reranker, Qdrant, A-36b halfvec/HNSW, Slice 3.4 metadata filtering, harness corpus/gold changes. |

## Phase 4 — Grounded Answer Pipeline *(Stage 1 — Product baseline)*
| Slice | Files / artifacts |
| --- | --- |
| 4.1 context assembler | `core/domain/models.py` adds the channel-neutral `AnswerContext` (`query_id`, `query_text`, `ordered_chunks`, `model_name`, `created_at`); `services/context_assembler.py` (pure mapping from `Query` + RRF-merged `FusedHit` list → `AnswerContext`, order preserved from RRF, dedup upstream); `QueryService.answer` invokes the assembler after RRF and attaches the result to `AnswerResult.context` for success / no-evidence / empty-query contours; date grouping and other presentation shapes stay out of the canonical model. `tests/test_context_assembler.py` plus extended `tests/test_query_service.py`. Telegram reply layer unchanged in this slice. |
| 4.2 answer prompt contract | `core/domain/answer_prompt.py` adds `PROMPT_VERSION = "v1"`, `AnswerPrompt`, `build_answer_prompt(context: AnswerContext) -> AnswerPrompt` (deterministic; asserts R-8 single-community across `ordered_chunks`); `core/domain/answer_schema.py` adds `StructuredAnswer(answer_text, cited_chunk_ids, uncertainty)`, `UncertaintyMarker = Literal["confident", "uncertain", "no_evidence"]`, `parse_structured_answer(raw, *, context)` enforcing I-9 at the contract boundary (`cited_chunk_ids ⊆ AnswerContext.ordered_chunks`; empty only when `no_evidence`) with typed `StructuredAnswerError` subclasses. `core/domain/__init__.py` re-exports. `tests/test_answer_prompt.py`, `tests/test_answer_schema.py`. No `QueryService.answer`, dispatcher, retrieval, or schema changes — pure contract layer for the upcoming chat-client + AnswerTrace packets. Closes D-033. |
| 4.3a chat client + AnswerTrace | `core/answers/{client,__init__}.py` adds `ChatClient` Protocol + `ChatResponse` (`latency_ms` is the single source of truth); `adapters/answers/{mock,factory,__init__}.py` adds `MockChatClient` (honest provenance, `model_name="mock"`, `latency_ms=0`) and `build_chat_client`. `core/domain/models.py` adds `AnswerTrace` and an optional `AnswerResult.answer_text`. `storage/repository.py` adds two new Protocol methods (`save_answer_trace`, `get_answer_trace_for_query`); mock / sqlite / postgres backends implement the seam. `storage/postgres/schema.sql` adds the `answer_traces` table with FK to `queries.query_id` and `UNIQUE (query_id)`. `services/query_service.py` takes `chat_client: ChatClient` as a new positional arg, calls `build_answer_prompt` → `chat.complete` → `parse_structured_answer` → `save_answer_trace` on the success contour, and persists an `AnswerTrace` with empty `context_chunk_ids` + `fallback_mode=NO_EVIDENCE` on the no-evidence and empty-query contours (no LLM call). `adapters/telegram/webhook.py` wires the chat client; `config.py` adds `chat_backend: Literal["mock"]`; `app.py` adds `_verify_chat_contour`. `tests/test_chat_client_mock.py`, `tests/test_storage_answer_traces.py`; extensions to `tests/test_query_service.py` and `tests/test_end_to_end_smoke.py`. Closes D-034. Out of scope: real provider adapters; weak-evidence / ambiguous / provider-unavailable grading; `confidence_band` semantics; Telegram citation rendering; parse-failure repair / retry loops. |
| 4.3b answer-side fallback grading | `core/domain/models.FallbackMode` extended with `WEAK_EVIDENCE` / `AMBIGUOUS` / `PROVIDER_UNAVAILABLE` / `PARSE_FAILURE`; `UncertaintyMarker` extended with `"ambiguous"` and the parser tightens "empty citations only when `no_evidence`"; `core/answers/client.py` adds `ChatProviderUnavailableError`; `services/query_service.py` grades each contour, writes `Query.fallback` and `AnswerTrace.fallback_mode` from one decision so they always agree (the helper generalises to `_persist_answer_trace`); `services/dispatcher.py` switches `_format_answer_reply` on `FallbackMode` and sub-branches `NO_EVIDENCE` on `bool(AnswerResult.evidence)` so the LLM-marker case produces a distinct reply (R-6); Postgres+SQLite CHECK constraints on `queries.fallback` and `answer_traces.fallback_mode` widened (A-34); new `tests/test_dispatcher_retrieval_fallback.py`, `tests/test_query_service.py`, `tests/test_storage_answer_traces.py`, `tests/test_answer_schema.py`, `tests/test_end_to_end_smoke.py` cases. Closes D-035. `confidence_band` explicitly deferred. |
| 4.4 answer-text reply + on-demand `/sources` | `services/dispatcher.py` (rewrite `_format_answer_reply` to render `AnswerResult.answer_text` + unchanged trailers; add `_REPLY_SOURCES_NONE` + `_render_source_block`; private `_latest_sources` cache; `_update_latest_sources` + `_dispatch_sources`; remove `_format_evidence_lines`); `core/routing/models.py` (`RouteKind.SOURCES`; `DispatchResult.source_blocks`); `adapters/telegram/commands.py` (`/sources` token); `adapters/telegram/webhook.py` (SOURCES outbound branch via `pack_drafts_into_messages` + `send_message`); `tests/test_dispatcher_sources.py` + `tests/test_telegram_sources.py`; in-place flips of evidence-bullet assertions in `tests/test_dispatcher_retrieval_fallback.py`, `tests/test_end_to_end_smoke.py`, `tests/test_telegram_reply.py`, `tests/test_telegram_commands.py`. Closes D-036. **Out of scope:** rendering the LLM-emitted `StructuredAnswer.cited_chunk_ids`; fine-grained / per-quote attribution; cross-restart durability of the latest-sources cache; `/sources N` argument. |
| 4.5 OpenAI chat client adapter + boot gate | `adapters/answers/openai_client.py` adds `OpenAIChatClient` (canonical `gpt-4.1`, `response_format={"type":"json_object"}`, `temperature=0`, single attempt; `openai.OpenAIError` / `TimeoutError` → `ChatProviderUnavailableError`); `adapters/answers/factory.py` adds the `openai` branch; `adapters/answers/__init__.py` re-exports `OpenAIChatClient`; `config.py` widens `chat_backend: Literal["mock","openai"]` and records `chat_model` is load-bearing for openai; `app.py` adds `_CANONICAL_OPENAI_CHAT_MODEL = "gpt-4.1"`, extends `_verify_chat_contour` (mismatch / missing key abort), and the `app.created` log line gains `chat_model=…`; `.env.example` ships canonical `CHAT_MODEL=gpt-4.1`; `tests/test_boot_chat_gate.py` covers canonical-success / wrong-model / missing-key / mock-ignores cases (all offline); `tests/test_chat_client_openai.py` is the optional live smoke gated by `MEMORY_RAG_OPENAI_TEST_KEY` (not part of `make check`). Closes D-037. Closes A-9. **Out of scope:** retries / backoff / circuit breakers (Phase 6); dead-letter / repair (Phase 6); rate-limit / cost / hashing (Phase 6 / 7); streaming / multi-turn / tool-use; Anthropic or any second provider; provider observability expansion; `confidence_band` schema work; live OpenAI calls inside `make check`; D-026 renames; migration tooling. |

## Phase 5 — Optional AI Quality Boosters *(Stage 3 — Quality / Expansion)*
Stage 3 — every slice below is gated on the Stage-2 exit criteria (D-043).
| Slice | Files / artifacts |
| --- | --- |
| 5.1 feature-flag system | flag registry, per-request resolution, log of effective flags (R-12) |
| 5.2 query rewriting | rewriter behind flag |
| 5.3 reranking | reranker behind flag |
| 5.4 answer modes | timeline mode, analytical synthesis mode |

## OP-1 — Schema-migration tooling *(Stage 2 — Operationalization; complete)*
Stage 2 — runs after Stage 1 and before any Stage-3 slice (D-043). Packet group
**OP-1** in `docs/OPERATIONALIZATION-ROADMAP.md` (D-044); OP-1 leads Stage 2
because OP-2/OP-3/OP-4 all depend on non-destructive migrations. OP-1 is
complete: OP-1.1 landed the tooling foundation and OP-1.2 landed and validated
the first non-destructive schema-changing upgrade migration; A-34 is resolved.
| Slice | Files / artifacts |
| --- | --- |
| OP-1.1 migration tooling foundation | `yoyo-migrations` dependency; `src/memory_rag/storage/postgres/migrations/0001.baseline-schema.sql` (baseline migration capturing the retired `schema.sql` verbatim); `src/memory_rag/storage/postgres/migrations_runner.py` (`apply_migrations` / `stamp_baseline` / CLI); `PostgresDomainStore.__init__` rewired to apply migrations to head; `schema.sql` deleted; `pyproject.toml` wheel `force-include`; `tests/test_postgres_migrations.py`. Foundation only — does **not** close OP-1 or resolve A-34 (no schema-changing upgrade migration yet). Closes D-045. |
| OP-1.2 non-destructive upgrade | `src/memory_rag/storage/postgres/migrations/0002.index-embedding-status.sql` (first schema-changing upgrade — `CREATE INDEX IF NOT EXISTS idx_event_chunks_embedding_status ON event_chunks(embedding_status)`, additive and non-destructive); `tests/test_postgres_migrations.py` extended (discovery covers 0001 + 0002; gated tests prove the 0001→0002 upgrade over populated data, the stamp-adopted-volume upgrade, and store-constructor bootstrap to 0002). Completes OP-1 and resolves A-34. Closes D-046. |

## Phase 6 — Provider Hardening *(Stage 2 — Operationalization)*
Stage 2 — runs after Stage 1 and before any Stage-3 slice (D-043). Decomposed as
**OP-2** (provider hardening) in `docs/OPERATIONALIZATION-ROADMAP.md` (D-044);
A-35 failed-embedding reconciliation is the separate **OP-3** packet, which
consumes the 6.1 retry and 6.2 dead-letter primitives below. Slice 6.1
(timeouts & bounded retries, D-047), Slice 6.2 (dead-letter surface, D-048),
and Slice 6.3 (rate-limit backoff, D-049) have all landed — OP-2 is complete.
| Slice | Files / artifacts |
| --- | --- |
| 6.1 timeouts & retries | `src/memory_rag/adapters/resilience.py` (`RetryPolicy`, `OutcomeClass`, `classify_openai_error`, `run_with_retries` — shared adapter-side bounded-retry/timeout primitive, R-9); both OpenAI adapters (`adapters/embeddings/openai_client.py`, `adapters/answers/openai_client.py`) build the SDK client with explicit `timeout` + `max_retries=0` and wrap the API call in `run_with_retries`; `config.py` adds `provider_timeout_seconds` / `provider_max_attempts`; both factories thread a `RetryPolicy` into the `openai` branch; `.env.example` stanza; `tests/test_provider_resilience.py`, `tests/test_openai_embedding_retry.py`, `tests/test_openai_chat_retry.py` (offline). Embedding exhaustion re-raises the original SDK error (A-35 preserved); chat exhaustion raises `ChatProviderUnavailableError` (D-035 preserved). Closes D-047. |
| 6.2 dead-letter | `core/domain/models.py` adds the channel-neutral `IndexingDeadLetter` entity (re-exported from `core/domain/__init__.py`); `storage/postgres/migrations/0003.indexing-dead-letter-table.sql` adds the additive, non-destructive `indexing_dead_letters` table; SQLite gets the equivalent embedded DDL and the mock store a process-local dict; `DomainRepository` gains `save_indexing_dead_letter` / `list_indexing_dead_letters` / `get_indexing_dead_letter` with mock / sqlite / postgres parity; `DomainService._embed_chunks` attempts a best-effort dead-letter write after the `embedding_status='failed'` marking (`embedding.failed` log line gains `dead_letter_id`; a failed write logs `dead_letter.write_failed` and is swallowed); `tests/test_storage_dead_letter.py`, extended `tests/test_domain_service.py` and `tests/test_postgres_migrations.py`. Failed indexing jobs survive and are inspectable. Closes D-048. |
| 6.3 rate-limit handling | `adapters/resilience.py` adds `compute_backoff` (exponential delay with full jitter, clamped to the cap) and `extract_retry_after_seconds` (numeric `Retry-After` from `openai.RateLimitError`); `run_with_retries` gains an injected `sleep` seam and `retry_after` callable, waits between retryable attempts, and the retryable `provider.attempt` line that is followed by a wait gains `delay_ms` / `delay_source`; `RetryPolicy` gains `backoff_base_seconds` / `backoff_cap_seconds`; `config.py` adds `provider_backoff_base_seconds` / `provider_backoff_cap_seconds`; both factories thread them in; `.env.example` stanza; `adapters/embeddings/openai_client.py` and `adapters/answers/openai_client.py` pass `retry_after=extract_retry_after_seconds`; extended `tests/test_provider_resilience.py`, zero-backoff `RetryPolicy` in `tests/test_openai_embedding_retry.py` / `tests/test_openai_chat_retry.py` (offline). `Retry-After` is clamped to the cap so the R-9 worst-case bound stays computable. Closes D-049. |
| 6.4 failed-embedding reconciliation | retry `embedding_status='failed'` with bounded backoff + dead-letter (A-35) — decomposed as **OP-3** below |

## OP-3 — Failed-embedding reconciliation *(Stage 2 — Operationalization; complete)*
Stage 2 — runs after Stage 1 and before any Stage-3 slice (D-043). Packet group
**OP-3** in `docs/OPERATIONALIZATION-ROADMAP.md` (D-044); runs after OP-1 and
OP-2 because it consumes the 6.1 retry and 6.2 dead-letter primitives. OP-3.1
landed the read-only discovery seam, OP-3.2a landed the retry execution
(`failed → ready`), and OP-3.2b landed exhausted-retry dead-letter routing —
OP-3 is complete and **A-35 is resolved**.
| Slice | Files / artifacts |
| --- | --- |
| OP-3.1 failed-embedding discovery + read-only entrypoint | `storage/repository.py` adds the `DomainRepository.list_failed_event_chunks` Protocol method (community-scoped, `embedding_status='failed'`, `(created_at ASC, chunk_id ASC)` order, optional `limit`); `storage/mock/store.py`, `storage/sqlite/store.py`, `storage/postgres/store.py` implement it with full parity (Postgres reuses the OP-1.2 `idx_event_chunks_embedding_status` index — no new migration); `services/reconciliation.py` (new) adds `ReconciliationService.discover_failed_chunks`, the channel-neutral `FailedEmbeddingReport`, `render_report`, and the read-only `python -m memory_rag.services.reconciliation --community <id> [--limit N]` CLI (Postgres-targeted, mirroring `migrations_runner`); `services/__init__.py` re-exports both; `tests/test_storage_failed_chunks.py` and `tests/test_reconciliation.py` (new). Replaces the raw `psql` probe as the failed-chunk inspection surface. Discovery only — no retry, no status transition, no dead-letter routing; A-35 stays open. Closes D-050. |
| OP-3.2a failed-embedding retry execution | `services/reconciliation.py` — `ReconciliationService` gains an optional injected `EmbeddingClient` and `retry_failed_chunks(community_id, *, limit=None)`, which consumes the OP-3.1 `list_failed_event_chunks` seam, groups failed chunks by `source_message_id`, retries each group with one `EmbeddingClient.embed` call (OP-2 bounded backoff stays internal to the client), persists `EmbeddingRecord` rows then transitions succeeded chunks `failed → ready`, and leaves exhausted-retry chunks `failed`; new channel-neutral `RetryGroupOutcome` / `RetryOutcomeReport` dataclasses and `render_retry_report`; `reconciliation.retry.group.ok|failed` + `reconciliation.retry.summary` logs; `_main` gains a `--retry` mode wiring `build_embedding_client`; `services/__init__.py` re-exports `RetryOutcomeReport`; extended `tests/test_reconciliation.py` (success, exhausted failure, source grouping, records-before-status, `UNIQUE` collision, `_main --retry`, PG-DSN-gated end-to-end). No new repository method, no schema or migration. Retries and clears succeeded chunks but routes no dead letters; A-35 stays open. Closes D-051. |
| OP-3.2b failed-embedding retry dead-letter routing | `services/reconciliation.py` — `retry_failed_chunks` routes an exhausted retry group to the OP-2.2 dead-letter surface: it builds one `IndexingDeadLetter` (fresh `uuid4` id; the group's `source_message_id` / `community_id` / `chunk_ids`; live client `model_name`; exception class name; retry-run timestamp) and persists it via the existing `DomainRepository.save_indexing_dead_letter` seam. The write is best-effort and append-only — the group is already `failed`, the write runs after that outcome, and a write failure is logged (`dead_letter.write_failed`) and swallowed so it never regresses the outcome or raises. `RetryGroupOutcome` gains `dead_letter_id` (set iff the group failed and its write succeeded); the `reconciliation.retry.group.failed` log and `render_retry_report` surface it; the `--retry` CLI prints it for free. Extended `tests/test_reconciliation.py` (exhausted-group write, write-failure swallowed, success writes no dead letter, renderer, PG-DSN-gated end-to-end). No new repository method, no schema or migration. **Resolves A-35**; OP-3 complete. Closes D-052. |

## Phase 7 — Evaluation and Observability *(Stage 2 — Operationalization)*
Stage 2 — runs after Stage 1 and before any Stage-3 slice (D-043). Decomposed as
**OP-5** (evaluation & observability) in `docs/OPERATIONALIZATION-ROADMAP.md`
(D-044); OP-5 closes Stage 2.
| Slice | Files / artifacts |
| --- | --- |
| 7.1 gold eval set | `eval/retrieval/observability/{gold.json,corpus.jsonl}` — a ~21-query / 19-message OP-5 *observability* gold set, sitting **beside** the frozen D-038 baseline set (`eval/retrieval/{gold.json,corpus.jsonl}`), not superseding it; `tests/test_retrieval_harness_shape.py` parametrized over both fixture pairs (mock-mode, shape-only); `.gitignore` for the operator-produced embeddings caches; new D-056 entry in `decision-log.md`; RUNBOOK harness subsection gains a fixture-set / invocation note; `OPERATIONALIZATION-ROADMAP.md` + `todo.md` updated. Default mock invocation still loads the D-038 set; the observability set is selected via explicit `--gold`/`--corpus` flags. Closes D-056 — **→ OP-5.1**. **Out of scope:** any retrieval-behavior or `src/` change; harness code change; live OpenAI inside `make check`; quality thresholds / gating / nDCG; the D-038 Postgres baseline capture; a Postgres-mode capture over the observability set; retrieval/groundedness metrics and cost/latency (OP-5.2 / OP-5.3); D-026 renames; schema/DDL changes. |
| 7.2 retrieval & groundedness metrics | **OP-5.2a (retrieval half):** `src/memory_rag/eval/retrieval/harness.py` — `AggregateMetrics` gains `hit_rate` + `empty_rate`, two new pure helper functions compute them from the existing `PerQueryResult` rows, `run_harness` populates the fields; `src/memory_rag/eval/retrieval/__main__.py` renders both in the human report with explicit denominator annotations (JSON output via `asdict`). `hit_rate` uses a non-empty-gold denominator (owner-confirmed) so it stays distinct from `per_leg_recall_at_20.fused`; `empty_rate` divides by all queries. `tests/test_retrieval_harness_shape.py` gains shape-only assertions for both fields; new `tests/test_retrieval_harness_metrics.py` pins the pure-function semantics. New D-057 entry in `decision-log.md`; RUNBOOK harness subsection documents both metrics; `OPERATIONALIZATION-ROADMAP.md` + `todo.md` updated. Inspection only — CLI exit stays `0`, no thresholds. **OP-5.2b (groundedness half):** `src/memory_rag/eval/retrieval/harness.py` adds `PerAnswerResult` / `GroundednessMetrics` / `GroundednessReport` dataclasses, pure helpers `is_grounded` / `groundedness_rate` / `fallback_mode_counts`, and a `run_answer_harness` loop that drives `QueryService.answer` over every gold query (`RouteKind.ASK`) and grades groundedness from `AnswerResult.fallback`; `HarnessReport` gains an optional `groundedness: GroundednessReport | None` field. `src/memory_rag/eval/retrieval/__main__.py` builds a `QueryService` over the ingested store (mock: `MockChatClient`; postgres: `build_chat_client(settings)`), attaches the groundedness report via `dataclasses.replace`, renders the "Groundedness proxy (answer-path, fallback-derived, inspection only)" section in the human report, and extends `_TRUNCATE_TABLES` with `answer_traces` / `retrieval_hits` / `queries`. Groundedness is a **fallback-derived proxy** for the I-9 citation-subset semantics — not a citation-coverage or factuality score: the documented mapping is `{NONE, WEAK_EVIDENCE, AMBIGUOUS}` → grounded, with `PARSE_FAILURE` (the I-9 citation-subset violation contour) and `PROVIDER_UNAVAILABLE` / `NO_EVIDENCE` not grounded. `groundedness_rate` uses the non-empty-gold (answerable) denominator, mirroring `hit_rate`. `tests/test_retrieval_harness_shape.py` gains shape-only assertions for the groundedness report over both fixture pairs (its mock end-to-end helper drives the answer harness too); new `tests/test_retrieval_harness_groundedness.py` pins the pure-function semantics. New D-058 entry in `decision-log.md`; RUNBOOK gains a subsection whose title matches the CLI section verbatim; `OPERATIONALIZATION-ROADMAP.md` + `todo.md` updated. Inspection only — CLI exit stays `0`, no thresholds. Closes D-057 + D-058 — **→ OP-5.2a + OP-5.2b**. **Out of scope:** exposing `cited_chunk_ids` on `AnswerResult` for citation-coverage metrics (recorded deferred follow-up); cost & latency aggregation — OP-5.3; the D-038 Postgres baseline capture; quality thresholds / gating / nDCG / CI gates / factuality classification; live OpenAI inside `make check`; any production answer-path or retrieval-behavior change; schema/DDL changes; D-026 renames. |
| 7.3 cost & latency | `src/memory_rag/eval/retrieval/harness.py` adds `CostMetrics` / `LatencyMetrics` / `CostLatencyMetrics` dataclasses, a `RecordingChatClient` shim (single-call / single-consumer slot with read-and-consume `consume_last`, structurally implementing the `ChatClient` Protocol), pure helpers `_latency_stats` / `cost_metrics` / `latency_metrics`, and per-row carrier fields `PerQueryResult.retrieval_latency_ms` + `PerAnswerResult.{answer_latency_ms,prompt_tokens,completion_tokens}`. `run_harness` wraps `dense+sparse+RRF` with `time.perf_counter` (query-embedding lookup intentionally excluded — mock and Postgres modes obtain query embeddings asymmetrically). `run_answer_harness` gains keyword-only `chat_recorder: RecordingChatClient \| None = None`, wraps `query_service.answer(...)` with `time.perf_counter`, and reads provider-reported token counts via `consume_last()` — clear-on-read semantics structurally prevent any prior `ChatResponse`'s tokens from leaking onto a later answer-path contour that short-circuits without a chat call (`NO_EVIDENCE` / empty-query / `PROVIDER_UNAVAILABLE` — D-035). `HarnessReport` gains an optional `cost_latency: CostLatencyMetrics \| None` field. Latency is **wall-clock only** at the aggregate (mean / p50 / max for each of retrieval and answer); the provider-attributed `ChatResponse.latency_ms` stays trace-level provenance on `AnswerTrace` (D-034 / D-035) and is **not** re-aggregated. `p95` is intentionally omitted at the current ~20-21-query gold-set size — too noisy to be meaningful. `src/memory_rag/eval/retrieval/__main__.py` wires `RecordingChatClient` around the operator-selected chat client in both mock and Postgres modes, forwards it to `run_answer_harness`, computes `CostLatencyMetrics`, and attaches it via `dataclasses.replace`; `_format_human` renders the `Cost & latency (wall-clock + provider-reported tokens, inspection only)` section with explicit denominator annotations (`(denominator: answer-path calls with non-empty token_counts, n=<int>)` for the cost mean; `(denominator: all queries)` for both latency blocks). The mock client approximates `token_counts` from character counts (`[[feedback_honest_mock_provenance]]`), so the section title and the RUNBOOK subsection title both name them as "provider-reported". `tests/test_retrieval_harness_shape.py` adds `test_mock_mode_includes_cost_and_latency_shape` over both fixture pairs (shape-only `isinstance` + `>= 0` checks, `total_tokens == prompt + completion` arithmetic check, `max >= mean` / `max >= p50` distributional sanity) plus per-row latency/token type checks on the existing per-query and groundedness shape tests; new `tests/test_retrieval_harness_cost_latency.py` pins the pure-function semantics (12 tests, including empty-report → 0, zero-token-row exclusion from the per-call mean, and the user-required `test_recorder_no_misattribution_across_calls` guardrail). New D-059 entry in `decision-log.md`; new `Cost & latency (wall-clock + provider-reported tokens, inspection only)` subsection in `RUNBOOK.md` (title matches the CLI verbatim); `OPERATIONALIZATION-ROADMAP.md` + `todo.md` updated. Inspection only — CLI exit stays `0`, no thresholds, no live API in `make check`, no schema change, no `_TRUNCATE_TABLES` change (OP-5.2b already covers the answer-path tables). Closes D-059 — **→ OP-5.3**. **Out of scope:** quality thresholds / gating / regression budgets / CI gates; live OpenAI inside `make check`; any change to `AnswerResult` / `QueryService` / `parse_structured_answer` / retrieval-path code outside the eval-harness surface; aggregating provider-attributed `ChatResponse.latency_ms` (stays trace-level provenance); aggregating `chat_model_name` per-row (already persisted on `AnswerTrace`); distributional stats beyond mean / p50 / max; the operator-run D-038 Postgres baseline capture; D-026 renames; schema/DDL changes. |

## Phase 8 — Privacy and Shared-Diary Controls *(spans stages — see note)*
Phase 8 spans two stages (D-043). The raw-data durability/backup contour
(daily backup window, stronger-than-nightly recovery — D-027, A-40) is **Stage 2
only**, decomposed as **OP-4** in `docs/OPERATIONALIZATION-ROADMAP.md` (D-044) and
mapped to slice row 8.0 below. The remaining slices — access control, visibility,
export/delete, audit, retention — are **Stage 3**, are gated on the Stage-2 exit
criteria, and are **not** decomposed by D-044.
| Slice | Files / artifacts |
| --- | --- |
| 8.0 raw-data durability & backup | daily backup window (`03:00–05:00` target) + a stronger-than-nightly recovery primitive — nightly base backup + continuous WAL archiving → PITR — for raw `SourceMessage` (D-027, D-053; A-40 resolved) — **Stage 2**, OP-4 |
| 8.1 community-scoped access | enforced at every read (I-7, R-3) — Stage 3; decomposed in `docs/READ-ACCESS-ENFORCEMENT-ROADMAP.md` (D-087); packets 8.1.0..8.1.3 below — **complete (D-090)** |
| 8.2 visibility model | per-note scopes (assumption A-15) — Stage 3 |
| 8.3 export/delete flow | tombstones + audit log + retention policy — Stage 3 |

### Read-access enforcement (Slice 8.1)
Slice 8.1 is decomposed docs-first in `docs/READ-ACCESS-ENFORCEMENT-ROADMAP.md`
(D-087); that doc carries the as-built read-path audit and the refinable packet
sequence, mirroring the OP-/DEPLOY- roadmap precedent. The hot `/ask` read path
is already community-scoped and tested (R-3 / R-8); the remaining packets harden
the latent by-id/trace read seams.
| Slice | Files / artifacts |
| --- | --- |
| 8.1.0 audit + decomposition | docs-only (D-087): new `docs/decision-log.md` D-087 entry, new `docs/READ-ACCESS-ENFORCEMENT-ROADMAP.md`, this map row + note block, `docs/todo.md`. Records the I-7 / R-3 / R-8 framing, the as-built audit (enforced hot path vs. latent unscoped by-id/trace reads), the defensive-scoping decision, and the 8.1.1/8.1.2/8.1.3 ladder. No `src/` / `tests/` / schema change. → 8.1.0 (D-087). |
| 8.1.1 defensive scoping of unused by-id/trace reads | mandatory **keyword-only** `community_id` + null-guard + owning-community filter on `get_query`, `get_retrieval_hits_for_query`, `get_answer_trace_for_query`, `get_event_chunk` across the `DomainRepository` Protocol + mock / sqlite / postgres (trace reads scope via the `query_id → queries.community_id` join; `get_query` / `get_event_chunk` filter their own column; parent-missing → `[]` / `None` fail-closed across backends); test-only call sites updated; guard + isolation + shared parent-missing tests. No live `/ask` behavior change, no schema change. **Does not close `get_source_message` / `/sources` (8.1.2).** → 8.1.1 (D-088). |
| 8.1.2 `get_source_message` scoping + `/sources` isolation | scope `get_source_message` (keyword-only `community_id`, R-3 guard, own-column filter) across the Protocol + all backends; thread the **requester-scoped** `community_id` through the live `/sources` author-resolution path (webhook edge → `render_source_block` → `resolve_chunk_author_display`), keeping storage/helper seams on `community_id` vocabulary; `_latest_sources` / dispatcher unchanged (already-community-keyed); seam-focused mismatch test → opaque author floor. No schema change. → 8.1.2 (D-089). |
| 8.1.3 milestone closure / verification | consolidated cross-community isolation sweep (`tests/test_read_access_isolation.py`, also closing the previously-unproven `get_event_chunk` isolation/guard), `docs/RUNBOOK.md` read-access scoping note, execution-map + todo closure, DoD evidence ("cross-community leakage is prevented" / "access behavior is explicit"). No `src/` / schema change. → 8.1.3 (D-090). **Slice 8.1 complete.** |

## OP-4 — Raw-data durability & backup *(Stage 2 — Operationalization; complete)*
Stage 2 — runs after Stage 1 and before any Stage-3 slice (D-043). Packet group
**OP-4** in `docs/OPERATIONALIZATION-ROADMAP.md` (D-044); covers the Stage-2
raw-data durability/backup slice of Phase 8 (row 8.0 above) — depends only on
OP-1 (recovery restores into a schema-versioned database) and may run in
parallel with OP-2/OP-3. OP-4.1 landed the A-40 decision, OP-4.2 landed the
backup automation (WAL archiving + nightly base backup), and OP-4.3 landed the
restore tooling and executed the restore drill — OP-4 is complete.
| Slice | Files / artifacts |
| --- | --- |
| OP-4.1 backup mechanism & recovery contour decision | docs-only decision packet (D-053): resolves A-40 — for the reference/local Postgres shape (`docker-compose.yml`), the recovery primitive is a nightly physical base backup (`pg_basebackup`) + continuous WAL archiving (`archive_command`) → point-in-time recovery; managed-cloud and self-hosted shapes use the provider- or operator-owned equivalent (no vendor named — A-41 stays open). RPO ≤ 5 min / RTO ≤ 1 h for raw `SourceMessage` data; nightly base-backup window `03:00–05:00` local (D-027, unchanged); base backups retained 30 days, WAL retained to cover the oldest retained base backup; restore drill once before the first non-local deployment, then quarterly thereafter. Decomposes OP-4 into OP-4.1/4.2/4.3. Updates `decision-log.md`, `assumptions.md` + `assumption-audit.md` (**A-40 closed**), `OPERATIONALIZATION-ROADMAP.md`, `RUNBOOK.md`, this map, `todo.md`, `ARCHITECTURE.md`, `TechSpec.md`. Docs-only — no scripts, scheduler, WAL config, schema/migration, or `src/` change. **Resolves A-40**; OP-4 in progress. Closes D-053. |
| OP-4.2 backup automation | implementation packet (D-054): turns the D-053 contract into a runnable mechanism for the reference Postgres shape. `docker-compose.yml` — the `postgres` service `command:` passes `-c` flags for `wal_level=replica`, `archive_mode=on`, `archive_timeout=300`, and an idempotent/atomic `archive_command` copying WAL into the archive volume; a new `memory_rag_pg_archive` volume (`/archive/{wal,base}`), a one-shot `pg_archive_init` (creates + `chown`s the layout to UID 999), the `configs/postgres/initdb-replication-hba.sh` initdb hook (new — adds a `host replication` pg_hba rule for the sidecar), and an opt-in `pg_backup` sidecar under the `backup` Compose profile. `scripts/pg_backup/{scheduler,backup,prune}.sh` (new): one long-running `scheduler.sh` per container runs `pg_basebackup` (`--format=tar --wal-method=none --gzip`) once per day in the `[BACKUP_WINDOW_START,END)` local window, then `pg_archivecleanup`-based retention (30-day base backups; WAL keyed on the oldest retained backup's `START_WAL`); `backup.sh`/`prune.sh` share a `flock` on `/archive/.backup.lock`; cycles write `/archive/last_success.json` / `last_failure.json`. `.env.example` + `Makefile` (`backup-up`/`backup-run`/`backup-prune`) + `RUNBOOK.md` "Backup automation" updated. Infra + config + docs only — no schema/migration/`src/` change, no executed restore drill. OP-4 still in progress. Closes D-054. |
| OP-4.3 restore drill | implementation packet (D-055): adds operator-grade restore tooling and executes the restore drill. New `scripts/pg_restore/restore.sh` — prepares a recovered Postgres data dir from an OP-4.2 base backup (`base.tar.gz`) + archived WAL, supports `--target=latest` / `--target-timestamp` (PITR), `--dry-run`, `--yes`, refuses an apparently-live cluster, operates only on the throwaway `memory_rag_pg_restore_data` scratch volume, writes `/archive/restore_logs/` markers. `docker-compose.yml` — new opt-in `pg_restore` service (profile `restore`, host port `RESTORE_PORT` default `5433`) + `memory_rag_pg_restore_data` volume; `.env.example` (`RESTORE_PORT`) + `Makefile` (`restore-plan`/`restore-run`, non-gated). Executed drill on the reference/local shape: base backup → 3 synthetic `source_messages` batches → full restore (15/15 rows, RTO 5 s) and PITR restore (batch-1 only, RTO 3 s); RPO bounded by `archive_timeout=300` ≤ 5 min. Both D-053 targets **met** — recorded in `docs/op4-drill/op4.3-20260519-evidence.json`. `RUNBOOK.md` "Restore drill" subsection added; the "target, not a measured result" caveat replaced. Adapter + config + docs only — no schema/migration/`src/` change, no OP-4.2 backup-automation change. **Phase 8 raw-data durability DoD met; OP-4 complete.** Closes D-055. |

## Phase 9 — TheyGrow Integration Seam *(Stage 3 — Quality / Expansion)*
Stage 3 — every slice below is gated on the Stage-2 exit criteria (D-043).
| Slice | Files / artifacts |
| --- | --- |
| 9.1 internal API/SDK | non-Telegram client surface (assumption A-21) |
| 9.2 identity mapping | family / child mapping into TheyGrow identities |
| 9.3 isolation audit | confirm no Telegram leakage outside the adapter (I-1) |

## Deployment-shape rollout *(sequenced separately from the Phase/Stage axis)*
Deployment-shape rollout is sequenced separately from Phase 0..9 and Stages 1..3:
**DEPLOY-1** is the first implemented reference deployment shape (self-hosted
VPS + Telegram, single-community pilot); **DEPLOY-2** is the deferred
managed-cloud peer shape. The refinable packet sequence and dependencies live in
`docs/SELF-HOSTED-DEPLOYMENT-ROADMAP.md` (D-060); the rows below are placeholder
pointers that will be filled when each packet is planned.
| Slice | Files / artifacts |
| --- | --- |
| DEPLOY-1.1 decision + roadmap | `docs/decision-log.md` (D-060), `docs/SELF-HOSTED-DEPLOYMENT-ROADMAP.md` (new), `docs/assumptions.md` (A-22 closed / A-41 deferred / A-42 / A-43), `docs/assumption-audit.md`, this map, `docs/todo.md`, `docs/RUNBOOK.md`, `docs/OPERATIONALIZATION-ROADMAP.md` (see-also), `docs/product/BuildPlan.md` (target-state shape). Docs-only. → DEPLOY-1.1 (D-060). |
| DEPLOY-1.2 VPS runtime shape | `Dockerfile`, `.dockerignore`, new `app_init` + `app` services in `docker-compose.yml` gated by `profiles: ["vps"]` (single canonical bring-up: `docker compose --profile vps up -d --build`), new "VPS runtime shape (DEPLOY-1.2 / D-061)" subsection in `docs/RUNBOOK.md`. App port loopback-only (`127.0.0.1:8000`) until DEPLOY-1.3. OP-1 migrations + OP-4 archive volume shape reused unchanged. No `src/` change. → DEPLOY-1.2 (D-061). |
| DEPLOY-1.3 reverse-proxy + TLS | `configs/caddy/Caddyfile`, new `caddy` service in `docker-compose.yml` gated by `profiles: ["vps"]` (single canonical bring-up unchanged: `docker compose --profile vps up -d --build`), two new `.env.example` knobs (`PUBLIC_HOSTNAME`, `ACME_EMAIL`), new "Reverse-proxy + TLS contour (DEPLOY-1.3 / D-062)" subsection in `docs/RUNBOOK.md`. Pins Caddy as the proxy / TLS terminator default. Loopback `127.0.0.1:8000:8000` on `app` retained as operator-only inspection, not a closure signal. No `src/` change. → DEPLOY-1.3 (D-062). |
| DEPLOY-1.4 installer / upgrade script | `scripts/installer/deploy.sh` (new; bash, non-interactive; carries `INSTALLER_CONFIG_VERSION=1` + `migrate_to_v1`), `.gitignore` entries for `.installer-state.json` and `.installer-state.last_failure.json` (installer-owned per-host state — the D-060 configuration-versioning seam), new "Installer / upgrade script (DEPLOY-1.4 / D-063)" subsection in `docs/RUNBOOK.md`. Pins **bash, non-interactive** as the §3 installer-implementation default. No `src/`, schema, migration, `docker-compose.yml`, or `.env.example` change. → DEPLOY-1.4 (D-063). |
| DEPLOY-1.5 Telegram webhook automation | `scripts/installer/deploy.sh` — `register_telegram_webhook` helper called after the loopback + public-TLS probes (best-effort: `registered (<url>)` / `skipped (public_tls_probe=…)` / `failed (…)`); new `--unregister-webhook` subcommand calling Telegram `deleteWebhook` and clearing the state-file block; `webhook_registration` block added to `.installer-state.json` via `write_state_success` (and `write_state_unregistered` on teardown); `INSTALLER_CONFIG_VERSION` bumped `1 → 2` with `migrate_v1_to_v2` (first non-trivial use of the D-063 seam); `TELEGRAM_BOT_TOKEN` and `TELEGRAM_WEBHOOK_SECRET` joined `REQUIRED_ENV_KEYS`; usage text updated. New "Telegram webhook registration (DEPLOY-1.5 / D-064)" subsection in `docs/RUNBOOK.md` plus amendments to the existing DEPLOY-1.4 inspection block. No `src/` / schema / migration / `docker-compose.yml` / `.env.example` / `tests/` change. → DEPLOY-1.5 (D-064). |
| DEPLOY-1.6 off-box backup sink wiring | New `pg_offbox_uploader` service in `docker-compose.yml` (image `rclone/rclone:1.66`, profile `["backup"]`) + new `scripts/pg_offbox_uploader/uploader.sh` (poll loop reading `/archive/last_success.json`, mirrors `/archive/base` + `/archive/wal` to the operator-supplied S3-compatible target via `rclone sync`, writes `/archive/last_offbox.json`, emits `pg_backup.offbox.{start,skipped,begin,ok,error}` log lines). `scripts/installer/deploy.sh` extended with `probe_offbox_backup` (active probe via `docker run rclone/rclone:1.66 lsd`, returns `ok` / `failed (…)` / `skipped (BACKUP_S3_* unset)`), new `offbox_backup_probe` field in `.installer-state.json`, `INSTALLER_CONFIG_VERSION` bumped `2 → 3` with `migrate_v2_to_v3` (second non-trivial use of the D-063 seam), `selected_defaults.backup_tool` flipped from `null` to `"rclone"`. Five new optional `BACKUP_S3_*` knobs in `.env.example`. New "Off-box backup sink (DEPLOY-1.6 / D-065)" subsection in `docs/RUNBOOK.md`. Pins **rclone** as the §3 backup-tool default. A-43 logs-first observability scope deferred to a later DEPLOY-1.x packet. No `src/` / schema / migration / `tests/` change; no change to `pg_backup` or `scheduler.sh`. → DEPLOY-1.6 (D-065). |
| DEPLOY-1.7-preflight local-only upgrade-drill harness | `scripts/installer/drill_upgrade_local.sh` (new) + `docs/deploy1-drill/deploy1-upgrade-drill-<YYYYMMDD>-evidence.json` (new) + new "Local-only upgrade-drill preflight (DEPLOY-1.7-preflight / D-066)" subsection in `docs/RUNBOOK.md` + `docs/SELF-HOSTED-DEPLOYMENT-ROADMAP.md` §1 / §4 / §5 amendments + new `docs/todo.md` entry. Sandboxed git worktree under `mktemp -d` (main repo working tree never modified); pins `COMPOSE_PROJECT_NAME=deploy1-preflight-drill` so named volumes survive across legs while isolated from operator compose state; runs the unchanged `./scripts/installer/deploy.sh` across commits `7cb96fa` (DEPLOY-1.4, `installer_config_version=1`) / `e435e1a` (DEPLOY-1.5, `installer_config_version=2`) / `0aef179` (DEPLOY-1.6, `installer_config_version=3`). Probe verdicts (`public_tls_probe` / `webhook_registration` / `offbox_backup_probe`) are captured **verbatim** under `observed_probes` and classified as `operator_dependent`; only `exit_code == 0` and `installer_config_version` matching the expected integer per leg are asserted. De-risks the configuration-versioning seam locally; does **NOT** close DEPLOY-1.7 and DEPLOY-1 stays open. No `scripts/installer/deploy.sh` / `docker-compose.yml` / `pg_offbox_uploader` / `pg_backup` / `pg_restore` change; no `src/` / schema / migration / `tests/` change. → DEPLOY-1.7-preflight (D-066). |
| DEPLOY-1.7a clean-VPS pilot smoke + off-box §2-invariant verification | `docs/deploy1-drill/deploy1-pilot-smoke-<YYYYMMDD>-evidence.json` (new — pilot-smoke evidence artifact paralleling D-066's upgrade-drill evidence) + new "Clean-VPS pilot smoke (DEPLOY-1.7a / D-067)" subsection in `docs/RUNBOOK.md` + `docs/SELF-HOSTED-DEPLOYMENT-ROADMAP.md` §1 / §4 / §5 / §6 amendments + this map row split + `docs/todo.md` entry split. Captures verbatim: installer `.installer-state.json` fields, the `https://$PUBLIC_HOSTNAME/health` body, the `telegram.webhook ... route=start ... POST /telegram/webhook 200` log + observed `~2–3 s` `/start` latency, an explicit `getUpdates=409` note framed as expected-with-webhook-active, and the off-box happy path + additivity smoke (force-failure → `status=error`, `last_success.json` unchanged, `pg_backup.cycle.ok` unaffected) against a real S3-compatible target. Credential text redacted from the evidence artifact. No `src/` / scripts / `docker-compose.yml` / `Caddyfile` / schema / migration / `tests/` change. → DEPLOY-1.7a (D-067). |
| DEPLOY-1.7b v2 → v3 cross-version upgrade drill on a real previously-installed v2 VPS *(re-scoped to DEPLOY-2 prep by D-076)* | Operator-procedure prep landed in DEPLOY-1 scope by D-068 (committed template at `docs/deploy1-drill/deploy1-cross-version-drill-TEMPLATE.json` + "v2 → v3 cross-version upgrade drill (DEPLOY-1.7b / D-068)" subsection in `docs/RUNBOOK.md`). **D-076 moves DEPLOY-1.7b out of DEPLOY-1 closure into DEPLOY-2 prep**: the committed template and the RUNBOOK subsection are retained verbatim for DEPLOY-2 use; the v2 → v3 cross-version migration drill against a real previously-installed v2 VPS is no longer a DEPLOY-1 closure dependency. Closure of DEPLOY-1.7b itself remains by a populated dated `docs/deploy1-drill/deploy1-cross-version-drill-<YYYYMMDD>-evidence.json` produced by a future operator drill when DEPLOY-2 is pulled. → DEPLOY-1.7b prep (D-068); re-scoped to DEPLOY-2 prep (D-076). |
| DEPLOY-1 closure prep (post-REAL-1) | New `docs/deploy1-drill/deploy1-closure-post-real1-TEMPLATE.json` (committed evidence-file template carrying `"_template": true`, six top-level branches `metadata` / `installer_state` / `live_probes` / `post_real1_round_trip` / `summary` / `out_of_scope_for_this_packet`, dual `<REDACTED>` / `<TO_FILL_BY_OPERATOR>` placeholder convention) + new "DEPLOY-1 closure procedure (post-REAL-1) (D-076)" subsection in `docs/RUNBOOK.md` (operator pre-conditions, numbered procedure against the already-deployed v3 contour, evidence-file shape, redaction rule, closure signal anchored on the post-REAL-1 round-trip + the A-43-pinned existing log families, `make check` non-impact note). `docs/SELF-HOSTED-DEPLOYMENT-ROADMAP.md` §1 / §4 / §5 / §6 / §7 updated to drop DEPLOY-1.7b from DEPLOY-1's exit criterion and re-scope it to DEPLOY-2 prep. `docs/todo.md` DEPLOY-1.7b bullet pivoted; new closure-prep bullet added. No `src/` / scripts / `docker-compose.yml` / `Caddyfile` / schema / migration / `tests/` / `.env.example` / `Makefile` / `Dockerfile` change. → DEPLOY-1 closure prep (D-076). |
| A-43 logs-first observability pin | `docs/assumptions.md` A-43 moved to "Recently closed" with `→ D-077`; `docs/assumption-audit.md` A-43 row struck through with `Closed → D-077`. Pin: existing `pg_backup.*` family in `scripts/pg_backup/scheduler.sh` + `scripts/installer/deploy.sh` + `scripts/pg_offbox_uploader/uploader.sh`; Caddy access logs at the reverse-proxy contour; app-side `telegram.webhook` (`src/memory_rag/adapters/telegram/webhook.py`), `retrieval.hybrid` (`src/memory_rag/services/retrieval.py`), and `answer.*` (`src/memory_rag/services/query_service.py` / `services/dispatcher.py`) families — captured verbatim, not invented. No new logging contract in `src/`. Forward seam to remote sinks deliberately unpinned and deferred. → A-43 logs-first observability pin (D-077, closes A-43). |
| DEPLOY-2 managed-cloud reference deployment *(deferred)* | Deferred — managed-cloud peer shape. Resolves A-41. Gets its own roadmap doc when pulled. Inherits DEPLOY-1.7b's committed cross-version-drill template + RUNBOOK subsection (retained verbatim by D-076) as DEPLOY-2 prep. |

## Post-DEPLOY-1 Phase-4 UX polish *(D-067 §Observations follow-up)*
Three Phase-4 user-facing surface gaps were surfaced by the DEPLOY-1.7a clean-VPS pilot smoke (D-067 §Observations) and deferred to a separate post-DEPLOY-1 milestone. Each packet is scoped to "Phase-4 surface polish without retrieval / answer-path / schema / migration change."

| Slice | Files / artifacts |
| --- | --- |
| Packet 1 — `/ask` user-facing reply de-leaks chunk UUID + `dense+sparse RRF` | `src/memory_rag/services/dispatcher.py` (`_RETRIEVAL_TRAILER` removed; `_format_answer_reply` `FallbackMode.NONE` branch returns `result.answer_text or ""`; `_render_source_block` signature becomes `(chunk, *, index, total)` and returns `[YYYY-MM-DD] (i/N)\n\n<chunk_text>`; `_dispatch_sources` passes 1-based index + `len(chunks)`), `tests/test_dispatcher_sources.py` (six `source_blocks` literal updates — chunk-id literals `c-1` / `c-2` / `a-1` / `b-1` no longer appear), `tests/test_end_to_end_smoke.py` (two assertions flipped to negative RRF / hybrid-jargon invariants plus an explicit `"\n\n" not in text` guardrail), `docs/RUNBOOK.md` (§"Selected-chunks recall (`/sources`, D-036)" rendering description updated with the per-last-`/ask` ephemeral framing; §"Hybrid retrieval (D-025)" reply description updated to say success-case is `answer_text` alone), `docs/decision-log.md` (D-069), `docs/todo.md` (new milestone block). R-6 / I-9 unaffected. No retrieval / scoring / answer-path / schema / migration / persistence change; no change to `INVARIANTS.md` / `RUNTIME-INVARIANTS.md` / `assumptions.md` / `assumption-audit.md` / `ARCHITECTURE.md` / `product/*` / `GLOSSARY.md` / `AGENTS.md` / `CLAUDE.md` / `README.md` / `QUICKSTART.md`. → Packet 1 (D-069). |
| Packet 2 — `/note` first-line date — six-form near-ISO whitelist + de-leaked error wording + proactive `/start` blurb | `src/memory_rag/core/domain/parser.py` (new pure helper `normalize_iso_date_token` accepting six zero-padded forms `YYYY-MM-DD` / `YYYY/MM/DD` / `YYYY.MM.DD` / `DD-MM-YYYY` / `DD/MM/YYYY` / `DD.MM.YYYY`, with `_YYYY_FIRST_RE` + `_DD_FIRST_RE` back-reference-enforced single-separator regexes and a final `date.fromisoformat` calendar guard; `parse_note` / `_parse_iso_date` / `_split_non_empty_lines` byte-for-byte unchanged), `src/memory_rag/services/dispatcher.py` (new dispatcher-local `_normalize_note_first_line` helper using `splitlines(keepends=True)` + `dataclasses.replace` to rewrite only the first-line token in-place; wired into the `RouteKind.NOTE` branch under `if not is_heuristic:` so the legacy heuristic auto-route is byte-identical; `_format_ingest_reply` `INVALID_INPUT` reply becomes `"First line must be a date like 2026-05-09. Got: '<got>'."` — the `"Mock"` prefix is gone; `_REPLY_START` extended by one sentence naming the recommended `2026-05-09`, listing the other five accepted forms, and stating the DD-first → DD/MM/YYYY convention), `tests/test_note_parser.py` (eight new parametrized blocks covering the six-form positive whitelist, the DD/MM/YYYY convention pin `05/09/2026` → `2026-09-05`, the unpadded-reject list, the mixed-separator-reject list, the natural-language-reject list, the empty/junk-reject list, the impossible-calendar-date-reject list, and the non-string + whitespace-stripping cases; existing `parse_note` tests untouched), `tests/test_dispatcher_note_normalization.py` (new file — eleven tests covering helper behavior and dispatcher seam plus two `/start` blurb pins asserting `"2026-05-09"` and `"DD/MM/YYYY"` are present in `_REPLY_START`), `tests/test_end_to_end_smoke.py` (invalid-date assertion updated to new wording + `"Mock" not in text`; four positive smoke cases for `/note 2026/05/09`, `/note 05/09/2026` convention pin → `Saved 1 event for 2026-09-05.`, `/note 09.05.2026`, and rejected `/note 2026-5-9`; legacy-classifier regression guardrail `test_heuristic_classifier_not_broadened_by_packet_2` whose docstring explicitly frames it as a guard against accidental coupling rather than a contract assertion), `docs/RUNBOOK.md` (new "`/note` first-line date format (D-070)" subsection inside §"Command surface (D-028, D-030, D-031, D-036)" naming the six accepted forms, the DD/MM/YYYY convention pin with verbatim `05/09/2026 → 2026-09-05`, the rejected categories, the new error string verbatim, the operator script for the parent, and the explicit scope note that the legacy heuristic auto-route is not coupled and is slated for separate cleanup), `docs/decision-log.md` (D-070), `docs/todo.md` (Packet 2 flipped to done), `QUICKSTART.md` (inline curl-example error literal updated). I-5 / I-15 / R-13 / D-026 unaffected. No retrieval / scoring / answer-path / embedding / schema / migration change; no `INVARIANTS.md` / `RUNTIME-INVARIANTS.md` / `assumptions.md` / `assumption-audit.md` / `ARCHITECTURE.md` / `product/*` / `GLOSSARY.md` / `AGENTS.md` / `CLAUDE.md` / `README.md` change. A-28 deliberately not edited — the in-parser strictness is byte-for-byte preserved, so A-28's named claim about `core/domain/parser.py` is still accurate; a holistic A-28 rewrite is deferred to the legacy-classifier cleanup milestone. → Packet 2 (D-070). |
| Packet 3 — `/ask` empty-evidence NO_EVIDENCE reply gets plain-language wording + two short question-side nudges | `src/memory_rag/services/dispatcher.py` (new module-level constant `_REPLY_NO_MATCHES_TEMPLATE = "Nothing in your saved notes matched '{query}'. Try rephrasing the question, or use words that appear in your notes."` placed alongside the existing `_REPLY_PROVIDER_UNAVAILABLE` / `_REPLY_PARSE_FAILURE` constants; the empty-evidence branch of `_format_answer_reply` — `FallbackMode.NO_EVIDENCE` with non-empty `query_text` and empty `evidence` — now returns `_REPLY_NO_MATCHES_TEMPLATE.format(query=result.query_text)` instead of the prior bare `f"No memories matched '{result.query_text}'."`; `_format_answer_reply` docstring updated to describe the new empty-evidence shape; no other source line touched), `tests/test_dispatcher_retrieval_fallback.py` (empty-evidence asserts in `test_not_implemented_error_translates_to_no_evidence` and `test_llm_marker_no_evidence_distinct_from_empty_retrieval` updated to the new literal; byte-distinctness assertion in the latter continues to hold; new explicit anti-bleed guard `test_sibling_fallback_wording_unchanged` pinning the `PROVIDER_UNAVAILABLE` and `PARSE_FAILURE` reply literals so any cross-contour wording change is caught in this packet's own test diff), `tests/test_end_to_end_smoke.py` (two smoke-level empty-evidence asserts updated in `test_ask_with_no_match_returns_no_evidence_fallback` and `test_ask_before_any_note_returns_no_evidence`), `docs/RUNBOOK.md` (§"Hybrid retrieval (D-025)" empty-merged-set sentence replaced with the new wording, framed neutrally about cause), `QUICKSTART.md` (the two inline `# → text: …` example-output comments for the empty-retrieval contour — Postgres smoke and SQLite smoke — updated to the new wording), `docs/decision-log.md` (D-071), `docs/todo.md` (Packet 3 flipped to done; milestone closed). Vocabulary migration "memories" → "notes" contour-scoped to this one user-facing branch — product-prose mentions of "memory/journal core" / "Diary Memory Service" stay as-is; sibling fallback wording (empty-query `"No query text provided."`, LLM-marker NO_EVIDENCE, `WEAK_EVIDENCE`, `AMBIGUOUS`, `PROVIDER_UNAVAILABLE`, `PARSE_FAILURE`, `NONE`) byte-identical; `/start` (D-070's surface) not reopened. R-6 / I-9 / I-1 unaffected. No retrieval / scoring / answer-path / schema / migration / persistence / logging change; no `INVARIANTS.md` / `RUNTIME-INVARIANTS.md` / `assumptions.md` / `assumption-audit.md` / `ARCHITECTURE.md` / `product/*` / `GLOSSARY.md` / `AGENTS.md` / `CLAUDE.md` / `README.md` change. → Packet 3 (D-071). |

## Real-answer e2e proof rollout *(sequenced separately from the Phase/Stage axis)*
Sequenced separately from Phase 0..9 and Stages 1..3, mirroring the "Deployment-shape rollout" framing above. Under the binding owner rule, the product baseline is incomplete until at least one real-backend `/note` → retrieval → grounded-answer round-trip has been recorded as a committed evidence artifact. **REAL-1** exercises the already-wired OpenAI paths (D-024 embeddings + D-037 chat client + boot gate) under the existing OP-2 / OP-4 / OP-5 contours and produces a committed evidence artifact analogous to DEPLOY-1.7a (D-067). Reuse-only: no new resilience knob, no new aggregate, no new harness code, no behavioral change. The packet sequence is REAL-1.0 (operator-procedure prep — D-073) → REAL-1.1 (operator live run + populated dated evidence). REAL-1.0 lands the procedure + template + cross-doc registration; REAL-1.1 is the operator-execution closure step against real OpenAI credentials.
| Slice | Files / artifacts |
| --- | --- |
| REAL-1.0 operator-procedure prep | `docs/real-answer-drill/real-answer-smoke-TEMPLATE.json` (new committed evidence-file template with `"_template": true`, five top-level branches `metadata` / `preflight_state` / `note_round_trip` / `ask_round_trip` / `summary` plus `out_of_scope_for_this_packet`, `<REDACTED>` / `<TO_FILL_BY_OPERATOR>` dual placeholder convention, stable pre-filled canonical env-knob set + structural log-line shapes + expected `answer_traces` row contract), `docs/RUNBOOK.md` (new "Real-answer end-to-end smoke (REAL-1 / D-073)" `###` subsection between "Answer traces (D-034, D-035)" and "Selected-chunks recall (`/sources`, D-036)" — operator pre-conditions, numbered run procedure that reuses `QUICKSTART.md` lines 87-92 / 95-99 curl recipes unchanged in shape with the real webhook secret, evidence-file shape, redaction rule with pre-commit grep checklist, closure signal, `make check` non-impact note), `docs/decision-log.md` (D-073), `docs/todo.md` (new "Real-answer end-to-end value-loop proof (REAL-1)" milestone block). No `src/` / schema / migration / `docker-compose.yml` / `Dockerfile` / `.env.example` / `Makefile` / `pyproject.toml` / `uv.lock` / `tests/` / `scripts/` change. No live OpenAI in `make check`; no new gated test. R-6 / R-9 / R-10 / I-3 / I-9 unaffected. `INVARIANTS.md` / `RUNTIME-INVARIANTS.md` / `assumptions.md` / `assumption-audit.md` / `ARCHITECTURE.md` / `product/*` / `GLOSSARY.md` / `OPERATIONALIZATION-ROADMAP.md` / `SELF-HOSTED-DEPLOYMENT-ROADMAP.md` / `RENAMING-ROADMAP.md` / `AGENTS.md` / `CLAUDE.md` / `README.md` / `QUICKSTART.md` not touched. The next free decision number on this branch is D-073 — D-072 stays parked on the sibling `rescue/d072-doc-closure-and-routing-contract` branch per its checkpoint commit message and is not reused here. → REAL-1.0 (D-073). |
| REAL-1.1 operator live run + populated dated evidence | **Done (D-074).** Populated dated artifact `docs/real-answer-drill/real-answer-smoke-20260528-evidence.json` records the real-backend `/note` → retrieval → grounded-answer smoke with real Postgres, OpenAI embeddings (`text-embedding-3-large` @ 3072), OpenAI chat (`gpt-4.1`), dense+sparse+merged retrieval traces, and an `answer_traces` row with `fallback_mode='none'`, `prompt_version='v1'`, non-empty `context_chunk_ids`, positive `latency_ms`, and non-empty token counts. Closure flags are green: `summary.note_round_trip_green`, `summary.ask_round_trip_green`, `summary.answer_grounded`, and `summary.closes_real_1`. Closes REAL-1. |

## Stage-1 capture/routing baseline correction *(sequenced separately from the Phase/Stage axis)*
Sequenced separately from Phase 0..9 and Stages 1..3, mirroring the "Deployment-shape rollout" / "Real-answer e2e proof rollout" framing above. The Stage-1 capture/routing baseline carried a contract inconsistency: D-028 enforced the draft floor but **deliberately kept** the high-confidence plain-text NOTE/ASK auto-routes layered on top, which can silently upgrade a casual dated line or a question-shaped message into a fully-indexed NOTE / executed ASK the user never explicitly requested — misaligned with the drafts-vs-notes contract (repeatedly flagged by D-070, the RUNBOOK §"Command surface" scope note, and TechSpec §"Convenience routing"). The correction is decomposed docs-first: Packet 1 records the contract (D-078); Packet 2 brings the classifier code into compliance; Packet 3 adds the `/note`-without-explicit-date → "today" companion. D-072 stays parked on the sibling `rescue/d072-doc-closure-and-routing-contract` branch and is not reused.
| Slice | Files / artifacts |
| --- | --- |
| Packet 1 — docs/routing-contract correction | **Done (D-078).** Docs-only. `docs/decision-log.md` (D-078 — retire heuristic plain-text NOTE/ASK auto-routing; command-less plain text routes only to the draft floor; supersedes only the heuristic-routing portions of D-020 and D-028), `docs/assumptions.md` (A-16/A-17/A-28 annotations), `docs/assumption-audit.md` (A-16/A-17 struck through + closed, reconciling the standing open-row inconsistency; A-28 annotated), `docs/INVARIANTS.md` (I-14), `docs/RUNTIME-INVARIANTS.md` (R-11/R-13), `docs/ARCHITECTURE.md` (target control surface + lifecycle rules), `docs/product/PRD.md` (§6 in-scope bullet, narrow), `docs/product/TechSpec.md` (§"Convenience routing", narrow), `QUICKSTART.md` (forward-looking note only — example payloads/outputs unchanged), `docs/RUNBOOK.md` (§"Command surface" current-behavior + target-contract note; D-070 scope-note update), this map, `docs/todo.md` (new milestone block). No `src/` / `tests/` / schema / migration / config change; the live classifier still auto-routes until Packet 2. `README.md` not touched. → Packet 1 (D-078). |
| Packet 2 — classifier code change | **Done (D-079).** Collapsed the NOTE/ASK branches in `src/memory_rag/core/routing/classifier.py` so command-less plain text resolves only to `RouteKind.DRAFT` (reason `draft_floor_no_signal`); removed the now-unused `parse_note` import + `_QUESTION_WORDS` / `_TRAILING_PUNCT`. Removed the dead dispatcher machinery for the now-impossible heuristic NOTE/ASK state (`is_heuristic` local, `if not is_heuristic:` `/note` normalize-gate, `_HEURISTIC_MARKER_NOTE` / `_HEURISTIC_MARKER_ASK` + appends, `_append_marker`). Updated `tests/test_routing_classifier.py` and the dispatcher / end-to-end routing tests; repurposed the D-070 guardrail as `test_command_less_dated_plain_text_routes_to_draft_floor`; removed the four impossible-state tests (two heuristic-marker tests, the weak-evidence heuristic-marker test, the heuristic-normalize test). Refreshed `QUICKSTART.md` examples + `docs/RUNBOOK.md` notes to past tense and the `INVARIANTS.md` (I-14) / `RUNTIME-INVARIANTS.md` (R-11/R-13) trailing "later packet" clauses to D-079 enforcement. `route_source` provenance and CLARIFY unchanged; `parse_note` strictness untouched (A-28 stays open). → Packet 2 (D-079). |
| Packet 2.1 — residual doc-clause reconciliation | **Done (D-080).** Docs-only follow-up to D-078 (contract) and D-079 (code). Flipped the six residual "classifier code change deferred / lands in a later packet" clauses — `docs/assumptions.md` (A-16/A-17 pointer), `docs/assumption-audit.md` (A-16 row), `docs/product/PRD.md` (§5 "Command-less plain text → draft" paragraph + §6 in-scope bullet), `docs/ARCHITECTURE.md` ("No command → draft" line), `docs/product/TechSpec.md` (§"Convenience routing") — to the past-tense D-079 enforcement wording already used in `INVARIANTS.md` I-14 / `RUNTIME-INVARIANTS.md` R-11/R-13. Classifier enforcement is now both **recorded and enforced** consistently across all canonical docs. Also `docs/decision-log.md` (D-080) and `docs/todo.md` (Packet 2.1 line). No `src/` / `tests/` / schema / migration / config change; D-078 and D-079 historical entries untouched; A-28 stays open (Packet 3). → Packet 2.1 (D-080). |
| Packet 3 — `/note`-without-explicit-date → "today" | **Done (D-085).** The explicit `/note` dispatch path defaults a missing/unrecognized first-line date to "today" (`InboundMessage.received_at.date()`, UTC) by prepending a canonical `YYYY-MM-DD` line in `_normalize_note_first_line` (`src/memory_rag/services/dispatcher.py`), so a dateless `/note` saves under today instead of returning `INVALID_INPUT`; the `INVALID_INPUT` reply survives only for an empty/whitespace-only `/note`. `core/domain/parser.parse_note` strictness is unchanged — the default lives in the dispatcher seam, not the parser (A-28 stays open as the A-12 precursor). No clock seam / constructor change (reuses the existing `received_at` field). Tests: `tests/test_dispatcher_note_normalization.py`, `tests/test_end_to_end_smoke.py`, `tests/test_note_parser.py` (incl. sibling-wording guards on the INVALID_INPUT / Saved / draft literals). Docs: `docs/RUNBOOK.md` (§"`/note` first-line date format"), `QUICKSTART.md`, `docs/assumptions.md` + `docs/assumption-audit.md` (A-28), this map, `docs/todo.md`. **Closes the Stage-1 capture/routing baseline correction milestone.** → Packet 3 (D-085). |

## Author display-name contract (D-081) *(sequenced separately from the Phase/Stage axis)*
Pins the opaque-ID → display-name seam before any capture/render code. Author
display is resolved **only at the Telegram adapter seam** (`username →
first_name → opaque short-ID`, host-supplied and non-authoritative); the core
carries only the opaque `author_user_id` (I-1 / I-6 unchanged; A-44). Docs-only;
no `src/` / `tests/` / schema change.
| Slice | Files / artifacts |
| --- | --- |
| Packet 1 — display-name contract + initial `/sources` surface | **Done (D-081).** Docs-only: `docs/decision-log.md` (D-081), `docs/assumptions.md` (A-44) + `docs/assumption-audit.md` (A-44 row), `docs/GLOSSARY.md` ("author display name"), `docs/ARCHITECTURE.md` (Axis 5 + adapter bullets), `docs/product/TechSpec.md` (§5 authorship note), `docs/RUNBOOK.md` (`/sources` forward note), `docs/INVARIANTS.md` (I-6 cross-ref — no new invariant), this map, `docs/todo.md`. `/sources` is the sole milestone display surface; answer-reply attribution deferred. No `src/` / `tests/` / schema / migration change. → Packet 1 (D-081). |
| Author display-input persistence shape + capture contract | **Done (D-082).** Docs-only decision (Option 3 hybrid): minimal snapshot-oriented persistence shape + capture contract — the adapter may snapshot host-supplied `username` / `first_name` at ingest, **nullable**, **non-authoritative**, **adapter/storage-owned**, for later adapter-side display resolution only; never a core field. Adapter-side identity directory/projection deferred to a later group-use milestone. `docs/decision-log.md` (D-082), this map, `docs/assumptions.md` + `docs/assumption-audit.md` (A-44 narrowed, stays open), `docs/todo.md`, `docs/product/TechSpec.md` (§5), `docs/ARCHITECTURE.md` (Axis 5 + adapter bullets), `docs/GLOSSARY.md` ("author display input"), `docs/INVARIANTS.md` (I-6 cross-ref — no new invariant). No `src/` / `tests/` / schema / DDL / migration / config change; code/migration is a later packet. → D-082. |
| Adapter-owned storage port + side-table seam | **Done (D-083).** Docs-only decision of record (Option A): the display-input snapshot lands in a separate Telegram-adapter-owned side table written through an adapter-owned storage port distinct from the core `DomainRepository`, keyed by the message idempotency tuple `external_chat_id + external_message_id + edit_seq` as opaque scalars; the snapshot never enters `InboundMessage` / `SourceMessage` / any core type or core function signature (D-026 / D-041 boundary preserved; open-source shared-core rule). Snapshot stays nullable / non-authoritative / host-supplied / resolution-only. Migration surface named descriptively only (next file in `storage/postgres/migrations/`). `docs/decision-log.md` (D-083), this map, `docs/assumptions.md` + `docs/assumption-audit.md` (A-44 narrowed, stays open), `docs/todo.md`, `docs/product/TechSpec.md` (§5), `docs/ARCHITECTURE.md` (Axis 5 + adapter bullets), `docs/GLOSSARY.md` ("author display input"), `docs/INVARIANTS.md` (I-6 cross-ref — no new invariant). No `src/` / `tests/` / schema / DDL / migration / config change; code/migration is the next packet. → D-083. |
| Author display-input capture + durable landing | **Done (D-084).** Code packet: `TelegramUser` carries nullable `username` / `first_name`; the webhook snapshots them on source-message-bearing routes (note/draft) straight from `message.from_` (never a core type), best-effort. New adapter-owned `AuthorDisplayInputStore` port co-located on the per-backend store (distinct from `DomainRepository`), new `author_display_inputs` side table (mock / sqlite / postgres parity + forward migration `0004`), keyed by the message idempotency tuple as opaque scalars; R-2 idempotency (re-delivery no-op, edits new row), both-null recorded. `src/memory_rag/adapters/telegram/{author_display.py,models.py,webhook.py}`, `storage/{mock,sqlite,postgres}` stores + migration, `tests/test_author_display_{capture,boundary}.py`, `tests/test_telegram_models.py`; `decision-log.md` (D-084), `assumptions.md` + `assumption-audit.md` (A-44 → capture/landing implemented, stays open), this map, `todo.md`. → D-084. |
| Author display-name resolution + `/sources` rendering | **Done (D-086).** Adapter-side resolution helper (`@username → first_name → opaque `user-<last8>` floor`) over the D-084 snapshot, bridging the opaque boundary via `get_source_message` to recover the keying tuple; `/sources` renders a `— <author>` line per block, resolution adapter-only (mirrors the `/drafts` return-domain-objects pattern). `DispatchResult.source_blocks` → `source_chunks` (the dispatcher returns opaque chunks; the adapter renders + resolves); dispatcher `_render_source_block` removed; new `get_backend_store` dependency. `src/memory_rag/adapters/telegram/{author_display.py,webhook.py}`, `core/routing/models.py`, `services/dispatcher.py`; `tests/test_author_display_resolution.py` (new), `tests/test_{telegram_sources,dispatcher_sources,author_display_boundary}.py`; `decision-log.md` (D-086), `RUNBOOK.md`, `assumptions.md` + `assumption-audit.md` (**A-44 resolved**), this map, `todo.md`. No core repo/type signature, schema, or migration change. **Closes A-44** (answer-reply attribution remains a separate deferred placeholder). → D-086. |
| Answer-reply contributor-attribution contract (docs-only) | **Done (D-091).** Docs-only contract for the `/ask` reply: contributor set = distinct authors (by opaque `author_user_id`) of the answer's grounding chunks (`AnswerResult.context.ordered_chunks` = `AnswerTrace.context_chunk_ids`); dedup by `author_user_id`, first-appearance order; render shape = a labeled `Contributors: …` footer line beneath `answer_text`, rendered iff the grounding set is non-empty; seam = ASK `DispatchResult` carries opaque grounding chunks (mirroring `source_chunks`) and the Telegram adapter resolves + renders via `resolve_chunk_author_display` (adapter-only, requester-`community_id`-scoped per D-089; I-1 / I-6 / I-7 preserved). `decision-log.md` (D-091), this map, `todo.md`, `assumptions.md` + `assumption-audit.md` (A-44 forward cross-ref — stays resolved, not reopened), `RUNBOOK.md` (`/sources` forward note), `INVARIANTS.md` (I-6 cross-ref — no new invariant). No `src/` / `tests/` / schema / migration change; surfaced by the code packet (D-092). **The user-facing footer this contract defined was later withdrawn — see D-101 (Packet C); `/sources` is now the sole attribution surface.** → D-091. |
| Answer-reply contributor-attribution rendering (code) | **Done (D-092).** Code packet enforcing D-091: the ASK `DispatchResult` carries the opaque grounding chunks on a new `grounding_chunks` field (parallel to `source_chunks`, set iff the grounding set is non-empty), and the Telegram adapter resolves the distinct contributors (deduped by opaque `author_user_id`, first-appearance order) and appends a `Contributors: …` footer beneath `answer_text` via the new adapter-only `render_contributors_footer` (reusing `resolve_chunk_author_display`, requester-`community_id`-scoped per D-089). Dispatcher stays channel-neutral (owns only `answer_text` + opaque chunks). `src/memory_rag/core/routing/models.py`, `services/dispatcher.py`, `adapters/telegram/{author_display.py,webhook.py}`; `tests/test_author_display_resolution.py`, `tests/test_telegram_ask_contributors.py` (new), `tests/test_dispatcher_retrieval_fallback.py`; `decision-log.md` (D-092), `RUNBOOK.md`, `todo.md`, this map. No core repo/type signature, schema, or migration change; `/sources` rendering unchanged. I-1 / I-6 / I-7 / R-3 preserved. **Out of scope:** A-15 visibility, A-14 multi-diary, per-claim `cited_chunk_ids` citation attribution, `author_display`/`/sources` seam-shape change. **Footer removed as a user surface by D-101 (Packet C); `render_contributors_footer` + `DispatchResult.grounding_chunks` deleted.** → D-092. |

## Grouped & multi-diary (community bootstrap & membership, D-093) *(sequenced separately from the Phase/Stage axis)*
Ratifies grouped-diary + multi-diary-on-one-instance on the existing
community-scoped data plane (Slice 8.1). The mechanics already work — a Telegram
group chat is one `community_id` with distinct per-sender `author_user_id`
(I-6), distinct chats are isolated communities on one instance (I-7 / R-3 /
R-8) — so this milestone pins the contract, makes the chat→community mapping a
single adapter-owned seam, and characterizes the behavior. Decomposed docs-first
in `docs/GROUPED-MULTI-DIARY-ROADMAP.md` (D-093); packets G-0..G-4 below. A-14
is **closed → D-093** for the community half; subject/child bootstrap is carved
out to **A-45** (since resolved at the contract level by **D-097** — see the
Subject-scoping milestone below); A-15 visibility is deferred and sequenced after
the first grouped pilot. **Milestone closed by G-3 (D-096): G-0..G-3 landed (D-093 / D-094
/ D-095 / D-096); G-4 deferred.** **Out of scope:** core participant/ACL/registry,
`/setup`, visibility model (A-15 / Slice 8.2), subject bootstrap (A-45), DEPLOY-2,
any schema/DDL/migration.
| Slice | Files / artifacts |
| --- | --- |
| G-0 — contract + assumption split + roadmap | **Done (D-093).** Docs-only: `docs/decision-log.md` (D-093), new `docs/GROUPED-MULTI-DIARY-ROADMAP.md`, `docs/assumptions.md` + `docs/assumption-audit.md` (close A-14 → D-093, open A-45, sequence A-15), this map row + note block, `docs/todo.md`; cross-reference-only touches to `docs/INVARIANTS.md` (I-6 / I-7), `docs/RUNTIME-INVARIANTS.md` (R-3 / R-8 / R-14), `docs/RUNBOOK.md`, `docs/product/TechSpec.md` (§5), `docs/ARCHITECTURE.md` (Axis 5). Ratifies implicit-on-first-message bootstrap, the 1:1 `external_chat_id → community_id` adapter-axis mapping (no longer a "surrogate"), membership inherited from host-chat membership (no core ACL), and multi-diary-on-one-instance as a core capability. No `src/` / `tests/` / schema / migration change. → G-0 (D-093). |
| G-1 — consolidate the chat→community resolver | **Done (D-094).** New **adapter-owned** `adapters/telegram/community.py` `resolve_community_id` (default 1:1) is the single chat→community seam; the resolved opaque `community_id` crosses the boundary on the new required `InboundMessage.community_id`, set by the webhook edge. The core reads `message.community_id` everywhere it previously derived scope from `external_chat_id` — the real surface was broader than the audit's "three sites": 6 core/services derivations (`domain_service` `_community_id_for` deleted; `query_service`; `dispatcher` ×4) + 2 `webhook.py` copies. `external_chat_id` stays as the transport / R-2 idempotency key only. Behavior-preserving (default 1:1); strengthens I-1. Tests: `community_id` on 8 `InboundMessage` factories + new `tests/test_telegram_community_resolver.py`. No `src/` behavior / schema / migration change. → G-1 (D-094). |
| G-2 — grouped + multi-diary regression suite | **Done (D-095).** New consolidated `tests/test_grouped_multi_diary.py` (6 tests, mock-mode) pinning already-true behavior through the G-1 `resolve_community_id` seam: group chat → one `community_id` + distinct `author_user_id` per sender; grouped `/ask` preserves ≥2 contributors into `AnswerResult.context.ordered_chunks` (I-6) and the ASK dispatch `grounding_chunks` seam (D-091); N distinct chats → N isolated communities; cross-community `/sources`-cache isolation at grouped granularity (composing with `tests/test_read_access_isolation.py` and `tests/test_dispatcher_sources.py`). Mock-mode only; PG/sqlite storage-read parity stays with the existing PG-gated suites. No `src/` / schema / migration change. → G-2 (D-095). |
| G-3 — operator + product docs | **Done (D-096).** New `docs/RUNBOOK.md` "Grouped & multi-diary on one instance" how-to (bootstrap/mapping/membership + running multi-diary on one instance); reconciled `docs/ARCHITECTURE.md` Axis-5 prose (named the `resolve_community_id` seam, D-094) + `docs/product/TechSpec.md` §5 (marked grouped/multi-diary **supported**); flipped the roadmap `Status:` / §6 / this row + note block / `docs/todo.md` to milestone-closed. Docs-only — no `src/` / `tests/` / schema / migration change. → G-3 (D-096). |
| G-4 — visibility model (A-15 / Slice 8.2) *(deferred, not built)* | Enumerate `visibility_scope` + per-note / per-participant read control **after** the first grouped pilot. Out of scope of this milestone. |

## Subject scoping (`subject_id` dimension, D-097) *(sequenced separately from the Phase/Stage axis)*
Adds a subject-scoping dimension on top of the closed community-scoped data plane
(Milestone G): an opaque, community-scoped, **nullable** `subject_id` on `Note` /
`EventChunk`, assigned by an adapter-axis function with a default single-subject
mapping (`null` = community-wide), with an **optional** retrieval filter mirroring
the D-040 `date_range` seam. Decomposed docs-first in
`docs/SUBJECT-SCOPING-ROADMAP.md` (D-097); packets H-0..H-4 below. **A-45 is
closed → D-097** at the contract level; community scoping (I-7 / R-3 / R-8) is the
unchanged outer boundary. **Status: closed — H-0 (D-097), H-1 (data model),
H-2 (adapter-axis assignment), H-3 (retrieval filter, D-107), and H-4 (characterization suite + operator/product docs) landed; milestone closed at H-4.** **Out of scope:** core subject registry/entity, explicit
subject-selection command / multi-subject UX, A-15 visibility (separate; Slice 8.2
/ G-4), reopening Milestone G.
| Slice | Files / artifacts |
| --- | --- |
| H-0 — subject-scoping contract + A-45 resolution + roadmap | **Done (D-097).** Docs-only: `docs/decision-log.md` (D-097), new `docs/SUBJECT-SCOPING-ROADMAP.md`, `docs/assumptions.md` + `docs/assumption-audit.md` (close A-45 → D-097; A-15 clarified, stays open), this map block; cross-reference-only touches to `docs/product/TechSpec.md` (§5), `docs/GLOSSARY.md`, `docs/RUNBOOK.md`. Ratifies the opaque / community-scoped / nullable `subject_id` contract, adapter-axis assignment with a default single-subject mapping, no core subject registry, an optional retrieval filter mirroring D-040, and orthogonality to A-15. No `src/` / `tests/` / schema / migration change. → H-0 (D-097). |
| H-1 — `subject_id` in the data model | **Done (H-1).** Nullable, opaque `subject_id` on `Note` / `EventChunk` (`core/domain/models.py`), round-tripped through the mock / sqlite / postgres stores, plus the non-destructive `0005.subject-id-columns` Postgres migration (default `null`; community scoping unchanged). No assignment / retrieval change yet. |
| H-2 — adapter-axis subject assignment | **Done (H-2).** New adapter-owned `adapters/telegram/subject.py` `resolve_subject_id(community_id) -> str \| None` (default single-subject mapping returns `None` = community-wide, parallel to `resolve_community_id`); the resolved opaque `subject_id` crosses on the new defaulted `InboundMessage.subject_id` field (set at the webhook edge); `domain_service.ingest` carries it onto `Note` / `EventChunk`. Behavior- and data-preserving under the default (persisted `subject_id` stays `null`); no schema / migration change (H-1's nullable column already round-trips it). Tests: `tests/test_telegram_subject_resolver.py` (new), `tests/test_domain_service.py` (default-`None` + non-`None` threading guards). No assignment-divergence or retrieval change yet. |
| H-3 — optional subject retrieval filter | **Done (H-3 / D-107).** Keyword-only `subject_scope: str \| None = None` on both `storage/search_repository.py` legs, mirroring the D-040 `date_range` seam: **strict match** (`subject_id = scope`; community-wide `NULL` rows excluded, reachable only via the default `None` = no constraint), composing with `date_range` as a conjunction inside the unchanged community boundary (I-7 / R-3). Postgres splices a conditional `AND ec.subject_id = %s` fragment on both legs (`_subject_scope_sql`, parallel to `_date_range_sql`); mock applies the identical predicate (`_chunk_in_subject_scope`); sqlite is signature-only parity (retrieval still `NotImplementedError`, D-022 / D-025). `QueryService.answer` gains the per-call keyword-only `subject_scope`, threads it to both legs, and records it on the persisted `Query` row via the new nullable `Query.subject_scope` field + `queries.subject_scope` column (non-destructive `0006.query-subject-scope` Postgres migration; sqlite DDL carries the same H-1-style no-runner upgrade asymmetry). No inbound subject syntax — the webhook/dispatcher call site is unchanged. Tests: subject-filter blocks in `tests/test_search_repository_mock.py` + (PG-gated) `tests/test_search_repository_postgres.py`; threading / persisted-row / fail-closed-`NO_EVIDENCE` cases in `tests/test_query_service.py`; cross-store round-trips in `tests/test_storage_query_traces.py`; `0006` discovery + upgrade in `tests/test_postgres_migrations.py`. → H-3 (D-107). |
| H-4 — regression suite + operator/product docs + closure | **Done (H-4; enforces D-097 / D-107, no new decision-log entry; pre-checkpoint).** New consolidated `tests/test_subject_scoping.py` — end-to-end characterization at `QueryService` granularity **through both adapter seams** (every `InboundMessage` built with `community_id=resolve_community_id(chat)` and, on the default path, `subject_id=resolve_subject_id(community_id)`; explicit non-`None` values simulate a divergent mapping), parametrized over **mock + PG-gated postgres** (the first service-level ingest→answer runs against Postgres; sqlite excluded — retrieval `NotImplementedError`, D-022/D-025). Pins: resolver-default ingest persists community-wide rows retrievable only via the default `None` scope; a scoped `answer()` strict-matches one subject end-to-end and records the scope on the persisted `Query` row; a named scope with zero matches fails closed to `NO_EVIDENCE` (scope still recorded); `subject_scope` composes with `date_range` as a conjunction; the same `subject_id` string in two communities stays isolated (I-7 / R-3 unchanged). Composes with (does not re-run) the per-packet suites — indexed in its module docstring. **Docs:** new `docs/RUNBOOK.md` "Subject scoping within a community (D-097, D-107)" operator section (+ the grouped-section forward note repointed from "code is future" to landed); `docs/product/TechSpec.md` §5 flipped to **supported** + the forecast field-list reconciliation (`child_id` → `subject_id` on `Note`/`EventChunk`, `child_scope` → `subject_scope` on `Query`, §7 metadata `child_id` → `subject_id`); `docs/ARCHITECTURE.md` Axis-5 names the `resolve_subject_id` seam; `docs/GLOSSARY.md` landed flip; `docs/SUBJECT-SCOPING-ROADMAP.md` Status/§2/§4/§6 + this block + `docs/todo.md` → milestone-closed (conditional on this packet landing). No `src/` / schema / migration / config change. **Closes the milestone.** → H-4 (enforces D-097 / D-107). |

## Evidence-faithful answer & source attribution (D-098) *(sequenced separately from the Phase/Stage axis)*
Makes `/ask` answers and `/sources` report only the evidence the LLM actually
used (its `cited_chunk_ids`), not every retrieved chunk, and present authors as
human names rather than opaque numeric ids. Root cause: `QueryService.answer`
parses `structured.cited_chunk_ids` (a subset of `AnswerContext.ordered_chunks`,
I-9) but discards it, so `/sources` (`_update_latest_sources`) and the
`Contributors:` footer (D-091 / D-092) both render the full retrieved set.
| Slice | Files / artifacts |
| --- | --- |
| Packet 1 — expose `cited_chunk_ids` on the answer/result seam | **Done (D-098).** Additive `AnswerResult.cited_chunk_ids: tuple[str, ...] = ()` carrying the LLM's used-evidence set, distinct from the full retrieved `context_chunk_ids`; threaded through the single `QueryService._finalize` convergence point and set explicitly at all five contours (`()` on empty-query / empty-merged / `PROVIDER_UNAVAILABLE` / `PARSE_FAILURE`; `structured.cited_chunk_ids` on the graded branch). No consumer reads it yet — pure plumbing, reply rendering byte-unchanged. `core/domain/models.py`, `services/query_service.py`; `tests/test_query_service.py` (cited-set characterization incl. a strict-subset fidelity case via a test-only `_MarkerChatClient` `cited_subset_size` knob); `decision-log.md` (D-098), this map. No schema / DDL / migration / config change; `AnswerTrace.context_chunk_ids` still records the full context set. I-9 / R-5 preserved. **Out of scope:** `/sources` cited-only rendering (Packet 2), `Contributors:` footer removal (Packet 3), `/drafts` author names (Packet 4), persisting the cited subset, `DispatchResult`/adapter threading. → Packet 1 (D-098). |
| Packet A — ratify the `/ask` no-evidence guardrail | **Planned (D-099) — next packet; pre-checkpoint.** Docs-only ratification of the contract "if `cited_chunk_ids` is empty, `/ask` returns an explicit technical no-evidence response and MUST NOT surface free-form `answer_text`"; cited-empty reading only (semantic-groundedness deferred to Phase 7). Cross-reference clauses on I-9 + R-6 → D-099 (no new invariant, no semantic change); no `src/` / `tests/` / schema / migration / config change; no runtime behavior change. Status flips to **Done** only after execution / report / checkpoint. → Packet A (D-099). |
| Packet B — `/sources` renders only the cited chunks | **Implemented (D-100) — pre-checkpoint.** `/sources` consumes `AnswerResult.cited_chunk_ids` (D-098) instead of `context.ordered_chunks`: `Dispatcher._update_latest_sources` caches the cited subset of `ordered_chunks` (post-RRF order) and the cache write is now always-set (the `.pop`-on-empty clear path removed), so key presence records that an `/ask` ran; `_dispatch_sources` branches on key presence — absent → `_REPLY_SOURCES_NONE` (unchanged); present-but-empty → new `_REPLY_SOURCES_NONE_CITED` "Your last /ask answer didn't cite any specific notes." (every cited-empty contour per D-099); present-non-empty → the cited chunks. The D-099 guard test lands in `tests/test_dispatcher_sources.py` (parametrized over the five cited-empty contours, asserting no free-form `answer_text` leaks; reply bodies stay pinned by the D-071 guards in `tests/test_dispatcher_retrieval_fallback.py`). `src/memory_rag/services/dispatcher.py`; `tests/test_dispatcher_sources.py` (cited-only + both empty wordings + guard), `tests/test_telegram_sources.py` (empty-cited inline delivery; `_FixedAnswerQueryService` sets `cited_chunk_ids`); `decision-log.md` (D-100), `RUNBOOK.md` §"Selected-chunks recall (`/sources`, D-036)". `AnswerTrace.context_chunk_ids` still records the full retrieved set; I-9 / R-6 preserved. No `QueryService`/`AnswerResult` change, no `Contributors:` footer change (Packet C), no schema / migration / config change. Status flips to **Done** only after report / checkpoint. → Packet B (D-100). |
| Packet C — remove the all-retrieved `Contributors:` footer | **Implemented (D-101) — pre-checkpoint.** Removes the D-091 / D-092 `Contributors:` footer as a user-facing element (owner chose full removal over re-keying to the cited subset): the webhook no longer appends the footer, and the footer-only seam is deleted — `DispatchResult.grounding_chunks` (field + `Dispatcher` ASK derivation) and `render_contributors_footer` are gone; the shared author resolvers + `render_source_block` stay (`/sources` uses them). `/sources` (cited-only, D-100) is now the sole user-facing attribution surface; grounded `/ask` replies are the answer text alone. Supersedes the *user-facing rendering* of D-091 / D-092; no invariant change (I-1 / I-6 / I-7 preserved); `AnswerTrace.context_chunk_ids` still records the full retrieved context. `src/memory_rag/{core/routing/models.py,services/dispatcher.py,adapters/telegram/{author_display.py,webhook.py}}`; `tests/test_telegram_ask_contributors.py` (presence → absence guard), `tests/test_author_display_resolution.py` (drop footer units), `tests/test_end_to_end_smoke.py`, `tests/test_grouped_multi_diary.py` (drop the `grounding_chunks` seam test; multi-author I-6 retained via `context.ordered_chunks`), `tests/test_dispatcher_retrieval_fallback.py` (drop `grounding_chunks` asserts; retain a core "no footer" guard); `decision-log.md` (D-101), `RUNBOOK.md`, `INVARIANTS.md`, this map. No schema / migration / config change. Status flips to **Done** only after report / checkpoint. → Packet C (D-101). |
| Packet D — human author names in `/drafts` | **Implemented (D-102) — pre-checkpoint.** `/drafts` renders the author via the existing D-086 ladder (`@username → first_name → opaque `user-<last8>` floor`) instead of the raw opaque `author_user_id`, and the block header is trimmed to `📝 <iso> · <author>` (the prior `author:<id>` / `id:<short>` technical segments dropped). New **internal** adapter helper `adapters/telegram/author_display._resolve_source_author_display(source, store, *, community_id)` reads the snapshot directly off the draft `SourceMessage`'s own `(external_chat_id, external_message_id, edit_seq)` tuple — no `get_source_message` bridge — and applies the existing public `resolve_author_display_name`; `community_id=inbound.community_id` is a defensive D-089 scope check (foreign community → opaque floor, no snapshot read). No public `author_display` seam / store-protocol change; authorship still carried only as opaque `author_user_id` (I-1 / I-6 / I-7 preserved). `adapters/telegram/{author_display.py,webhook.py}` (`_render_draft_block` + the `/drafts` call site); `tests/test_author_display_resolution.py` (helper unit section), `tests/test_telegram_drafts.py` (`get_backend_store` override + snapshot seeding + three-tier integration + byte-stable format); `decision-log.md` (D-102), `RUNBOOK.md` (new `/drafts` author-display subsection). No schema / migration / config change. Status flips to **Done** only after report / checkpoint — at which point the milestone's Packet 1/A/B/C/D rows close together. → Packet D (D-102). |

## Post-attribution user-surface cleanup (D-103) *(sequenced separately from the Phase/Stage axis)*
Trims three user-facing surfaces left rough by the Evidence-faithful attribution
milestone (D-098 → D-102): the draft-save confirmation, the `/drafts` header, and
the `/sources` header. Product-baseline surface polish (Stage-1 / Phase-4 surfaces),
sequenced ahead of resuming Subject scoping (H-2..H-4). Branch
`fix/post-attribution-surface-cleanup`. **Out of scope:** retrieval / answer-path /
author-resolution behavior, subject scoping, schema / migrations / core models.
| Slice | Files / artifacts |
| --- | --- |
| Packet 1 — trim the draft-save confirmation reply | **Done (D-103).** `services/dispatcher.py` `_format_draft_reply` drops the `/note` + `/ask` hint (returns `f"{_DRAFT_REPLY_PREFIX}{suffix}."`); the unused `_DRAFT_REPLY_HINT` constant is deleted. Fresh store → `"Stored as draft."`; idempotent replay → `"Stored as draft (replay)."` (R-2 provenance suffix kept, owner-decided). `tests/test_telegram_reply.py` (two assertions tightened to exact equality + new `test_draft_reply_wording_and_sibling_literals_are_pinned` byte-equality guard pinning fresh/replay replies, the removed-hint-absent-everywhere check, and the sibling literals `_REPLY_START` / `_REPLY_HELP` / `_REPLY_UNKNOWN` / `_REPLY_CLARIFY`), `tests/test_end_to_end_smoke.py` (`"/note" in …` draft assertion → exact equality); `decision-log.md` (D-103), `QUICKSTART.md` (three example outputs + the now-false promote sentence). No schema / migration / config change; R-2 preserved. → Packet 1 (D-103). |
| Packet 2 — `/drafts` header: date + author only | **Done (D-104).** `adapters/telegram/webhook.py` `_render_draft_block` renders the header date-only: `draft.created_at.isoformat()` → `draft.created_at.date().isoformat()`, so `📝 <created_at full-ISO> · <author>` (D-102) becomes `📝 <created_at date> · <author>` (`YYYY-MM-DD`), dropping the datetime + timezone suffix. Author resolution (D-086 ladder, requester-`community_id`-scoped per D-089), the `· ` separator, blank-line layout, and verbatim draft body are byte-unchanged. Supersedes only the header-format half of D-102. `tests/test_telegram_drafts.py` (the `test_render_draft_block_format_is_byte_stable` literal updated to the date-only header; the nine sibling tests unchanged); `decision-log.md` (D-104), `RUNBOOK.md` (`/drafts` author-display header literal). No schema / migration / config change. → Packet 2 (D-104). |
| Packet 3 — remove the `/sources` header line | **Done (D-105).** `services/dispatcher.py` `_dispatch_sources` drops the `Selected chunks for your last /ask (N chunk(s)):` header on the populated branch (returns `reply_text=""`); the populated `/sources` reply is now the source blocks alone, the chunk count being implicit in the per-block `(i/N)` indices. The empty header is absorbed by the adapter's `pack_drafts_into_messages` (empty header → no leading blank message; the populated branch always carries ≥1 block), so no adapter/packer code changes and `/drafts` packing is unaffected. Both empty `/sources` contours (`_REPLY_SOURCES_NONE`, `_REPLY_SOURCES_NONE_CITED`), block format, `(i/N)` indices, cited-only selection (D-098 / D-100), author resolution (D-086), `_latest_sources` cache, and `metadata` (incl. `returned`) are byte-unchanged. `tests/test_dispatcher_sources.py` (two header asserts → `reply_text == ""`; the two empty-contour literals stay byte-pinned), `tests/test_telegram_sources.py` (two outbound-body asserts reworked to expect blocks-first output + a negative assert the removed header appears nowhere); `decision-log.md` (D-105). No schema / migration / config change; no new I-/R- number. Supersedes only the header-presence half of D-036. **Milestone closed.** → Packet 3 (D-105). |

## Single-chunk `/note` capture (D-106) *(sequenced separately from the Phase/Stage axis)*
Product-baseline closure of a hidden Telegram diary bug: an explicit `/note` whose
payload contains newlines is split into one `EventChunk` per line. The owner-confirmed
contract is **one explicit `/note` = exactly one logical chunk**; newlines are content
structure, not event separators; per-line splitting is retired (supersedes the per-line
half of D-005 / the original I-5). Decomposed docs-first: Packet 1 ratifies the contract
(revised I-5, CLAUDE.md rule #5, product docs); Packet 2 brings the parser + `domain_service`
code into compliance and reconciles the two user-facing/eval surfaces the code change made
inconsistent (the `/note` reply wording and a mechanical gold-handle collapse to `#0`, both
owner-approved). Branch `feat-note-capture-single-chunk`. **Milestone closed** at Packet 2.
**Out of scope:** any batch / multi-event capture command or separator contract; broader eval
corpus redesign / retrieval-quality re-baselining (only the mechanical `#1`/`#2` → `#0` gold
collapse was pulled in to keep the gate green); retroactive re-chunking; schema / migrations /
core models.
| Slice | Files / artifacts |
| --- | --- |
| Packet 1 — revise I-5 to single-chunk `/note` (docs-only) | **Done (D-106), pre-checkpoint.** Docs-only ratification of the owner-confirmed contract. `docs/decision-log.md` (new D-106, supersedes the per-line half of D-005 — D-005 not rewritten); `docs/INVARIANTS.md` I-5 retitled "One explicit `/note` is one chunk" and rewritten (I-12 reviewed — replayability is granularity-independent, no change); `CLAUDE.md` preserved-rule #5; `docs/product/TechSpec.md` §6 Chunking Contract; `docs/product/PRD.md` §5 note-capture shape + §6 MVP functional scope; this map row; `docs/todo.md`. `docs/RUNTIME-INVARIANTS.md` / `docs/RUNBOOK.md` untouched (no per-line contract assertion; the RUNBOOK eval `event_index` handle is harness mechanics, not the `/note` contract). No `src/` / `tests/` / schema / migration / config change; no new I-/R- number. → Packet 1 (D-106). |
| Packet 2 — retire per-line splitting (code) | **Done (enforces D-106; pre-checkpoint).** Brings the runtime into compliance with D-106 / revised I-5 — no new decision-log entry (enforcement of an existing decision; no invariant change). **Code:** `core/domain/parser.py` — `ParsedNote.events: list[str]` → `body: str` (the non-empty body lines after the date joined by `\n`, `""` for a date-only note); `parse_note` stops per-line splitting (first-line date strictness + `normalize_iso_date_token` byte-unchanged). `services/domain_service.py` — one `EventChunk` (`event_index=0`, `chunk_text=parsed.body`) when the body is non-empty, none for a date-only note; `note_text=parsed.body` reconciles the join site (note_text == chunk_text); `IngestResult.events_count` now carries the chunk count (0 or 1) — field name retained (renaming would be an out-of-scope model-shape change). **User-facing reply (owner-approved expansion):** `services/dispatcher.py` `_format_ingest_reply` reworded to note vocabulary — a pure wording change of the two existing success branches (`"Saved your note for {date}."`; date-only/no-content `"Saved your note for {date} with no content."`); `INVALID_INPUT` branch byte-identical; which inputs succeed is unchanged. **Eval gold reconciliation (owner-approved expansion):** `eval/retrieval/gold.json` + `eval/retrieval/observability/gold.json` — every `#1`/`#2` expected-handle collapsed to `#0` (no within-query same-message collisions, so no dedup needed) and the `_handle_contract` strings updated to the single-chunk semantics; `corpus.jsonl` files unchanged. Required because `make check` resolves every committed gold handle end-to-end (`run_harness` raises on a miss). `chunk_text` keeps the existing `_split_non_empty_lines` whitespace normalization (the contract is "no splitting into separate chunks", not byte-verbatim whitespace). **Tests:** `tests/test_note_parser.py` (`.events` → `.body`), `tests/test_domain_service.py` (single-chunk guard + chunk/embedding counts), `tests/test_query_service.py` (multi-chunk fixtures rebuilt as separate single-line notes), `tests/test_dispatcher_note_normalization.py` + `tests/test_end_to_end_smoke.py` (reply literals + chunk counts), `tests/test_retrieval_harness_shape.py` (inline handle set → `{"t-1#0"}`). **Docs:** `docs/RUNBOOK.md` (operator script + D-085 default + eval-handle contract), `QUICKSTART.md` (reply/chunk-count examples), this map row, `docs/todo.md`. No schema / DDL / migration / config change; canonical I-5 / TechSpec §6 / PRD §5–6 / CLAUDE.md already correct from Packet 1; no new I-/R- number. Full gate green (ruff + mypy + 700 passed / 68 PG-skipped). **Closes the milestone.** → Packet 2 (enforces D-106). |

## Routed chat core (D-108) *(sequenced separately from the Phase/Stage axis)*
Adds a routed conversational entry point to the functional core (owner-provided
milestone spec, ratified by D-108): an incoming natural-language question is
classified into one of four routes — `diary_lookup` / `diary_plus_llm` /
`diary_plus_web` / `general_llm` (product register; core identifiers
`notes_lookup` / `notes_plus_model` / `notes_plus_knowledge` / `model_only`,
D-041 dual-register mapping) — and answered under explicit per-segment
provenance labeling (notes / web / model; I-9 generalized). Router at the
service seam (I-1); hand-rolled dispatch; new `/chat` command; classifier =
OpenAI function-calling `gpt-4.1-mini`; knowledge source = narrow core port +
hardened Tavily adapter. Decomposed docs-first in `docs/ROUTED-CHAT-ROADMAP.md`
(D-108); packets RC-1..RC-4 below. **Explicit owner override of default
pick-next sequencing: A-10 edit/delete (row 2.5) is re-queued as the milestone
immediately after this one.** **Out of scope:** A-21 / Phase 9 host seams, web
UI, theygrow embedding, thematic tags, knowledge graphs, memory synthesis,
voice/photo, multi-agent orchestration, edit/delete mechanics, the explicit
`/ask` filter-syntax item, the curated domain-knowledge provider.
| Slice | Files / artifacts |
| --- | --- |
| RC-1 — docs-first contract + owner-override recording | **Done (D-108), pre-checkpoint.** Docs-only ratification: `docs/decision-log.md` (new D-108 — route taxonomy + dual-register naming, placement, `/chat` control surface, `gpt-4.1-mini` classifier pin, Tavily knowledge-source decision, generalized-I-9 provenance model, amended PRD medical non-goal + escalation prompt invariant, no-thresholds failure/fallback policy, contract-level trace extension, owner override + A-10 re-queue); new `docs/ROUTED-CHAT-ROADMAP.md` (RC-1..RC-4 ladder); `docs/INVARIANTS.md` (I-9 appended provenance-class clause, enforcement with RC-2..RC-4); `docs/product/PRD.md` (§6 medical bullet amended); `docs/product/TechSpec.md` §4 (`/chat` target line, lands with RC-2); `docs/GLOSSARY.md` (route mapping); `docs/assumptions.md` + `docs/assumption-audit.md` (A-10 re-queue annotations — A-10 stays open); this map block + the 2.5-row annotation; `docs/todo.md`. `docs/RUNTIME-INVARIANTS.md` untouched (R-7 already names the search backend; routed-chat runtime clauses become true only when the code packets land). No `src/` / `tests/` / schema / migration / config change; no new I-/R- number. → RC-1 (D-108). |
| RC-2 — classifier + dispatcher + two routes | **Done (D-109), pre-checkpoint.** **Code:** new core package `core/chat/` (`ChatRoute` enum with the four canonical identifiers, `ChatRouteClassifier` Protocol + failure split, `ChatRouteDecision` trace entity, `model-only-v1` prompt contract with lenient parse); new adapter package `adapters/chat_routing/` (factory + mock + OpenAI function-calling classifier on the canonical `gpt-4.1-mini` pin, D-047/D-049 hardening, shared `provider_*` knobs); new `services/routed_chat.py` (hand-rolled dispatch at the service seam: `diary_lookup` delegates to the existing grounded ask unchanged; `general_llm` answers via the D-037 chat contour with explicit model-knowledge labeling and its own `Query` + `AnswerTrace`; one cause-neutral fallback funnel → `diary_lookup`, requested vs effective preserved per R-6); `RouteKind.CHAT` + `/chat` token + dispatcher CHAT branch (keyword-only, disableable dependency per I-10; `_REPLY_HELP` inventory updated — the one deliberate pinned-literal change; `/sources` deliberately stays `/ask`-only); webhook wiring; `classifier_backend`/`classifier_model` knobs + `_verify_classifier_contour` boot gate. **Schema:** additive migration `0007.chat-route-decisions.sql` (new table, `requested_route` nullable vs `effective_route`, classifier provenance, nullable `query_id` link, no confidence column) + repository seam across mock/sqlite/postgres (D-107 SQLite upgrade asymmetry restated in D-109). **Tests:** seven new modules (model-only prompt, mock classifier, OpenAI classifier retry/shape offline, boot gate, decision storage mock+sqlite+PG-gated, routed service incl. R-3/H-3 scoping, dispatcher CHAT byte-pins) + seven extended (incl. `MIGRATION_COUNT=7` + 0007 upgrade-preserves-data, `/help` pin moved with sibling pins byte-identical, heuristic-classifier never-CHAT guard, e2e `/chat` round-trips with `/ask` parity). Full gate green (ruff + mypy strict + 806 passed / 82 PG-skipped; PG-gated legs green against a dedicated test DB). → RC-2 (D-109). |
| RC-3 — enrichment + rewriting + `diary_plus_llm` | **Done (D-110), pre-checkpoint.** **Code:** new core contracts `core/chat/rewrite.py` (`QueryRewrite` with seam-ready never-emitted `subject_scope` slot — A-46; `QueryRewriter` Protocol with caller-supplied `today`; D-035-mirroring failure split) and `core/chat/enriched_prompt.py` (`notes-plus-model-v1`, R-8 enforced, byte-pinned `ESCALATION_CLAUSE` making the D-108 medical amendment live, strict-where-honest parser with the consistency triple `no_evidence ⇔ () ⇔ ""`, lenient extra keys); new rewriter adapters in `adapters/chat_routing/` (mock + OpenAI forced `rewrite_query` function calling, no subject parameter, riding the `classifier_backend`/`classifier_model` contour — no new knobs, boot gate builds it via the same factory); `QueryService.retrieve` pure-retrieval extraction (+ `build_retrieval_hits` in `services/retrieval.py`; `answer` seam + behavior unchanged, persistence untouched); `services/routed_chat.py` dispatches `diary_plus_llm` (rewrite failure degrades cause-neutrally to the original question; empty retrieval still generates; caller's `subject_scope` only; own `Query` + per-leg hits + `AnswerTrace` storing the raw segmented JSON verbatim; `chat.enriched` log line); `AnswerResult.model_text` additive carriage; dispatcher segmented rendering (degradation header strictly before model content; `_TRAILER_MODEL_KNOWLEDGE` doubles as the model-segment label; three new pinned literals, every pre-existing literal byte-identical with guard tests). **Schema:** additive migration `0008.chat-query-rewrites.sql` (one nullable-rewrite row per route execution, FK + UNIQUE per decision, written strictly after the decision row) + repository seam across mock/sqlite/postgres (SQLite asymmetry restated in D-110). **Tests:** five new modules (prompt contract incl. escalation byte pin, mock rewriter, OpenAI rewriter retry/shape offline, rewrite storage mock+sqlite+PG-gated, route end-to-end incl. R-3/R-8 + `subject_scope` inside enrichment and red-flag escalation capture) + seven extended (funnel narrowed to `notes_plus_knowledge`, dispatcher reply byte-pins + sibling guards, mock chat-client branch, `retrieve` purity, `model_text` default, `MIGRATION_COUNT=8` + 0008 upgrade-preserves-data, e2e segmented round-trips with `/ask` unchanged). Full gate green (ruff + mypy strict + 883 passed / 89 skipped offline; PG-gated legs green against the dedicated test DB). → RC-3 (D-110). |
| RC-4 — `diary_plus_web` + closure | **Done (D-111), pre-checkpoint.** **Code:** new core contracts `core/chat/knowledge.py` (`KnowledgeSource` port, `KnowledgeExcerpt.ref` as an opaque locator — not welded to "web"; D-035-mirroring failure split), `core/chat/outward.py` (`OutwardQueryRewriter` — the D-108 outward rewrite conditioned on retrieved personal context, no `today` parameter), and `core/chat/knowledge_prompt.py` (`notes-plus-knowledge-v1`, six-field response, ESCALATION_CLAUSE **imported** from the RC-3 module, RC-3 notes triple + new knowledge consistency pair + fabricated-ref rejection); new `adapters/knowledge/` (mock + hardened Tavily over httpx — key only in the auth header, module-local retryable/`Retry-After` classification, `adapters/resilience.py` untouched) + `adapters/chat_routing/outward_{mock,openai}.py` riding the classifier contour (no new pin/knob set); `services/routed_chat.py` dispatches `diary_plus_web` when a knowledge source is wired (otherwise the RC-2 funnel survives): rewrite-to-kwargs → scoped enrichment retrieval → outward rewrite (failure degrades to the stripped original question) → search (failure degrades within the route to an empty knowledge plane; generation still runs) → one generation citing all three planes; `AnswerPrompt.knowledge_refs` + `AnswerResult.knowledge_text`/`knowledge_refs` additive; dispatcher three-segment rendering with one new pinned literal (`_LABEL_CHAT_WEB`, "web" register deliberate) and every pre-existing literal guard-tested byte-identical; `knowledge_backend`/`tavily_api_key`/`knowledge_max_results` knobs (A-47) + `_verify_knowledge_contour` boot gate; `/sources` stays `/ask`-only (owner decision, D-111). **Schema:** additive migration `0009.chat-knowledge-searches.sql` (one row per route execution incl. failed searches, outward-rewrite provenance folded in, FK + UNIQUE per decision) + repository seam across mock/sqlite/postgres (SQLite asymmetry restated in D-111). **Tests:** eight new modules (knowledge mock + Tavily offline transport, outward mock + OpenAI retry/shape offline, prompt contract incl. escalation identity+byte pin, knowledge-search storage mock+sqlite+PG-gated, route end-to-end incl. degradation contours and red-flag verification, boot gate) + the consolidated `tests/test_routed_chat_milestone.py` (all four routes at service granularity, mock + PG-gated) + six extended (dispatcher pins, mock chat client, domain models, `MIGRATION_COUNT=9` + 0009 upgrade-preserves-data, e2e three-segment round-trip with `/ask` unchanged). Full gate green (ruff + mypy strict + 994 passed / 96 skipped offline; PG-gated legs green against the dedicated test DB). **Docs:** RUNBOOK "Routed chat" operator section + drill subsection; `docs/routed-chat-drill/routed-chat-smoke-TEMPLATE.json`; I-9 / TechSpec §4 / GLOSSARY reconciliation; A-47 opened. → RC-4 (D-111). |
| RC-5 — drill evidence + milestone close | **Done (D-112), pre-checkpoint. Closes the milestone.** Operator-run real-backend drill per the RUNBOOK "Routed-chat real-backend smoke (RC closure drill / D-111)" subsection against the canonical real contour (postgres at migrations 0001..0009, openai embeddings/chat, `gpt-4.1-mini` classifier, tavily knowledge source): one seeded `/note` (chunk `ready` on the live embedding contour), then one `/chat` round trip per product-register route — every route green on its first steering attempt, `requested_route == effective_route` in every `chat_route_decisions` row, mixed-route `chat_query_rewrites` rows non-null, the `chat_knowledge_searches` row at `provider_name='tavily'` / `result_count=5` with the outward query carrying no person names (D-111 privacy seam observed), three Tavily URLs cited verbatim under the web label, all four `answer_traces` rows at the expected prompt versions with `fallback_mode='none'`, every provider call `attempt=1/3`. **New:** populated dated evidence artifact `docs/routed-chat-drill/routed-chat-smoke-20260612-evidence.json` (template-derived, `"_template"` dropped, credential values redacted, redaction grep clean, all summary booleans `true`). **Docs:** D-112 closure entry; roadmap / this block / `docs/todo.md` flipped to milestone-closed; A-10 edit/delete is now the top pick (owner re-queue, D-108). Docs + evidence only — no `src/` / `tests/` / schema / migration / config-surface change. → RC-5 (D-112). |

## SEC-1 — ASGI/idna Security Refresh with bounded-audit closure

Operational-hygiene milestone (Stage 2). Five Dependabot/pip security alerts surfaced at commit `2499490` (lockfile identical to HEAD). A bounded `pip-audit` sweep over the full lockfile confirmed four — three Starlette advisories + one idna — and found nothing beyond the Dependabot set; the fifth (pytest tmpdir) is OSV/GHSA-unconfirmed at the locked `pytest 8.4.2`, dev/test-only, and deferred (D-113). **Out of scope:** non-security freshness upgrades; the pytest tmpdir bump unless an authoritative advisory confirms it; the `TestClient` `httpx` → `httpx2` migration implied by the Starlette 1.x deprecation.

| Slice | Files / artifacts |
| --- | --- |
| SEC-1.1 — ASGI stack bump (Starlette 1.x via FastAPI) + idna, with compat gate | **Done (D-113), pre-checkpoint. Closes the milestone.** Confirmed ASGI/idna security closure as one coherent stack jump. **Manifest:** `pyproject.toml` FastAPI pin `>=0.115,<0.116` → `>=0.136,<0.137`; new **direct** `starlette>=1.0.1` security floor (taken because FastAPI 0.136.3's Starlette range still admits the vulnerable 0.46.2 and `uv lock` kept it — the carrier bump alone left Starlette unpatched; the plan's surfaced exception path). **Lock:** `uv.lock` — `fastapi` 0.115.14 → 0.136.3, `starlette` 0.46.2 → 1.3.1, `idna` 3.13 → 3.18 (≥ 3.15 via targeted upgrade), `annotated-doc 0.0.4` added (new FastAPI transitive); `anyio` / `httpx` (0.27.2 — no widening required) / `pytest` unchanged. Closes `CVE-2025-62727` (Range DoS, High), `CVE-2026-48710` (Host-header → `request.url.path`), `CVE-2025-54121` (multipart), `CVE-2026-45409` (idna). **Code:** none — no `src/` / `tests/` change; routes, dict responses, 200/401 contracts, boot gates untouched; no runtime behavior change. **Validation:** ruff + mypy strict clean (173 files); pytest 994 passed / 96 PG-skipped on the Starlette 1.x major; `pip-audit` over the updated lockfile = "No known vulnerabilities found" (all four CVEs closed). One non-fatal `StarletteDeprecationWarning` (1.x `TestClient` deprecates `httpx` → `httpx2`), dev/test-only, recorded as follow-up. **Docs:** D-113 entry + this row. No other doc hardcodes a dependency version. **Closure:** SEC-1 is a single-slice milestone — this row is also the closing row; closure runs through the existing D-113 (no new decision-log entry, mirroring the H-4 precedent) and reconciles `docs/todo.md` (new closed SEC-1 block). The deferred pytest tmpdir alert is recorded there as a carryover pointing back to D-113. A-10 edit/delete remains the top pick (owner re-queue, D-108). → SEC-1.1 (D-113). |
